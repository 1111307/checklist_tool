

 



配置核查

作业指导书















适合系统/数据库/中间件：windows7、windowsXP、中标麒麟、银河麒麟、mysql、sqlserver、达梦、redis、nginx、tomcat、apache。





目  录

（目录生成中…请右键选择【更新域】）



## 1 系统安全

## 1.1 操作系统、数据库管理系统、中间件等平台软件应及时安装补丁程序

判定标准：操作系统、数据库、中间件均已安装官方发布的最新安全补丁或经测评验证的对应版本，无长期未修复的高危漏洞；无法访问互联网的离线环境应有补丁离线导入记录与版本台账。

### 1.1.1 核查操作系统安装补丁情况

#### 1.1.1.1 Windows操作系统 (Win7, WinXP)

方法1：针对本地登录，使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“wmic qfe list brief”，查看结果。



方法2：开始-控制面板-系统和安全-Windows Update-查看更新历史记录。此界面会显示补丁名称、编号、安装信息等。



点击检查更新，查看可更新的补丁。



#### 1.1.1.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，首先更新本地的软件包列表，确保获取到最新的补丁信息，在命令行输入命令“sudo apt update”，结果示例如下。



接着检查可用的更新，在命令行输入命令“apt list --upgradable”，结果示例如下。



### 1.1.2 核查数据库补丁情况

#### 1.1.2.1 MySQL

 Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行“ SELECT VERSION();” 查看当前完整版本号。执行“SHOW STATUS LIKE 'patch%'; ”。





最后访问MySQL官方公告，在下载页面查找MySQL版本对应的最新补丁和更新。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

eXport LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行“ SELECT VERSION();” 查看当前完整版本号。执行“SHOW STATUS LIKE 'patch%'; ”。





最后访问MySQL官方公告，在下载页面查找MySQL版本对应的最新补丁和更新。

#### 1.1.2.2 SQL Server

Windows操作系统下的核查方法

在搜索框中输入“SSMS”后回车，打开SQL Server Management Studio (SSMS)，在连接服务器对话框中输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口，用以下查询语句查看补丁信息：

SELECT

@@VERSION AS '版本信息',

SERVERPROPERTY('ProductVersion') AS '产品版本',

SERVERPROPERTY('ProductLevel') AS '产品级别',

SERVERPROPERTY('Edition') AS '版本',

SERVERPROPERTY('ResourceVersion') AS '资源版本',

SERVERPROPERTY('ResourceLastUpdateDateTime') AS '资源最后更新'

GO



可查看到以下结果。



最后访问SQLServer官方公告，在下载页面查找SQLServer版本对应的最新补丁和更新。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令，查看补丁信息：

SELECT

@@VERSION AS '版本信息',

SERVERPROPERTY('ProductVersion') AS '产品版本',

SERVERPROPERTY('ProductLevel') AS '产品级别',

SERVERPROPERTY('Edition') AS '版本',

SERVERPROPERTY('ResourceVersion') AS '资源版本',

SERVERPROPERTY('ResourceLastUpdateDateTime') AS '资源最后更新'

GO



可查看到以下结果。



最后访问SQLServer官方公告，在下载页面查找SQLServer版本对应的最新补丁和更新。

#### 1.1.2.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入“select banner from v$version;”查看数据库版本信息。



接着在disql提示符下依次输入以下命令，查询补丁历史记录。

set linesize 200 pagesize 20

col patch_name for a20

col patch_desc for a50

col apply_time for a25

select patch_name, patch_desc, apply_time from v$patch_history order by apply_time desc;



结果示例如下：



最后根据需求更新补丁。

#### 1.1.2.4 Redis

Windows操作系统下的核查方法

打开Redis安装所在目录，在redis-cli.exe程序的当前目录下鼠标右键打开终端，命令行输入 “.\redis-cli -version ”查看版本号。本地执行命令截图如下：



最后访问 Redis官方发布页面，确认当前版本是否为最新稳定版。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行输入 “redis-cli --version”查看版本号。本地执行命令截图如下：



最后访问 Redis官方发布页面，确认当前版本是否为最新稳定版。

### 1.1.3 核查中间件补丁情况

#### 1.1.3.1 Nginx

打开Nginx安装目录在nginx.exe当前目录下打开中断执行命令“ .\nginx -v ”查看版本号，本地执行命令截图如下：



最后访问 Nginx官网 查看最新稳定版和安全公告。

#### 1.1.3.2 Tomcat

从官网下载的Tomcat压缩包解压后，其文件夹名称会包含版本号。



进入Tomcat安装目录下的 bin 文件夹，双击运行 version.bat 文件，屏幕上就会显示详细的版本。



访问 Apache Tomcat官网 版本补丁情况。查看当前使用的版本是否受已知漏洞影响，以及官方推荐的安全版本是多少。

#### 1.1.3.3 Apache

打开命令提示符或终端，进入Apache安装目录下的bin文件夹，执行命令“httpd -v”（Windows）或“apachectl -v”（中标麒麟、银河麒麟）查看当前Apache版本号。

最后访问Apache HTTP Server官方发布页面及安全公告，确认当前版本是否为最新稳定版，是否存在已知未修复的安全漏洞。

## 1.2 操作系统应安装防病毒软件并及时升级

判定标准：已安装防病毒软件且实时防护开启，病毒库定义日期与核查日期接近；麒麟系统未部署第三方防病毒软件时，kysec 安全机制应处于开启状态（非 disable）；未安装防病毒软件、防护关闭或病毒库长期未更新即不合规。

### 1.2.1 Windows7、windowsXP

点击系统开始图标，点选【设置】点击【隐私和安全性】，然后点击【Windows安全中心】，点选【病毒和威胁防护】。



在病毒和威胁防护部分，将显示电脑上当前已安装的防病毒软件。



注意：安装多个防病毒软件可能导致不利影响。例如，多个防病毒软件之间可能会出现冲突，或者系统后台同时运行导致系统运行卡顿或不稳定，因此请尽量避免出现这种情况。

命令行核查：使用win+R组合键打开运行框，输入cmd，命令行输入“wmic /namespace:\\root\\SecurityCenter2 path AntiVirusProduct get displayName,productState”，列出系统已注册的防病毒软件及产品状态；再结合“病毒和威胁防护”界面中的病毒定义版本日期，判断病毒库是否及时更新。

### 1.2.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，命令行输入“getstatus”。



可根据显示结果检查当前kysec状态，命令行输入“setstatus -p disable”可关闭。命令行输入“getstatus” 查看状态。



命令行输入“setstatus enable softmode”开启安全机制，但是开启安全机制需要重启系统生效。

	

命令行输入“dpkg -l | grep -iE "antivirus|clamav|360|qianxin"”（银河麒麟）或“rpm -qa | grep -iE "antivirus|clamav|360|qianxin"”（中标麒麟），核查是否安装防病毒软件；若部署 ClamAV，命令行输入“clamscan --version”查看病毒库版本日期，判断是否及时更新。

## 1.3 操作系统应按需求裁剪服务和端口

判定标准：仅保留业务必需的服务和端口，多余的系统服务已停止并禁用，无监听端口对应无用服务；保留的开放端口均有明确的业务用途说明。

### 1.3.1 Windows7、windowsXP

使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“services.msc”，查看所有服务，确认非必需服务（如Telnet, Bluetooth Support Service, Print Spooler（若无打印机））的启动类型为“禁用”。



### 1.3.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，按照需求在命令行输入以下命令查看服务器状态。

systemctl list-units --type=service --state=running

systemctl list-unit-files --type=service



结果示例如下。



检查端口开放情况，输入以下命令。

netstat -tulpn

netstat -tnp



结果示例如下。





根据需求禁用不必要的服务，输入如下命令。

sudo systemctl stop bluetooth

sudo systemctl disable bluetooth

sudo systemctl mask bluetooth



结果示例如下。



## 1.4 操作系统应具备防火墙功能

判定标准：防火墙服务处于启用状态且默认策略非全放行，已按业务需求配置入站/出站规则；防火墙关闭或安装未启用即不合规。

### 1.4.1 Windows7、WindowsXP

进入“控制面板” -> “Windows 防火墙”，查看防火墙状态是否为“启用”。



命令行核查：使用win+R组合键打开运行框，输入cmd，命令行输入“netsh advfirewall show allprofiles”，查看域配置文件、专用配置文件、公用配置文件的状态均应为“启用”。

### 1.4.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令查看系统是否具备防火墙功能。

sudo ufw status

sudo ufw status verbose



结果示例如下。



银河麒麟（firewalld）在命令行输入“systemctl status firewalld”查看防火墙服务状态，输入“firewall-cmd --state”确认防火墙运行中，输入“firewall-cmd --list-all”查看默认区域及已放行的服务与端口。

## 1.5 操作系统应停用冗余网络设置

判定标准：冗余网络协议组件已卸载或停用，无线、蓝牙等未使用的网络模块已禁用，仅保留业务所需的网络连接与协议组件。

### 1.5.1 Windows7、WindowsXP

进入控制面板，点击“网络和共享中心”，



接着点击“本地连接”，



然后点击“属性”，



仅勾选“QOS数据包计划程序”，然后点击“确定”。



### 1.5.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，命令行输入“ip link show”查看当前启用的网络接口，未使用的网桥、虚拟网卡等冗余接口不应长期处于 UP 状态。

输入“nmcli connection show”查看已配置的网络连接清单，仅保留业务所需的连接，未使用的有线、无线、VPN 连接配置应删除或停用。

输入“systemctl status bluetooth”查看蓝牙服务状态，未使用无线模块时应停止并禁用，命令行依次输入“sudo systemctl stop bluetooth”“sudo systemctl disable bluetooth”。

## 1.6 操作系统远程管理应开放唯一管理服务,指定管理终端并采取传输加密保护措施

判定标准：远程管理仅开放唯一管理服务，管理终端地址已限定，远程会话采用加密传输；同时开放多个远程管理通道、未限定管理终端或使用 Telnet 等明文协议即不合规。

### 1.6.1 Windows7、WindowsXP

首先核查远程桌面服务状态，使用win+R组合键打开运行框，输入services.msc回车，找到Remote Desktop Services：应设置为 “自动” 并处于 “正在运行” 状态、Remote Desktop Configuration：应处于 “正在运行” 状态。



控制面板” -> “程序” -> “启用或关闭Windows功能”。在列表中找到 “Telnet客户端” 和 “Telnet服务器”，确保它们都没有被勾选。如果已勾选，请取消勾选并确定。



核查是否指定了管理终端，通过Windows防火墙设置IP限制（图形化方法）：打开 “Windows Defender 防火墙” -> “高级设置”。点击 “入站规则”。在右侧找到名为 “远程桌面-用户模式(TCP-In)” 的规则。通常有域、专用、公用三个配置文件对应的规则。双击每条规则，进入 “作用域” 选项卡。在 “远程IP地址” 部分，选择 “下列IP地址” 并点击 “添加”。将允许远程管理的终端计算机的IP地址或IP地址段添加进去。如果“远程IP地址”设置为 “任何IP地址”，则不符合安全要求。













核查传输加密保护措施，按 Win + R，输入 gpedit.msc 并回车（注意：家庭版Windows无此功能）。导航到：计算机配置 -> 管理模板 -> Windows 组件 -> 远程桌面服务 -> 远程桌面会话主机 -> 安全。找到 “要求使用指定的安全层” 策略，双击打开。检查其设置：应设置为 “已启用”，并且安全层选项应为 “SSL”。这会强制连接使用TLS 1.0/1.2加密。找到 “设置客户端连接加密级别” 策略，双击打开。检查其设置：应设置为 “已启用”，并且加密级别应设置为 “高”。这会强制使用128位加密。

备选方案有使用注册表核查，按 Win + R，输入 regedit 并回车。导航到以下路径：“HKEY_LOCAL_MACHINE”，查看右侧的 SecurityLayer 和MinEncryptionLevel 值。SecurityLayer 值为 2 表示协商SSL/TLS加密。MinEncryptionLevel 值为 3 表示使用128位加密。





### 1.6.2 中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令识别远程管理服务。

sudo netstat -tulpn | grep LISTEN

sudo ss -tulpn | grep LISTEN



结果示例如下。



接着，配置唯一安全的远程管理服务，在命令行输入以下命令。

sudo apt update

sudo apt install openssh-server -y

sudo systemctl start ssh

sudo systemctl enable ssh

sudo systemctl status ssh





然后，指定管理终端，只允许特定IP访问SSH，输入以下命令。

sudo ufw allow from 192.168.1.0/24 to any port 22 comment "管理网络SSH访问"



## 1.7 数据库管理系统应删除冗余帐户,应设置不少于8个字符且字符采用字母大小写，数字及特殊字符混合编制的账户口令

判定标准：数据库无测试、过期、锁定及与业务无关的冗余账户；各账户口令不少于8个字符且由字母大小写、数字及特殊字符混合编制，未使用默认口令与空口令。

### 1.7.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。首先需要查看当前MySQL服务器上的所有用户账户，命令行输入“SELECT User, Host FROM mysql.user;”用于识别哪是冗余的。



使用DROP USER语句删除不再需要的冗余账户，语句如下。

DROP USER 'redundant_user'@'%';

DROP USER ''@'localhost';



为确保新密码和修改的密码符合要求，命令行输入“INSTALL PLUGIN validate_password SONAME 'validate_password.so';”安装和配置密码验证插件。

为保留的账户设置符合新策略的强密码，语句如下。

ALTER USER 'test_user'@'localhost' IDENTIFIED WITH mysql_native_password BY 'SecureP@sswerd';

ALTER USER 'developer'@'192.168.1.%' IDENTIFIED WITH mysql_native_password BY 'AnotherSecureP@ss123';





中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。首先需要查看当前MySQL服务器上的所有用户账户，命令行输入“SELECT User, Host FROM mysql.user;”用于识别哪是冗余的。



使用DROP USER语句删除不再需要的冗余账户，语句如下。

DROP USER 'redundant_user'@'%';

DROP USER ''@'localhost';



为确保新密码和修改的密码符合要求，命令行输入“INSTALL PLUGIN validate_password SONAME 'validate_password.so';”安装和配置密码验证插件。

为保留的账户设置符合新策略的强密码，语句如下。

ALTER USER 'test_user'@'localhost' IDENTIFIED WITH mysql_native_password BY 'SecureP@sswerd';

ALTER USER 'developer'@'192.168.1.%' IDENTIFIED WITH mysql_native_password BY 'AnotherSecureP@ss123';





### 1.7.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。识别冗余账户语句如下。

SELECT name, create_date, type_desc, is_disabled

FROM sys.server_principals

WHERE type IN('S','U')

ORDER BY name;



执行结果：



确认账户冗余后，使用 “DROP LOGIN [login_name];”语句删除它们，例如：“DROP LOGIN [redundant_user];”。

删除服务器登录后，与之关联的数据库用户可能变为“孤立用户”检测特定数据库中的孤立用户的语句如下。

USE [Your_Database_Name];

GO

EXEC sp_change_users_login @Action='Report';



删除孤立的数据库用户的语句如下。

sql

USE [Your_Database_Name];

GO

DROP USER [old_user]; 



将孤立用户重新映射到新的登录名的语句如下。

USE [Your_Database_Name];

GO

ALTER USER [old_user] WITH LOGIN = [new_login];



输入以下命令，查看所有SQL Server登录账户的密码策略设置：

SELECT 

    name AS LoginName,

    type_desc AS LoginType,

    is_disabled AS IsDisabled,

    is_policy_checked AS PasswordPolicyEnforced,

    is_expiration_checked AS PasswordExpirationEnforced,

    lockout_time AS LockoutTime,

    create_date AS CreateDate,

    modify_date AS ModifyDate

FROM sys.sql_logins

ORDER BY name;



结果示例如下：



接着，输入以下命令查看SQL Server实例级别的密码策略配置：

EXEC sp_configure 'password policy';

EXEC sp_configure 'password expiration';

EXEC sp_configure 'password lockout';



结果示例如下：



输入以下命令配置密码策略：

CREATE LOGIN [new_secure_user]

WITH PASSWORD = 'Sec@rePwd789',

CHECK_POLICY = ON,

CHECK_EXPIRATION= OFF;

ALTER LOGIN [existing_user]

WITH PASSWORD = 'N3wStrOngP@ss',

CHECK_POLICY = ON;



为现有账户设置强密码的语句如下。

ALTER LOGIN [sa] WITH PASSWORD = 'YourV3ryStrOngP@sswerd!' ;

ALTER LOGIN [report_user] WITH PASSWORD = 'R3pOrt!Secure2024';





中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令，识别冗余账户。

SELECT name, create_date, type_desc, is_disabled

FROM sys.server_principals

WHERE type IN('S','U')

ORDER BY name;



执行结果：



确认账户冗余后，使用 “DROP LOGIN [login_name];”语句删除它们，例如：“DROP LOGIN [redundant_user];”。

删除服务器登录后，与之关联的数据库用户可能变为“孤立用户”检测特定数据库中的孤立用户的语句如下。

USE [Your_Database_Name];

GO

EXEC sp_change_users_login @Action='Report';



删除孤立的数据库用户的语句如下。

sql

USE [Your_Database_Name];

GO

DROP USER [old_user];



将孤立用户重新映射到新的登录名的语句如下。

USE [Your_Database_Name];

GO

ALTER USER [old_user] WITH LOGIN = [new_login];



输入以下命令，查看所有SQL Server登录账户的密码策略设置：

SELECT 

    name AS LoginName,

    type_desc AS LoginType,

    is_disabled AS IsDisabled,

    is_policy_checked AS PasswordPolicyEnforced,

    is_expiration_checked AS PasswordExpirationEnforced,

    lockout_time AS LockoutTime,

    create_date AS CreateDate,

    modify_date AS ModifyDate

FROM sys.sql_logins

ORDER BY name;



结果示例如下：



接着，输入以下命令查看SQL Server实例级别的密码策略配置：

EXEC sp_configure 'password policy';

EXEC sp_configure 'password expiration';

EXEC sp_configure 'password lockout';



结果示例如下：



输入以下命令配置密码策略：

CREATE LOGIN [new_secure_user]

WITH PASSWORD = 'Sec@rePwd789',

CHECK_POLICY = ON,

CHECK_EXPIRATION= OFF;

ALTER LOGIN [existing_user]

WITH PASSWORD = 'N3wStrOngP@ss',

CHECK_POLICY = ON;



为现有账户设置强密码的语句如下。

ALTER LOGIN [sa] WITH PASSWORD = 'YourV3ryStrOngP@sswerd!' ;

ALTER LOGIN [report_user] WITH PASSWORD = 'R3pOrt!Secure2024';





### 1.7.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令查看所有用户账户。

set linesize 150 pagesize 30

col username for a20

col account_status for a20

col created for a20

col profile for a20

select

username,

account_status,

to_char(created, 'YYYY-MM-DD HH24:MI:SS') as created,

profile,

default_tablespace

from dba_users

order by created desc;



结果示例如下：



然后在disql提示符下输入以下命令，检查锁定、过期或长期未使用的账户。

select

username,

account_status,

to_char(created, 'YYYY-MM-DD') as created_date,

to_char(last_login, 'YYYY-MM-DD') as last_login_date

from dba_users

where account_status not in ('OPEN', 'LOCKED(TIMED)')

or username in ('GUEST', 'PUBLIC', 'TEST%', 'DEV%', 'TEMP%')

or (last_login is not null and months_between(sysdate, last_login) > 6)

order by created_date desc;



接着检查密码策略配置，在disql提示符下输入以下命令，检查密码配置信息。

col profile for a20

col resource_name for a30

col limit for a20

select

profile,

resource_name,

limit

from dba_profiles

where resource_type = 'PASSWORD'

order by profile, resource_name;



结果示例如下：



如果检查发现不合规，按基线修改 PROFILE：“ALTER PROFILE DEFAULT LIMIT PASSWORD_LIFE_TIME 90;”“ALTER PROFILE DEFAULT LIMIT PASSWORD_GRACE_TIME 7;”“ALTER PROFILE DEFAULT LIMIT FAILED_LOGIN_ATTEMPTS 6;”“ALTER PROFILE DEFAULT LIMIT PASSWORD_LOCK_TIME 1;”“ALTER PROFILE DEFAULT LIMIT PASSWORD_REUSE_TIME 180;”“ALTER PROFILE DEFAULT LIMIT PASSWORD_REUSE_MAX 5;

如需启用复杂度校验函数：“ALTER PROFILE DEFAULT LIMIT PASSWORD_VERIFY_FUNCTION DM_VERIFY_PWD;”把业务用户统一绑定到合规 profile：“ALTER USER 用户名 PROFILE DEFAULT;”。

### 1.7.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，先双击redis-server.exe，再双击redis-cli.exe连接到redis。 





Redis6.0以下没有多用户概念，只有默认用户，所以这里只讨论6.0以上版本，删除冗余账户删除名为 'redundant_user' 的 ACL 用户命令行键入“ACL DELUSER redundant_user”。

设置强密码策略则可以修改 Redis 配置文件 (通常是 redis.conf)找到 requirepass 配置项，并将 foobared 替换为强密码：requirepass YourStrongPasswordHere!2024，重启 Redis 服务使配置生效。

检查方法：查 requirepass（适用于老版本或未使用 ACL 的情况）“redis-cli CONFIG GET requirepass”。查 ACL 用户和密码（Redis 6+）

“redis-cli ACL LIST”。确认真正使用的配置文件路径（如果有权限）“ps aux | grep redis”或“systemctl status redis”查看启动参数中是否有 --config /path/to/redis.conf。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



Redis6.0以下没有多用户概念，只有默认用户，所以这里只讨论6.0以上版本，删除冗余账户删除名为 'redundant_user' 的 ACL 用户命令行键入“ACL DELUSER redundant_user”。

设置强密码策略则可以修改 Redis 配置文件 (通常是 redis.conf)找到 requirepass 配置项，取消注释（删除行首的 #），并将 foobared 替换为强密码：requirepass YourStrongPasswordHere!2024，重启 Redis 服务使配置生效。

检查方法：查 requirepass（适用于老版本或未使用 ACL 的情况）“redis-cli CONFIG GET requirepass”。查 ACL 用户和密码（Redis 6+）

“redis-cli ACL LIST”。确认真正使用的配置文件路径（如果有权限）“ps aux | grep redis”或“systemctl status redis”查看启动参数中是否有 --config /path/to/redis.conf。

## 1.8 数据库管理系统应删除冗余存储过程

判定标准：数据库中 xp_cmdshell、sp_oacreate 等高危系统存储过程已禁用或移除，无业务不再使用的自定义存储过程残留；高危存储过程可被普通用户调用即不合规。

首先对于各个操作系统冗余存储过程可能具备以下一个或多个特征：功能重复：多个存储过程实现相同或高度相似的业务逻辑。

长期未使用：在较长时间内（例如半年或一年）都未被任何程序、作业或其他存储过程调用。已被取代：由于业务逻辑变更或优化，存在新版存储过程，旧版已不再使用。关联对象已不存在：存储过程所依赖的表、视图或其他存储过程等已不存在。

### 1.8.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入以下语句，查看所有存储过程。

USE 'your_database_name';

SELECT ROUTINE_NAME, ROUTINE_TYPE, ROUTINE_DEFINITION, CREATED

FROM information_schema.ROUTINES

WHERE ROUTINE_SCHEMA = DATABASE( )

AND ROUTINE_TYPE = 'PROCEDURE'

ORDER BY ROUTINE_NAME;



运行结果如下。



命令行输入“DROP PROCEDURE IF EXISTS ‘old_backup_procedure’;”删除存储过程old_backup_procedure。

确认删除结果，

SELECT ROUTINE_NAME

FROM information_schema.ROUTINES

WHERE ROUTINE_SCHEMA = DATABASE( )

AND ROUTINE_TYPE = 'PROCEDURE';



运行结果如下。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入以下语句，查看所有存储过程。

USE 'your_database_name';

SELECT ROUTINE_NAME, ROUTINE_TYPE, ROUTINE_DEFINITION, CREATED

FROM information_schema.ROUTINES

WHERE ROUTINE_SCHEMA = DATABASE( )

AND ROUTINE_TYPE = 'PROCEDURE'

ORDER BY ROUTINE_NAME;



运行结果如下。



命令行输入“DROP PROCEDURE IF EXISTS ‘old_backup_procedure’;”删除存储过程old_backup_procedure。

确认删除结果，

SELECT ROUTINE_NAME

FROM information_schema.ROUTINES

WHERE ROUTINE_SCHEMA = DATABASE( )

AND ROUTINE_TYPE = 'PROCEDURE';



运行结果如下。



### 1.8.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。使用目标数据库“USE YourDatabaseName;”。查看当前数据库中的所有用户定义的存储过程，语句如下。

SELECT

SCHEMA_NAME(schema_id) AS [Schema Name],

name AS [Procedure Name],

create_date,

modify_date

FROM sys.objects

WHERE type = 'P'

ORDER BY [Schema Name], [Procedure Name];



运行结果



删除存储过程，语句如下。

DROP PROCEDURE IF EXISTS dbo.usp_OldBackupProcedure;

确认删除结果，语句如下。

SELECT

SCHEMA_NAME (schema_id) AS [Schema Name],

name AS [Procedure Name]

FROM sys.objects

WHERE type = 'P'

ORDER BY [Schema Name], [Procedure Name];



运行结果示例截图。



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入命令“USE YourDatabaseName;”使用目标数据库。查看当前数据库中的所有用户定义的存储过程，输入如下语句。

SELECT

SCHEMA_NAME(schema_id) AS [Schema Name],

name AS [Procedure Name],

create_date,

modify_date

FROM sys.objects

WHERE type = 'P'

ORDER BY [Schema Name], [Procedure Name];



运行结果



删除存储过程，语句如下。

DROP PROCEDURE IF EXISTS dbo.usp_OldBackupProcedure;

确认删除结果，语句如下。

SELECT

SCHEMA_NAME (schema_id) AS [Schema Name],

name AS [Procedure Name]

FROM sys.objects

WHERE type = 'P'

ORDER BY [Schema Name], [Procedure Name];



运行结果示例截图。



### 1.8.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令,统计所有用户下的存储过程总数。

select

owner as所属用户，

count(*) as存储过程数量，

count(distinct object_name) as 不同名称数量

from dba_objects

where object_type = 'PROCEDURE'

group by owner

order by count(*) desc;



结果示例如下：



接着检查无效的存储过程，在disql提示符下输入以下命令，查找状态为INVALID的存储过程。

select

owner,

object_name,

to_char(created, 'YYYY-MM-DD') as created_date,

to_char(last_ddl_time, 'YYYY-MM-DD') as last_modified,

case

when months_between(sysdate,last_ddl_time) >6 then '超过6个月未修复'

else '近期失效'

end as失效状态

from dba_objects

where object_type = 'PROCEDURE'

and status = 'INVALID'

order by last_ddl_time;



结果示例如下：



若没有失效存储过程，则符合要求；若有，按照需求及时删除冗余存储过程。

删除冗余存储过程时，可根据上一步查询结果确认存储过程所属模式和名称，在disql提示符下输入“DROP PROCEDURE IF EXISTS 模式名.存储过程名;”执行删除。

若删除当前登录模式下的存储过程，也可输入“DROP PROCEDURE IF EXISTS 存储过程名;”。删除完成后，重新执行失效存储过程查询语句，确认对应记录已不存在。

### 1.8.4 Redis

#### Redis没有存储过程。

## 1.9 数据库管理系统应具有基于表级增删改查等细粒度访问和管理授权功能

### 1.9.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入“SHOW GRANTS FOR 'username'@'localhost';”查看特定用户的所有权限。



命令行输入以下语句查询系统权限表。

SELECT db, user, table_name, table_priv, column_priv

FROM mysql.tables_priv

WHERE table_name = 'your_table_name';



运行结果



命令行输入以下语句授予与回收表级权限。

GRANT SELECT,INSERT,UPDATE, DELETE ON mydb.mytable TO 'someuser'@'somehost';

REVOKE INSERT, DELETE ON mydb.mytable FROM 'someuser'@'somehost';



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入“SHOW GRANTS FOR 'username'@'localhost';”查看特定用户的所有权限。

命令行输入以下语句查询系统权限表。



SELECT db, user, table_name, table_priv, column_priv

FROM mysql.tables_priv

WHERE table_name = 'your_table_name';



运行结果：



命令行输入以下语句授予与回收表级权限。

GRANT SELECT,INSERT,UPDATE, DELETE ON mydb.mytable TO 'someuser'@'somehost';

REVOKE INSERT, DELETE ON mydb.mytable FROM 'someuser'@'somehost';



### 1.9.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

命令行输入以下语句查看当前数据库中所有针对特定对象（如表）的权限分配情况。

SELECT

perm.class_desc,

prin.[name] AS [Principal Name],

prin.[type_desc] AS [Principal Type],

obj.[name] AS [Object Name],

obj.[type_desc] AS [Object Type],

perm.[permission_name],

perm.[state_desc]

FROM

sys.database_permissions perm

INNER JOIN

sys.database_principals prin ON perm.grantee_principal_id= prin.principal_id

LEFT JOIN

sys.objects obj ON perm.major_id = obj.object_id

WHERE

obj.[name] = 'YourTableName'

AND obj.[type]='U'

ORDER BY

prin.[name], perm.[permission_name];



运行结果



命令行输入以下语句授予表级权限功能操作。

GRANT SELECT, INSERT ON dbo.Employees TO app_user;

GRANT SELECT, INSERT, UPDATE, DELETE ON dbo.Employees TO hr_role;



命令行输入以下语句回收表级权限功能操作。

REVOKE INSERT ON dbo.Employees TO app_user;



命令行输入以下语句拒绝权限功能操作。

DENY DELETE ON dbo.Employees TO temp_user;



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令查看当前数据库中所有针对特定对象（如表）的权限分配情况。

SELECT

perm.class_desc,

prin.[name] AS [Principal Name],

prin.[type_desc] AS [Principal Type],

obj.[name] AS [Object Name],

obj.[type_desc] AS [Object Type],

perm.[permission_name],

perm.[state_desc]

FROM

sys.database_permissions perm

INNER JOIN

sys.database_principals prin ON perm.grantee_principal_id= prin.principal_id

LEFT JOIN

sys.objects obj ON perm.major_id = obj.object_id

WHERE

obj.[name] = 'YourTableName'

AND obj.[type]='U'

ORDER BY

prin.[name], perm.[permission_name];



运行结果：



命令行输入以下语句授予表级权限功能操作。

GRANT SELECT, INSERT ON dbo.Employees TO app_user;

GRANT SELECT, INSERT, UPDATE, DELETE ON dbo.Employees TO hr_role;



命令行输入以下语句回收表级权限功能操作。

REVOKE INSERT ON dbo.Employees TO app_user;



命令行输入以下语句拒绝权限功能操作。

DENY DELETE ON dbo.Employees TO temp_user;



### 1.9.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，首先检查细粒度访问控制（FGAC）功能。在disql提示符下输入以下命令， 查看是否存在虚拟私有数据库（VPD）策略（达梦可能使用视图或存储过程实现）。

select

owner,

object_name,

object_type,

created

from dba_objects

where object_name like '%POLICY%'

or object_name like '%VPD%'

or object_name like '%FGAC%'

order by created desc;



结果示例如下：



接着检查行级安全策略，在disql提示符下输入以下命令，查看是否存在行级安全视图。

select

owner,

view_name,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_views

where view_name like '%SECURE%'

or view_name like '%RESTRICT%'

or view_name like '%FILTER%'

order by created_date desc;



结果示例如下：



然后检查列级权限授予情况，在disql提示符下输入以下命令，查看列级权限（达梦通过视图或存储过程实现）。

select table_name

from dba_tables

where table_name like '%COLUMN%PRIV%'

or table_name like '%COL PRIV%';



随后，在disql提示符下输入以下命令，检查通过视图实现的列级控制。

select

owner,

view_name,

text_length

from dba_views

where text like '%CASE%WHEN%'

or text like '%DECODE%'

order by text_length desc

fetch first 10 rows only;



结果示例如下：



授予表级权限功能操作。在disql提示符下可输入“GRANT SELECT, INSERT, UPDATE, DELETE ON 模式名.表名 TO 用户名;”将指定表的增删改查权限授予目标用户；如允许目标用户继续转授，可在语句末尾加“WITH GRANT OPTION”。

回收表级权限功能操作。在disql提示符下可输入“REVOKE SELECT, INSERT, UPDATE, DELETE ON 模式名.表名 FROM 用户名;”回收已授予的对象权限；若该权限已被继续转授，可根据实际情况在语句末尾加“CASCADE”进行级联回收。

如需进行列级授权，可按需输入“GRANT SELECT(列名), UPDATE(列名) ON 模式名.表名 TO 用户名;”；回收列级权限时，输入“REVOKE SELECT, UPDATE ON 模式名.表名 FROM 用户名;”统一回收对应类型的列权限。

### 1.9.4 Redis

判定标准：default用户已禁用或已设置强密码且未开放+@all全部命令权限；各业务用户按最小权限原则仅授权所需键模式和命令类别；禁止存在无密码（nopass）且高权限的用户。

6.0以下版本，输入“CONFIG GET requirepass”确认已设置登录密码，并说明该版本无ACL功能，仅能通过单一密码认证，无法进行用户级细粒度授权。

6.0及以上版本，依次输入“ACL LIST”“ACL GETUSER default”查看用户列表及default用户权限，输入“ACL SETUSER app_user on >AppPass123! ~业务键前缀* +@read +@write -@dangerous”按最小权限配置用户，输入“ACL DELUSER 用户名”删除多余用户。

在桌面鼠标右键-打开终端，命令行输入“redis-cli -a 密码”连接Redis服务，输入“INFO server”确认redis_version。

中标麒麟、银河麒麟操作系统下的核查方法

按最小权限原则创建或修改用户，输入“ACL SETUSER app_user on >AppPass123! ~业务键前缀* +@read +@write -@dangerous”，按需限制可访问的键模式（~pattern）和命令类别，禁止给业务用户授权+@all全部命令。

ACL CAT

ACL WHOAMI

ACL GETUSER default

ACL LIST

6.0及以上版本，依次输入以下命令核查用户与权限：

首先查看Redis版本，在命令行输入“INFO server”，确认redis_version。6.0以下版本没有ACL用户概念，只有默认的单一管理员身份，通过requirepass密码认证，无法进行用户级细粒度授权。

打开Redis安装目录，在redis-cli.exe程序的当前目录下鼠标右键打开终端，命令行输入“redis-cli.exe”连接Redis服务，如已设置密码，输入“AUTH 密码”进行认证。

Windows操作系统下的核查方法

## 1.10 数据库管理系统应具有自主访问控制功能

判定标准：数据库库、表、视图、存储过程等对象均已定义属主与授权规则，普通用户仅能按授权访问对象，不存在 PUBLIC 等公共角色拥有非授权对象访问权限的情形。

### 1.10.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。输入“SELECT user, host FROM mysql.user; ”列出所有用户。



输入“ SHOW GRANTS FOR 'user'@'host'; ”仔细查看用户的权限，及时删除或锁定多余的、过期的账户以及测试后遗留的账户。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。输入“SELECT user, host FROM mysql.user; ”列出所有用户。



输入“ SHOW GRANTS FOR user@'host'; ”仔细查看用户的权限，及时删除或锁定多余的、过期的账户以及测试后遗留的账户。



### 1.10.2 SQLServer

Windows操作系统下的核查方法

检查身份验证模式，在SQL Server Management Studio (SSMS) 中，右键点击服务器实例，选择“属性”，查看“安全性”选项卡下的“服务器身份验证”。



使用sql语句进行辅助核查。登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

输入以下语句进行核查。

SELECT name, type_desc, is_disabled

FROM sys.server_principals

WHERE IS_SRVROLEMEMBER ('sysadmin', name) = 1;

SELECT dp.name AS DatabaseUser, r.name AS RoleName

FROM sys.database_principals dp

LEFT JOIN sys.database_role_members rm ON dp.principal_id = rm.member_principal_id

LEFT J0IN sys.database_principals r ON rm.role_principal_id = r.principal_id

WHERE dp.type IN ('U', 'S', 'G');

SELECT USER_NAME(grantee_principal_id) AS UserName,

OBJECT_NAME(major_id) AS ObjectName,

permission_name AS Permission

FROM sys.database_permissions

WHERE major_id > 0;



检查服务器角色成员结果示例。



检查数据库用户及角色结果示例。



检查具体对象的权限结果示例。



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令进行核查。

SELECT name, type_desc, is_disabled

FROM sys.server_principals

WHERE IS_SRVROLEMEMBER ('sysadmin', name) = 1;

SELECT dp.name AS DatabaseUser, r.name AS RoleName

FROM sys.database_principals dp

LEFT JOIN sys.database_role_members rm ON dp.principal_id = rm.member_principal_id

LEFT J0IN sys.database_principals r ON rm.role_principal_id = r.principal_id

WHERE dp.type IN ('U', 'S', 'G');

SELECT USER_NAME(grantee_principal_id) AS UserName,

OBJECT_NAME(major_id) AS ObjectName,

permission_name AS Permission

FROM sys.database_permissions

WHERE major_id > 0;



检查服务器角色成员结果示例。



检查数据库用户及角色结果示例。



检查具体对象的权限结果示例。



### 1.10.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法



在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，检查GRANT/REVOKE权限管理功能，首先在disql提示符下输入以下命令，查看系统权限授予情况。

col grantee for a20

col privilege for a30

col admin_option for a10

select

grantee,

privilege,

admin_option

from dba_sys_privs

where grantee not in ('PUBLIC', 'DBA')

order by grantee, privilege

fetch first 15 rows only;



结果示例如下：



接着，查看对象权限授予情况，在disql提示符下输入以下命令：

col table_name for a25

col privilege for a15

col grantable for a10

select

grantee,

owner,

table_name,

privilege,

grantable

from dba_tab_privs

where owner ='HR'

and table_name = 'EMPLOYEE'

order by grantee, privilege;



结果示例如下：



### 1.10.4 Redis

Windows操作系统下的核查方法

核查自主访问控制，有以下核查项目需通过。



打开redis安装目录，先双击redis-server.exe，再双击redis-cli.exe连接到redis。 





输入“CONFIG GET requirepass”，返回非空，密码认证已启用。



输入“CONFIG GET port”，查看占用端口。



 输入“CONFIG GET rename-command”，检查危险命令已禁用。



输入CONFIG GET bind，核查仅绑定必要IP。



输入CONFIG GET protected-mode，检查保护模式已开启。



中标麒麟、银河麒麟操作系统下的核查方法

核查自主访问控制，有以下核查项目需通过。



在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



输入“CONFIG GET requirepass”，返回非空，密码认证已启用。



输入“CONFIG GET port”，查看占用端口。



 输入“CONFIG GET rename-command”，检查危险命令已禁用。



输入“CONFIG GET bind”，核查仅绑定必要IP。



输入“CONFIG GET protected-mode”，检查保护模式已开启。



## 1.11 数据库管理系统应具有备份和恢复功能

判定标准：已配置定期备份策略且备份文件按期生成并异地或离线存放，备份保留周期满足要求，抽查最近一次备份可在测试环境成功恢复；仅依赖人工临时备份且无恢复验证记录即不合规。

说明：本章节说明各个数据库的备份方法和恢复方法。恢复功能核查应优先在测试环境、备机或临时恢复库中进行，避免在生产库直接执行覆盖式恢复操作；若现场不具备恢复演练条件，可通过核查恢复命令、备份可读性校验结果及最近一次恢复演练记录进行佐证。

### 1.11.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

检查数据库备份状态和配置。首先，检查数据库大小，评估备份需求，执行以下命令：

SELECT

table_schema AS'数据库',

ROUND(sUM(data_length + index_length) / 1024 / 1024, 2) AS '大小(MB)'

FROM information_schema.tables

GROUP BY table_schema

ORDER BY SUM(data_length + index_length) DESC;





接着，检查二进制日志状态（关键for增量备份），在MySQL数据库中继续执行以下命令：

SHOWVARIABLES LIKE 'log_bin';

SHOW VARIABLES LIKE 'binlog_format';

SHOW BINARY LOGS;



最后，检查备份相关参数，在MySQL数据库中执行以下命令：

SHOWVARIABLES LIKE' innodb_file_per_table';

SHOW VARIABLES LIKE 'sync_binlog';

SHOW VARIABLES LIKE 'innodb_flush_log_at_trx_commit';

	

核查备份历史记录。首先，创建备份记录表（如果不存在），执行以下命令：

CREATE TABLE IF NOT EXISTS backup_audit (

id INT AUTO_INCREMENT PRIMARY KEY,

backup_type VARCHAR(50),

database_name VARCHAR(100)

backup_size_mb DECIMAL(10,2),

backup_time DATETIME,

status VARCHAR(20),

backup_file VARCHAR(255),

created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



接着，查询备份历史记录，在MySQL数据库中继续执行以下命令：

SELECT

backup_type,

database_name,

COUNT(*) as backup_count,

MAX(backup_time) as latest_backup,

AVG(backup_size_mb) as avg_size_mb

FROM backup_audit

WHERE backup_time > DATE_SUB(NOW( ), INTERVAL 30 DAY)

GROUP BY backup_type, database_name

ORDER BY latest_backup DESC;



最后，检查最近七天备份成功率，在MySQL数据库中执行以下命令：

SELECT

DATE(backup_time) as backup_date,

backup_type,

COUNT(*) as total,

SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END) as success,

ROUND(SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END) * 100 /COUNT(*), 2) as success_rate

FROM backup_audit

WHERE backup_time > DATE_SUB(NOW( ), INTERVAL 7 DAY)

GROUP BY DATE(backup_time), backup_type

ORDER BY backup_date DESC;



补充恢复功能核查。建议优先在测试环境或临时恢复库中进行验证，若备份文件为.sql逻辑备份，可先在命令行界面输入“mysqladmin -uroot -p create restore_verify”创建恢复验证库。

然后输入“mysql restore_verify < 备份文件路径\dump.sql”将最近一次备份导入恢复验证库；若备份文件包含CREATE DATABASE和USE语句，也可直接输入“mysql < 备份文件路径\dump.sql”进行导入。

恢复完成后，重新连接MySQL，执行“USE restore_verify; SHOW TABLES;”或查询关键业务表记录数，确认备份数据可正常恢复；若现场采用物理备份工具，应同时核查对应恢复脚本或最近一次恢复演练记录。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

检查数据库备份状态和配置。首先，检查数据库大小，评估备份需求，执行以下命令：

SELECT

table_schema AS'数据库',

ROUND(sUM(data_length + index_length) / 1024 / 1024, 2) AS '大小(MB)'

FROM information_schema.tables

GROUP BY table_schema

ORDER BY SUM(data_length + index_length) DESC;



接着，检查二进制日志状态（关键for增量备份），在MySQL数据库中继续执行以下命令：

SHOWVARIABLES LIKE 'log_bin';

SHOW VARIABLES LIKE 'binlog_format';

SHOW BINARY LOGS;



最后，检查备份相关参数，在MySQL数据库中执行以下命令：

SHOWVARIABLES LIKE' innodb_file_per_table';

SHOW VARIABLES LIKE 'sync_binlog';

SHOW VARIABLES LIKE 'innodb_flush_log_at_trx_commit';



核查备份历史记录。首先，创建备份记录表（如果不存在），执行以下命令：

CREATE TABLE IF NOT EXISTS backup_audit (

id INT AUTO_INCREMENT PRIMARY KEY,

backup_type VARCHAR(50),

database_name VARCHAR(100)

backup_size_mb DECIMAL(10,2),

backup_time DATETIME,

status VARCHAR(20),

backup_file VARCHAR(255),

created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



接着，查询备份历史记录，在MySQL数据库中继续执行以下命令：

SELECT

backup_type,

database_name,

COUNT(*) as backup_count,

MAX(backup_time) as latest_backup,

AVG(backup_size_mb) as avg_size_mb

FROM backup_audit

WHERE backup_time > DATE_SUB(NOW( ), INTERVAL 30 DAY)

GROUP BY backup_type, database_name

ORDER BY latest_backup DESC;



最后，检查最近七天备份成功率，在MySQL数据库中执行以下命令：

SELECT

DATE(backup_time) as backup_date,

backup_type,

COUNT(*) as total,

SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END) as success,

ROUND(SUM(CASE WHEN status = 'SUCCESS' THEN 1 ELSE 0 END) * 100 /COUNT(*), 2) as success_rate

FROM backup_audit

WHERE backup_time > DATE_SUB(NOW( ), INTERVAL 7 DAY)

GROUP BY DATE(backup_time), backup_type

ORDER BY backup_date DESC;



补充恢复功能核查。建议优先在测试环境或临时恢复库中进行验证，若备份文件为.sql逻辑备份，可先在终端输入“mysqladmin -uroot -p create restore_verify”创建恢复验证库。

然后输入“mysql restore_verify < 备份文件路径/dump.sql”将最近一次备份导入恢复验证库；若备份文件包含CREATE DATABASE和USE语句，也可直接输入“mysql < 备份文件路径/dump.sql”进行导入。

恢复完成后，重新连接MySQL，执行“USE restore_verify; SHOW TABLES;”或查询关键业务表记录数，确认备份数据可正常恢复；若现场采用物理备份工具，应同时核查对应恢复脚本或最近一次恢复演练记录。

### 1.11.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

检查备份历史记录，在查询窗口执行以下命令：

SELECT

database_name,

type,

CASE type

WHEN 'D' THEN '完整备份'

WHEN 'I' THEN '差异备份'

WHEN 'L' THEN '事务日志备份'

ELSE '其他'

END AS backup_type,

backup_start_date,

backup_finish_date,

backup_size/1024/1024 AS backup_size_mb,

recovery_model,

physical_device_name

FROM msdb.dbo.backupset bs

INNER JOIN msdb.dbo.backupmediafamily bmf

ON bs.media_set_id = bmf.media_set_id

ORDER BY backup_start_date DESC;



结果示例如下：



检查特定数据库的备份情况，在SQL Server的查询窗口下执行如下命令：

SELECT

database_name,

type,

backup_start_date,

backup_finish_date,

DATEDIFF(MINUTE, backup_start_date, backup finish_date) AS duration_minutes,

backup_size/1024/1024 AS backup_size_mb

FROM msdb.dbo.backupset

WHERE database_name = 'YourDatabaseName'

ORDER BY backup_start_date DESC;



结果示例如下：



检查恢复模式和相关设置，在SQL Server的查询窗口下执行如下命令：

SELECT

name AS database_name,

recovery_model_desc,

state_desc,

log_reuse_wait_desc

FROM sys.databases

ORDER BY name;



结果示例如下。



补充恢复功能核查。建议优先在测试库或备机上开展验证，在SQL Server的查询窗口下执行如下命令：“RESTORE VERIFYONLY FROM DISK = N'备份文件完整路径';”。

如需进一步核查备份中的数据文件信息，可继续执行“RESTORE FILELISTONLY FROM DISK = N'备份文件完整路径';”。

若具备测试恢复条件，可将备份恢复到测试库后查询关键表记录数；若不具备实际恢复条件，应至少留存最近一次恢复演练记录或恢复验证截图。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令，检查备份历史记录。

SELECT

database_name,

type,

CASE type

WHEN 'D' THEN '完整备份'

WHEN 'I' THEN '差异备份'

WHEN 'L' THEN '事务日志备份'

ELSE '其他'

END AS backup_type,

backup_start_date,

backup_finish_date,

backup_size/1024/1024 AS backup_size_mb,

recovery_model,

physical_device_name

FROM msdb.dbo.backupset bs

INNER JOIN msdb.dbo.backupmediafamily bmf

ON bs.media_set_id = bmf.media_set_id

ORDER BY backup_start_date DESC;



结果示例如下：



检查特定数据库的备份情况，在命令行输入以下语句：

SELECT

database_name,

type,

backup_start_date,

backup_finish_date,

DATEDIFF(MINUTE, backup_start_date, backup finish_date) AS duration_minutes,

backup_size/1024/1024 AS backup_size_mb

FROM msdb.dbo.backupset

WHERE database_name = 'YourDatabaseName'

ORDER BY backup_start_date DESC;



结果示例如下：



检查恢复模式和相关设置，在命令行输入以下语句：

SELECT

name AS database_name,

recovery_model_desc,

state_desc,

log_reuse_wait_desc

FROM sys.databases

ORDER BY name;



结果示例如下。



补充恢复功能核查。在命令行输入以下语句：“RESTORE VERIFYONLY FROM DISK = N'备份文件完整路径';”。

如需进一步核查备份中的数据文件信息，可继续执行“RESTORE FILELISTONLY FROM DISK = N'备份文件完整路径';”。

若具备测试恢复条件，可将备份恢复到测试库后查询关键表记录数；若不具备实际恢复条件，应至少留存最近一次恢复演练记录或恢复验证截图。

### 1.11.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入命令“!ls -la /home/dmdba/dmdbms/bin | grep -i backup”查看达梦备份工具，结果示例如下：



接着，检查数据库归档模式，在disql提示符下输入以下命令：

set linesize 120 pagesize 20

col name for a30

col value for a30

col description for a50

select

name,

value,

description

from v$parameter

where name like '%ARCHIVE%';



结果示例如下：



然后，在disql提示符下输入以下命令，检查归档是否开启。

select

'归档状态'as 检查项，

case

when value = '1' then '已开启归档'

else '未开启归档'

end as 状态

from v$parameter

where name = 'ARCH INI'

union all

select

'归档目录'，

value

from v$parameter

where name = 'ARCH_DEST';



结果示例如下：



紧接着，在disql提示符下输入以下命令，查看备份相关参数。

select

name,

value,

description

from v$parameter

where name like '%BAKPOLICY%'

or name like '%BAK%'

or name like '%BACKUP%'

order by name;



结果示例如下：



补充恢复功能核查。建议优先在测试环境或独立目录中进行验证，在达梦数据库安装目录的bin目录下输入“./dmrman”进入DMRMAN工具。

在DMRMAN命令行中执行“CHECK BACKUPSET '备份集路径';”校验备份集是否完整可用。

若具备测试恢复条件，可按实际备份方式执行还原和恢复命令，例如依次执行“RESTORE DATABASE 'dm.ini路径' FROM BACKUPSET '备份集路径';”“RECOVER DATABASE 'dm.ini路径' FROM BACKUPSET '备份集路径';”以及“RECOVER DATABASE 'dm.ini路径' UPDATE DB_MAGIC;”完成恢复验证，恢复完成后启动测试实例并核查关键业务表数据。

### 1.11.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，先双击redis-server.exe，再双击redis-cli.exe连接到redis。 





检查备份配置，首先，检查RDB持久化配置，命令行输入“ CONFIG GET save ”查看自动触发BGSAVE的条件；通过 “CONFIG GET dir” 获取RDB文件路径。执行 “CONFIG GET *save* ”在后台创建RDB快照。生产环境推荐使用BGSAVE以避免阻塞。





检查AOF持久化配置，命令行输入 “CONFIG GET appendonly ”确认AOF是否开启；通过“ CONFIG GET appendfsync ”了解数据写入磁盘的策略（如everysec）。执行“CONFIG GET *apend*” 优化并重写AOF文件。



恢复功能测试，首先，停止Redis服务，输入命令“sudo systemctl stop redis”;然后，备份当前数据文件，以此执行“dump.rdb.backp”：



接着，用备份文件替换当前文件（模拟恢复），执行如下命令：



然后，重新启用Redis服务，输入命令“sudo systemctl start redis”；最后，验证数据恢复，执行如下命令：

redis-cli DBSIZE

redis-cli KEYS "*"

redis-cli GET important_key

redis-cli HGETALL important_hash



数据验证命令以下为不同数据验证查询的命令，根据不同场景的不同需求选择执行。

redis-cli DBSIZE

redis-cli KEYS "*"

redis-cli --scan --pattern "*"

redis-cli TYPE key_name

redis-cli GET string_key

redis-cli HGETALL hash_key

redis-cli LRANGE list_key 0 -1

redis-cli SMEMBERS set_key

redis-cli ZRANGE zset_key 0 -1 WITHSCORES

redis-cli INFO memory



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



检查备份配置，首先，检查RDB持久化配置，命令行输入“ CONFIG GET save”查看自动触发BGSAVE的条件；通过 “CONFIG GET dir” 获取RDB文件路径。执行 “CONFIG GET *save* ”在后台创建RDB快照。生产环境推荐使用BGSAVE以避免阻塞。





检查AOF持久化配置，命令行输入 “CONFIG GET appendonly ”确认AOF是否开启；通过“ CONFIG GET appendfsync ”了解数据写入磁盘的策略（如everysec）。执行“CONFIG GET *apend*” 优化并重写AOF文件。



恢复功能测试，首先，停止Redis服务，输入命令“sudo systemctl stop redis”;然后，备份当前数据文件，以此执行“dump.rdb.backp”：



接着，用备份文件替换当前文件（模拟恢复），执行如下命令：



然后，重新启用Redis服务，输入命令“sudo systemctl start redis”；最后，验证数据恢复，执行如下命令：

redis-cli DBSIZE

redis-cli KEYS "*"

redis-cli GET important_key

redis-cli HGETALL important_hash



数据验证命令以下为不同数据验证查询的命令，根据不同场景的不同需求选择执行。

redis-cli DBSIZE

redis-cli KEYS "*"

redis-cli --scan --pattern "*"

redis-cli TYPE key_name

redis-cli GET string_key

redis-cli HGETALL hash_key

redis-cli LRANGE list_key 0 -1

redis-cli SMEMBERS set_key

redis-cli ZRANGE zset_key 0 -1 WITHSCORES

redis-cli INFO memory



## 1.12 数据库管理系统应具有表级审计、告警和阻断功能

### 1.12.1 Mysql

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

表级审计功能核查，需临时开启查询日志，执行以下语句。

SET GLOBAL log_output = 'FILE';

SET GLOBAL log_output = 'TABLE';

SET GLOBAL log_output = 'FILE,TABLE';

SHOW VARIABLES LIKE 'general_log';

SHOW VARIABLES LIKE 'general_log_file';

SHOW VARIABLES LIKE 'log_output';

SELECT * FROM mysql.general_log WHERE event_time > NOW( ) - INTERVAL 1 HOUR LIMIT 10;





重点查看查询日志是否已实际记录对目标业务表的访问。若日志输出到表，查询mysql.general_log时应重点关注event_time、user_host、command_type、argument等字段；若日志输出到文件，则应重点查看操作时间、客户端/用户信息以及具体SQL语句。

判定标准：日志中应能查到对指定业务表执行的SELECT、INSERT、UPDATE、DELETE等语句，且至少能够对应到操作时间、操作用户（或来源主机）和具体语句内容，可据此还原表级访问行为，则表级审计符合要求；若未开启日志、日志中无目标表操作记录，或记录无法区分操作对象和操作主体，则不符合。

告警功能核查，在MySQL数据库中，执行以下命令进行慢查询日志告警核查。

SHOW VARIABLES LIKE 'slow_query_log';

SHOW VARIABLES LIKE 'long_query_time';

SHOW VARIABLES LIKE 'slow_query_log_file';

SELECT * FROM mysql.slow_log

WHERE start_time > NOW() - INTERVAL 1 DAY

ORDER BY start_time DESC

LIMIT 10;



阻断功能核查，执行“SET SESSION sql_safe_updates = 1;”开启

MySQL的安全更新模式 。执行“SELECT @@sql_safe_updates;”检查是否开启，为1则是开启状态。开启后以下类型的SQL将被阻止。

DELETE FROM table_name;

UPDATE table_name SET column_name = 'value';

DELETE FROM table_name WHERE non_key_column = 'value';

     

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

表级审计功能核查，需临时开启查询日志，执行以下语句。

SET GLOBAL log_output = 'FILE';

SET GLOBAL log_output = 'TABLE';

SET GLOBAL log_output = 'FILE,TABLE';

SHOW VARIABLES LIKE 'general_log';

SHOW VARIABLES LIKE 'general_log_file';

SHOW VARIABLES LIKE 'log_output';

SELECT * FROM mysql.general_log WHERE event_time > NOW( ) - INTERVAL 1 HOUR LIMIT 10;





重点查看查询日志是否已实际记录对目标业务表的访问。若日志输出到表，查询mysql.general_log时应重点关注event_time、user_host、command_type、argument等字段；若日志输出到文件，则应重点查看操作时间、客户端/用户信息以及具体SQL语句。

判定标准：日志中应能查到对指定业务表执行的SELECT、INSERT、UPDATE、DELETE等语句，且至少能够对应到操作时间、操作用户（或来源主机）和具体语句内容，可据此还原表级访问行为，则表级审计符合要求；若未开启日志、日志中无目标表操作记录，或记录无法区分操作对象和操作主体，则不符合。

告警功能核查，在MySQL数据库中，执行以下命令进行慢查询日志告警核查。

SHOW VARIABLES LIKE 'slow_query_log';

SHOW VARIABLES LIKE 'long_query_time';

SHOW VARIABLES LIKE 'slow_query_log_file';

SELECT * FROM mysql.slow_log

WHERE start_time > NOW() - INTERVAL 1 DAY

ORDER BY start_time DESC

LIMIT 10;





阻断功能核查，执行“SET SESSION sql_safe_updates = 1;”开启

MySQL的安全更新模式 。执行“SELECT @@sql_safe_updates;”检查是否开启，为1则是开启状态。开启后以下类型的SQL将被阻止。

DELETE FROM table_name;

UPDATE table_name SET column_name = 'value';

DELETE FROM table_name WHERE non_key_column = 'value';

     

### 1.12.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

表级审计功能核查，在查询窗口下执行以下命令：

SELECT * FROM sys.server_audits WHERE is_state_enabled = 1

SELECT * FROM sys.database_audit_specifications WHERE is_state_enabled = 1;



结果示例如下：



重点查看审计配置和审计结果两部分。配置侧应确认数据库审计规范已启用，且针对目标表配置了SELECT、INSERT、UPDATE、DELETE等对象级审计动作；结果侧通过审计文件或sys.fn_get_audit_file(...)查询时，应重点查看event_time、server_principal_name、database_name、object_name、statement、succeeded等字段。

判定标准：若已启用数据库审计规范，且审计结果中能够定位到目标表的访问记录，并清晰显示操作时间、操作账户、对象名称、执行语句及是否成功，则符合要求；若仅有服务器级审计而无目标表对象审计、审计未启用，或日志中不能反映目标表访问行为，则不符合。

告警功能核查，在SQLServer的查询窗口下执行以下命令：

SELECT message_id, severity, is_event_logged, [text]

FROM sys.messages

WHERE message_id = 1205 AND language_id = 1033;





阻断功能核查，依次在SQLServer的查询窗口下执行以下命令：

SELECT name, is_disabled, parent_class_desc

FROM sys.triggers

WHERE parent_class = 0;



USE YourDatabase;

GO

CREATE TRIGGER trg_PreventTableDrop

ON DATABASE

FOR DROP_TABLE

AS

PRINT 'DROP TABLE 操作不被允许。'

ROLLBACK;

GO







中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令进行表级审计功能核查：

SELECT * FROM sys.server_audits WHERE is_state_enabled = 1

SELECT * FROM sys.database_audit_specifications WHERE is_state_enabled = 1;



结果示例如下：



重点查看审计配置和审计结果两部分。配置侧应确认数据库审计规范已启用，且针对目标表配置了SELECT、INSERT、UPDATE、DELETE等对象级审计动作；结果侧通过审计文件或sys.fn_get_audit_file(...)查询时，应重点查看event_time、server_principal_name、database_name、object_name、statement、succeeded等字段。

判定标准：若已启用数据库审计规范，且审计结果中能够定位到目标表的访问记录，并清晰显示操作时间、操作账户、对象名称、执行语句及是否成功，则符合要求；若仅有服务器级审计而无目标表对象审计、审计未启用，或日志中不能反映目标表访问行为，则不符合。

告警功能核查，在命令行输入以下语句：

SELECT message_id, severity, is_event_logged, [text]

FROM sys.messages

WHERE message_id = 1205 AND language_id = 1033;





阻断功能核查，依次在命令行输入以下语句：

SELECT name, is_disabled, parent_class_desc

FROM sys.triggers

WHERE parent_class = 0;



USE YourDatabase;

GO

CREATE TRIGGER trg_PreventTableDrop

ON DATABASE

FOR DROP_TABLE

AS

PRINT 'DROP TABLE 操作不被允许。'

ROLLBACK;

GO









### 1.12.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令， 检查审计服务状态。

select

'审计服务状态' as检查项，

case

when value ='1' then '√已启用'

else '×未启用'

end as 状态

from v$parameter

where name = 'ENABLE_AUDIT'

union all

select

'审计文件路径'，

value

from v$parameter

where name = 'AUDIT_FILE_PATH';



结果示例如下：



接着，检查表级审计配置，首先，在disql提示符下输入以下命令，查看现有的审计策略。

col audit_option for a30

col object_schema for a15

col object_name for a25

col policy_name for a30

select

policy_name,

object_schema,

object_name,

audit_option,

condition,

enabled

from dba_audit_policies

where object_type = 'TABLE'

order by object_schema, object_name;



结果示例如下：



继续在disql提示符下输入以下命令，查看详细的表级审计设置。

col privilege for a20

col audit_condition for a50

select

table_schema,

table_name,

privilege,

audit_condition,

audit_timestamp,

audit_user

from dba_tab_audit_opts

where table_schema not in ('SYS')

order by table_schema, table_name

fetch first 10 rows only;



结果示例如下：



重点查看审计服务是否开启、对象级或语句级审计策略是否覆盖目标表，以及审计记录中是否存在对应访问行为。对审计记录进行分析时，可通过V$AUDITRECORDS重点查看操作时间、用户名、IP、对象名、成功失败标志及操作类型等信息。

判定标准：若审计开关已开启，且表级审计策略已覆盖目标表的查询或增删改操作，并能在审计记录中查询到对应用户对目标表的访问时间、对象名称、操作类型及结果，则符合要求；若仅开启审计但未配置目标表策略，或记录中无法体现具体对象和操作主体，则不符合。

最后，查看告警配置，在disql提示符下输入以下命令：

col alert_name for a30

col alert_type for a20

col condition for a50

select

alert_name,

alert_type,

condition,

action,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_alerts

where object_type = 'TABLE'

order by created_date desc;



结果示例如下：



### 1.12.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，先双击redis-server.exe，再双击redis-cli.exe连接到redis。 





核查审计功能，首先查看慢查询日志配置，接着查看记录的慢查询，依次执行如下代码：

CONFIG GET slowlog-log-slower-than

CONFIG GET slowlog-max-len

SLOWLOG GET [number]

   

重点查看慢查询日志配置和记录内容。配置侧应关注slowlog-log-slower-than是否大于等于0且未关闭日志、slowlog-max-len是否大于0；记录侧通过SLOWLOG GET应重点查看日志ID、时间戳、执行耗时、命令及参数、客户端IP/端口、客户端名称。

判定标准：若慢查询日志已启用，能够记录关键业务key相关命令的执行时间、命令参数和客户端来源等信息，可用于追溯访问行为，则审计能力符合要求；若阈值设置为关闭、日志长度为0，或日志中无法体现关键命令和来源信息，则不符合。

核查告警功能，执行如下代码：

INFO memory

INFO stats

INFO keyspace

   

核查阻断功能，首先，输入命令“CONFIG GET rename-command”以查看当前生效的命令重命名配置，参考如下配置文件示例：

rename-command FLUSHALL " "

rename-command KEYS "RENAME_KEYS"

 	

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



核查审计功能，首先查看慢查询日志配置，接着查看记录的慢查询，依次执行如下代码：

CONFIG GET slowlog-log-slower-than

CONFIG GET slowlog-max-len

SLOWLOG GET [number]

   

重点查看慢查询日志配置和记录内容。配置侧应关注slowlog-log-slower-than是否大于等于0且未关闭日志、slowlog-max-len是否大于0；记录侧通过SLOWLOG GET应重点查看日志ID、时间戳、执行耗时、命令及参数、客户端IP/端口、客户端名称。

判定标准：若慢查询日志已启用，能够记录关键业务key相关命令的执行时间、命令参数和客户端来源等信息，可用于追溯访问行为，则审计能力符合要求；若阈值设置为关闭、日志长度为0，或日志中无法体现关键命令和来源信息，则不符合。

核查告警功能，执行如下代码：

INFO memory

INFO stats

INFO keyspace

   

核查阻断功能，首先，输入命令“CONFIG GET rename-command”以查看当前生效的命令重命名配置，参考如下配置文件示例：

rename-command FLUSHALL " "

rename-command KEYS "RENAME_KEYS"

 	

## 1.13 数据库管理系统的数据应和其它应用的数据分类独立存储

以下SQL语句涵盖了各数据库管理类系统实例、存储、权限、日志等各个方面的隔离性检查。

### 1.13.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

核查数据库实例独立性，首先，输入命令“SHOW VARIABLES LIKE  ‘port’;”以检查MySQL服务端口；接着，检查进程ID和运行用户，执行以下命令：

SHOW VARIABLES LIKE 'pid_file';

SELECT * FROM performance_schema.threads WHERE TYPE = 'BACKGROUND';



然后，输入命令“SHOW VARIABLES LIKE ‘datadir’;”以检查数据目录；最后，输入命令“SHOW VARIABLES LIKE ‘config_file’检查配置文件路径;”。

核查数据库和用户权限隔离，首先，输入命令“SHOW DATABASES”查看所有数据库；接着，检查业务数据库是否存在且孤立，执行如下命令：

SELECT SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME,

DEFAULT_COLLATION_NAME

FROM information_schema.SCHEMATA

WHERE SCHEMA_NAME NOT IN ('mysql', 'information_schema',

'performance_schema', 'sys');



然后，查看所有用户及权限，紧接着，检查业务数据库用户权限，最后，检查用户权限范围（确保没有跨数据库权限），依次执行如下命令：

SELECT user, host, authentication_string FROM mysql.user;

SHOW GRANTS FOR 'your_business_user'@'%';

SELECT * FROM mysql.db WHERE Db = 'your_business_database';



核查表空间和存储配置，首先，输入命令“SHOW VARIABLES LIKE ‘innodb_file_per_table’;”检查是否启用独立表空间；接着，输入命令“SHOW VARIABLES LIKE ‘innodb_data_file_path’;”检查表空间文件配置；然后，查看业务数据库的表文件存储，执行如下命令：

SELECT

table_schema,

table_name,

engine,

ROUND((data_length + index_length) / 1024 / 1024, 2) as size_mb,

create_time

FROM information_schema.tables

WHERE table_schema = 'your_business_database'

ORDER BY (data_length + index_length)  DESC;



最后，检查临时表空间配置，执行如下命令：

SHOW VARIABLES LIKE 'tmpdir';

SHOW VARIABLES LIKE 'innodb_temp_data_file_path';



核查日志文件隔离，首先，检查二进制日志位置，输入如下命令：

SHOWVARIABLES LIKE 'Log_bin';

SHOW VARIABLES LIKE 'log_bin_basename';



接着，检查错误日志位置，再检查慢查询日志位置，然后，检查通用查询日志位置，最后，检查中继日志位置，依次执行如下命令：

SHOW VARIABLES LIKE 'log_error';

SHOW VARIABLES LIKE 'slow_query_log_file';

SHOW VARIABLES LIKE 'general_log_file';

SHOW VARIABLES LIKE 'relay_log';



核查备份和恢复隔离，首先，创建备份策略检查表，执行以下命令：

CREATE TABLE IF NOT EXISTS backup_audit (

id INT AUTO_INCREMENT PRIMARY KEY,

audit_date DATE,

database_name VARCHAR(100),

backup_type VARCHAR(50),

storage_location VARCHAR(255),

isolation_status ENUM('独立','共享','未知'),

notes TEXT,

created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



然后，记录检查结果，执行如下命令：

INSERT INTO backup_audit (audit_date, database_name,backup_type, storage_location, isolation_status, notes)

SELECT

CURDATE( ),

'your_business_database'

'数据文件',

VARIABLE_VALUE,

CASE

WHEN VARIABLE_VALUE LIKE '%/business_data/%' THEN  '独立'

WHEN VARIABLE_VALUE LIKE '%/mysql/%' THEN '共享'

ELSE'未知'

END,

'数据目录位置核查'

FROM information_schema.GLOBAL_VARIABLES

WHERE VARIABLE_NAME = 'datadir';



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

核查数据库实例独立性，首先，输入命令“SHOW VARIABLES LIKE  ‘port’;”以检查MySQL服务端口；接着，检查进程ID和运行用户，执行以下命令：

SHOW VARIABLES LIKE 'pid_file';

SELECT * FROM performance_schema.threads WHERE TYPE = 'BACKGROUND';



然后，输入命令“SHOW VARIABLES LIKE ‘datadir’;”以检查数据目录；最后，输入命令“SHOW VARIABLES LIKE ‘config_file’检查配置文件路径;”。

核查数据库和用户权限隔离，首先，输入命令“SHOW DATABASES”查看所有数据库；接着，检查业务数据库是否存在且孤立，执行如下命令：

SELECT SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME,

DEFAULT_COLLATION_NAME

FROM information_schema.SCHEMATA

WHERE SCHEMA_NAME NOT IN ('mysql', 'information_schema',

'performance_schema', 'sys');



然后，查看所有用户及权限，紧接着，检查业务数据库用户权限，最后，检查用户权限范围（确保没有跨数据库权限），依次执行如下命令：

SELECT user, host, authentication_string FROM mysql.user;

SHOW GRANTS FOR 'your_business_user'@'%';

SELECT * FROM mysql.db WHERE Db = 'your_business_database';



核查表空间和存储配置，首先，输入命令“SHOW VARIABLES LIKE ‘innodb_file_per_table’;”检查是否启用独立表空间；接着，输入命令“SHOW VARIABLES LIKE ‘innodb_data_file_path’;”检查表空间文件配置；然后，查看业务数据库的表文件存储，执行如下命令：

SELECT

table_schema,

table_name,

engine,

ROUND((data_length + index_length) / 1024 / 1024, 2) as size_mb,

create_time

FROM information_schema.tables

WHERE table_schema = 'your_business_database'

ORDER BY (data_length + index_length)  DESC;



最后，检查临时表空间配置，执行如下命令：

SHOW VARIABLES LIKE 'tmpdir';

SHOW VARIABLES LIKE 'innodb_temp_data_file_path';



核查日志文件隔离，首先，检查二进制日志位置，输入如下命令：

SHOWVARIABLES LIKE 'Log_bin';

SHOW VARIABLES LIKE 'log_bin_basename';



接着，检查错误日志位置，再检查慢查询日志位置，然后，检查通用查询日志位置，最后，检查中继日志位置，依次执行如下命令：

SHOW VARIABLES LIKE 'log_error';

SHOW VARIABLES LIKE 'slow_query_log_file';

SHOW VARIABLES LIKE 'general_log_file';

SHOW VARIABLES LIKE 'relay_log';



核查备份和恢复隔离，首先，创建备份策略检查表，执行以下命令：

CREATE TABLE IF NOT EXISTS backup_audit (

id INT AUTO_INCREMENT PRIMARY KEY,

audit_date DATE,

database_name VARCHAR(100),

backup_type VARCHAR(50),

storage_location VARCHAR(255),

isolation_status ENUM('独立','共享','未知'),

notes TEXT,

created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



然后，记录检查结果，执行如下命令：

INSERT INTO backup_audit (audit_date, database_name,backup_type, storage_location, isolation_status, notes)

SELECT

CURDATE( ),

'your_business_database'

'数据文件',

VARIABLE_VALUE,

CASE

WHEN VARIABLE_VALUE LIKE '%/business_data/%' THEN  '独立'

WHEN VARIABLE_VALUE LIKE '%/mysql/%' THEN '共享'

ELSE'未知'

END,

'数据目录位置核查'

FROM information_schema.GLOBAL_VARIABLES

WHERE VARIABLE_NAME = 'datadir';



### 1.13.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。核查数据文件与日志文件存储，在查询窗口下执行以下命令：

USE master;

GO

SELECT

name AS '逻辑文件名',

physical_name AS '物理文件路径',

type_desc AS '文件类型'

FROM sys.master_files

WHERE database_id=DB_ID('你的数据库名');

GO





中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令，核查数据文件与日志文件存储：

USE master;

GO

SELECT

name AS '逻辑文件名',

physical_name AS '物理文件路径',

type_desc AS '文件类型'

FROM sys.master_files

WHERE database_id=DB_ID('你的数据库名');

GO



结果示例如下：



### 1.13.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，检查数据库文件存储结构，在disql提示符下输入以下命令，查看数据库主配置文件。

set linesize 120 pagesize 20

col para_name for a30

col para_value for a50

select

para_name,

para_value,

description

from v$dm_ini

where para_name like '%PATH%'

or para_name like '%DIR%'

or para_name like '%FILE%'

order by para_name;



结果示例如下：



接着在disql提示符下输入以下命令，检查表空间存储分离情况。

col tablespace_name for a25

col file_name for a80

col status for a10

col autoextensible for a5

select

t.tablespace_name,

d.file_name,

d.status,

d.autoextensible,

round(d.bytes/1024/1024, 2) as size_mb,

round(d.maxbytes/1024/1024, 2) as max_mb

from dba_tablespaces t, dba_data_files d

where t.tablespace_name = d.tablespace_name

order by t.tablespace_name, d.file_id;



结果示例如下：



最后，检查文件系统层面的分离，在disql提示符下输入命令“exit”，退出disql查看文件系统，然后依次输入以下命令，查看数据文件的实际存储位置。

ls - la /home/dmdba/dmdbms/data/

find /home/dmdba -name "*.DBF"  -type f 2 >/dev/null | head -10



结果示例如下：



### 1.13.4 Redis

判定标准：不同密级、不同应用的数据已分别存储于不同的逻辑库（db）或不同的Redis实例中，端口、数据目录、持久化文件相互隔离，未出现不同密级数据混存于同一库或同一实例的情况。

输入“CONFIG GET port”“CONFIG GET dir”“CONFIG GET dbfilename”核查各实例的端口、数据目录、持久化文件是否相互独立。

输入“ps -ef | grep redis-server | grep -v grep”查看是否以多个实例（不同端口、不同配置文件）运行，例如“redis-server /etc/redis/6379.conf --port 6379”和“redis-server /etc/redis/6380.conf --port 6380”。

在桌面鼠标右键-打开终端，命令行输入“redis-cli -a 密码”连接Redis服务，输入“CONFIG GET databases”查看逻辑库数量，输入“INFO keyspace”查看各库键分布。

中标麒麟、银河麒麟操作系统下的核查方法

在命令行输入“tasklist | findstr redis-server”或“wmic process where "name='redis-server.exe'" get ProcessId,CommandLine”，查看是否存在多个使用不同端口、不同配置文件的redis-server实例。

核查多实例部署，输入“CONFIG GET port”“CONFIG GET dir”“CONFIG GET dbfilename”查看当前实例的端口、数据目录和持久化文件名，确认与其它实例相互分离。

核查逻辑库（db）隔离，输入“CONFIG GET databases”查看逻辑库数量，输入“SELECT 0”至“SELECT 15”逐个切换逻辑库，再输入“INFO keyspace”查看各库键分布，输入“DBSIZE”查看当前库键数量。不同密级、不同应用的数据应分别存放在不同db中。

打开Redis安装目录，在redis-cli.exe程序的当前目录下鼠标右键打开终端，命令行输入“redis-cli.exe -a 密码”连接Redis服务。

Windows操作系统下的核查方法

## 1.14 中间件应采取限制运行权限和使用安全管理通道等安全加固措施

判定标准：中间件以非特权账户运行，管理通道启用认证与加密，已更改默认端口与默认凭据，无样例应用与多余组件残留；仍以管理员权限运行或保留默认配置即不合规。

### 1.14.1 Nginx

打开Nginx安装目录，再打开conf文件夹，找到nginx.conf文件，添加以下配置。

http{

server_tokens off;

add_header X-Frame-Options "SAMEORIGIN" always;

add_header X-XSS-Protection "1; mode=block" always;

add_header X-Content-Type-Options "nosniff" always;

}

user nobody;



编写脚本核查，打开Nginx安装目录，右键新建文本文件，在文件中编辑以下文本。

@echo off

chcp 65001 >nul

echo=== Nginx安全快速核查===

echo.



echo 1.检查Nginx进程...

tasklist /fi "imagename eq nginx.exe" /fo table



echo.

echo2.检查配置文件中的用户设置...

findstr "user" conf \nginx.conf >nul && (echo 找到用户配置: && findstr "user" conf\nginx.conf)||echo 未配置运行用户



echo.

echo 3.检查ssL配置...

findstr "listen. *443" conf \nginx.conf >nul && echo 已配记置HTTPS || echo 未配记置HTTPS

findstr "ssl certificate" conf\nginx.conf >nul && echo 找到SsL证书配置 || echo 未找到SSL证书配置



echo.

echo 4.检查版本隐藏...

findstr "server tokens off" conf\nginx.conf >nul && echo 已隐藏版本号||echo 未隐藏版本号



echo.

echo 5.检查监听的端口..

netstat -ano | findstr ":80 :443"



echo.

echo === 核查完成===

pause



重命名为check.bat，以此双击运行nginx.exe文件和check.bat文件。脚本检查情况如下。



期望结果为：应该有 user nobody; 或指定的低权限用户，应该有SSL证书配置和443端口监听。

### 1.14.2 Tomcat

打开 conf/server.xml，找到 <Server> 标签，修改其端口和关闭指令：“<Server port="8527" shutdown="DangerousShutdownCommand">”

port：将其从默认的8005改为一个不易猜测的大于1024的端口。shutdown：将默认的SHUTDOWN指令改为其他复杂的、不易猜测的字符串。

同样在 conf/server.xml 中，配置一个HTTPS连接器。

<Connector port="8443"

protocol="org.apache.coyote.http11.Http11NioProtocol"

maxThreads="150"

SSLEnabled="true"

scheme="https"

secure="true"

keystoreFile="conf/localhost-rsa.jks"

keystorePass="your_keystore_password"

clientAuth="false"

sslEnabledProtocols="TLSv1.2,TLSv1.3"

cipherS="TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256, ..." />



编写脚本核查，打开Nginx安装目录，右键新建文本文件，在文件中编辑以下文本。

@echo off

chcp 65001 >nul

cls



echo ====================================

echo    Tomcat安全快速核查

echo ====================================

echo.



echo [1] 检查Tomcat进程运行用户...

echo ------------------------------------

tasklist /fi "imagename eq java.exe" /fo table | findstr /i "java"

if errorlevel 1 (

    echo 未发现Java进程

) else (

    echo 发现Java进程

)



echo.

echo [2] 检查Tomcat服务...

echo ------------------------------------

sc query Tomcat9 2>nul >nul && (

    echo 发现Tomcat服务

) || (

    echo 未找到Tomcat服务

)



echo.

echo [3] 检查网络监听端口...

echo ------------------------------------

netstat -ano | findstr ":8080 :8443" | findstr "LISTENING"



echo.

echo [4] 检查SSL配置...

echo ------------------------------------

findstr /i "keystoreFile" "conf\server.xml" >nul

if errorlevel 1 (

    echo 未配置SSL证书

) else (

    echo 已配置SSL证书

)



echo.

echo [5] 检查管理用户配置...

echo ------------------------------------

if exist "conf\tomcat-users.xml" (

    findstr /i "manager-script" "conf\tomcat-users.xml" >nul

    if errorlevel 1 (

        echo 未发现manager-script角色

    ) else (

        echo 发现manager-script角色

    )

) else (

    echo 未找到tomcat-users.xml

)



echo.

echo ====================================

echo     核查完成

echo ====================================

echo.

Pause

重命名为tomcat_security_check.bat文件，再双击运行。结果如图。



## 1.15 应具备数据库管理系统超级管理员远程登录限制远程登陆限制能力

判定标准：数据库超级管理员（sa、root、SYSDBA 等）已限制或禁止远程登录，仅允许本机或指定管理地址登录；超级管理员可从任意远程地址直接登录即不合规。

### 1.15.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



在MySQL服务器上，使用mysql -u root -p连接到MySQL服务，执行“SELECT user, host FROM mysql.user WHERE user = 'root';”查看所有用户的用户名和允许连接的主机。



如果查询结果中，root用户对应的host字段值为 localhost 或 127.0.0.1，则表示root用户只能从本地连接，符合安全要求。如果host字段值为 %，则表示root用户可以被任何IP地址远程连接，存在安全风险。使用“DROP USER 'root'@'%';”删除该用户。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在MySQL服务器上，使用mysql -u root -p连接到MySQL服务，执行“SELECT user, host FROM mysql.user WHERE user = 'root';”查看所有用户的用户名和允许连接的主机。



如果查询结果中，root用户对应的host字段值为 localhost 或 127.0.0.1，则表示root用户只能从本地连接，符合安全要求。如果host字段值为 %，则表示root用户可以被任何IP地址远程连接，存在安全风险。使用“DROP USER 'root'@'%';”删除该用户。

### 1.15.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

首先，我们需要确认SQL Server是否允许SQL Server身份验证（即混合模式），因为sa账户依赖于这种模式。在查询窗口输入以下命令：

SELECT

SERVERPROPERTY('IsIntegratedSecurityonly') AS [IsWindowsAuthenticationonly];



结果示例如下



检查sa账户状态：

SELECT name, is_disabled

FROM sys.sql_logins

WHERE name = 'sa';





核查具备超级管理员权限的登录名：

SELECT pri.name

FROM sys.server_principals pri

INNER JOIN sys.server_role_members rm ON pri.principal_id = rm.member_principal_id

INNER JOIN sys.server_principals rol ON rm.role_principal_id = rol.principal_id

WHERE rol.name = 'sysadmin';





中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例。

首先，我们需要确认SQL Server是否允许SQL Server身份验证（即混合模式），因为sa账户依赖于这种模式。在命令行输入以下语句：

SELECT

SERVERPROPERTY('IsIntegratedSecurityonly') AS [IsWindowsAuthenticationonly];



结果示例如下：



检查sa账户状态：

SELECT name, is_disabled

FROM sys.sql_logins

WHERE name = 'sa';





核查具备超级管理员权限的登录名：

SELECT pri.name

FROM sys.server_principals pri

INNER JOIN sys.server_role_members rm ON pri.principal_id = rm.member_principal_id

INNER JOIN sys.server_principals rol ON rm.role_principal_id = rol.principal_id

WHERE rol.name = 'sysadmin';



结果示例如下：



### 1.15.3 达梦

Windows操作系统下的核查方法

金仓数据库通过src_restrict插件实现IP来源限制，执行如下命令以查看所有用户的IP白名单设置：

SELECT A.USERNAME, B.ALLOW_ADDR

FROM ALL_USERS A, SYSUSERS B

WHERE A.USER_ID = B.ID AND A.USERNAME = 'SYSDBA';



核查超级用户权限限制：

SHOW shared_preload_libraries;



达梦数据库使用ALLOW_IP参数限制用户登录IP，执行如下命令查看所有用户的白名单设置：

SELECT A.USERNAME, B.ALLOW_ADDR

FROM ALL_USERS A, SYSUSERS B

WHERE A.USER_ID = B.ID AND A.USERNAME= 'SYSDBA';



核查超级用户权限限制，执行如下命令。

CREATE USER DBA_ADMIN IDENTIFIED BY "强密码@123";

GRANT DBA, RESOURCE, PUBLIC TO DBA_ADMIN;



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd /home/dmdba/dmdbms/data/DAMENG”按回车键，然后输入“cat dm.ini | grep -i "listen" ”查看数据库配置文件，结果示例如下：



命令行接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，检查SYSDBA用户的远程登录限制，首先在disql提示符下输入以下命令，查看用户登录相关参数。

set linesize 120 pagesize 20

col name for a30

col value for a30

select

name,

value,

description

from v$parameter

where name like '%LOGIN%'

or name 1ike '%REMOTE%'

or name like '%SECURITY%'

order by name;



结果示例如下：



接着，在disql提示符下输入以下命令，检查SYSDBA用户的登录属性。

col username for a15

col account_status for a15

col profile for a15

select

username,

account_status,

profile,

to_char(created, 'YYYY-MM-DD') as created_date,

to_char(expiry_date, 'YYYY-MM-DD') as expiry_date

from dba_users

where username ='SYSDBA';



结果示例如下：



然后，在disql提示符下输入以下命令，检查IP白名单配置。

col ip_address for a20

col access_type for a10

col description for a30

select

ip_address,

access_type,

description,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_ip_access

order by ip_address;



结果示例如下：



最后，在disql提示符下输入以下命令，检查SYSDBA的IP限制。

select

username,

ip_address,

access_type,

enabled

from dba_user_ip_access

where username = 'SYSDBA'

order by ip_address;



结果示例如下：



核查达梦是否具备超级管理员远程登录限制能力，主要涉及网络访问控制、认证机制和精细的权限管理。

### 1.15.4 Redis

Windows操作系统下的核查方法

核查网络访问控制，首先打开Redis安装所在的文件夹找到Redis配置文件redis.conf，找到bind指令。如果绑定的是127.0.0.1或特定管理IP，则仅允许本地或指定IP连接。如果配置为bind 0.0.0.0或被注释掉（如# bind 127.0.0.1 -::1），则允许所有远程连接，风险较高。



然后在redis.conf中查找protected-mode指令。如果设置为yes（默认值）且未设置bind指令且未设置密码时，Redis将只接受来自IPv4和IPv6环回地址（127.0.0.1和::1）的连接，即仅限本地。



如果设置为no，则会禁用此保护模式（如为了允许远程连接）。除非你明确了解风险且有其他安全措施（如严格的防火墙规则），否则不建议禁用。最后检查服务器防火墙（如iptables、firewalld）或云服务商安全组规则。

核查认证机制，在redis.conf中查找requirepass指令，如果该项未被注释且设置了复杂密码（如requirepass 你的密码），则客户端连接后需要通过AUTH <password>命令认证才能操作。如果该项被注释（如# requirepass foobared）或密码简单，则意味着无需认证或易被破解，风险极高。



核查精细权限控制 ，使用ACL LIST命令查看所有用户及其权限描述。重点关注是否有用户（如默认的default用户）拥有+@all权限（所有命令权限），这相当于超级管理员。应遵循最小权限原则，根据使用场景创建专用账户。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



核查网络访问控制，首先打开Redis安装所在的文件夹找到Redis配置文件redis.conf，找到bind指令。如果绑定的是127.0.0.1或特定管理IP，则仅允许本地或指定IP连接。如果配置为bind 0.0.0.0或被注释掉（如# bind 127.0.0.1 -::1），则允许所有远程连接，风险较高。



然后在redis.conf中查找protected-mode指令。如果设置为yes（默认值）且未设置bind指令且未设置密码时，Redis将只接受来自IPv4和IPv6环回地址（127.0.0.1和::1）的连接，即仅限本地。



如果设置为no，则会禁用此保护模式（如为了允许远程连接）。除非你明确了解风险且有其他安全措施（如严格的防火墙规则），否则不建议禁用。最后检查服务器防火墙（如iptables、firewalld）或云服务商安全组规则。

核查认证机制，在redis.conf中查找requirepass指令，如果该项未被注释且设置了复杂密码（如requirepass 你的密码），则客户端连接后需要通过AUTH <password>命令认证才能操作。如果该项被注释（如# requirepass foobared）或密码简单，则意味着无需认证或易被破解，风险极高。



核查精细权限控制 ，使用ACL LIST命令查看所有用户及其权限描述。重点关注是否有用户（如默认的default用户）拥有+@all权限（所有命令权限），这相当于超级管理员。应遵循最小权限原则，根据使用场景创建专用账户。

## 1.16 应具备数据库管理系统输入（参数）检查能力

判定标准：数据库端口或驱动已启用 SQL 注入拦截、参数化校验等输入检查能力，对畸形与攻击性语句能够告警或阻断；任意构造语句均可直达执行即不合规。

### 1.16.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



核查存储过程或函数中的输入检查，使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

创建名为InsertUser的存储过程；接收用户名和邮箱两个输入参数；对输入参数进行验证：检查参数是否为空；检查用户名长度是否至少3个字符；检查邮箱格式是否有效；如果所有检查通过，则将数据插入到users表中。执行以下命令：

DELIMITER //

CREATE PROCEDURE InsertUser(

IN p_username VARCHAR(50),

IN p_email VARCHAR(100)

)

BEGIN

IF p_username IS NULL OR p_email IS NULL THEN

SIGNAL SQLSTATE '45000'

SET MESSAGE_TEXT = 'Username and email cannot be null';

ELSEIF LENGTH(p_username) < 3 THEN

SIGNAL SQLSTATE  '45000'

SET MESSAGE_TEXT = 'Username must be at least 3 characters long';

ELSEIF NOT  p_email LIKE '%_@__%._%' THEN

SIGNAL SQLSTATE '45000'

SET MESSAGE_TEXT = 'Invalid email format';

ELSE

INSERT INTO users (username, email) VALUES (p_username, p_email);

END IF;

END //

DELIMITER;

  

在上面的例子中，如果p_username或p_email为NULL，或用户名长度不足3位，或邮箱格式不符，则会抛出错误。

核查参数化查询的使用，在应用程序代码中，应使用参数化查询（预编译语句）而非字符串拼接来构建SQL语句。以PHP(PDO)为例：

$sql = "SELECT * FROM users WHERE username = :username AND email = :email";

$stmt = $pdo->prepare($sql);

$stmt->execute([

'username' => $input_username,

email' => $input_email

]);



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



核查存储过程或函数中的输入检查，在桌面鼠标右键-打开终端，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

创建名为InsertUser的存储过程；接收用户名和邮箱两个输入参数；对输入参数进行验证：检查参数是否为空；检查用户名长度是否至少3个字符；检查邮箱格式是否有效；如果所有检查通过，则将数据插入到users表中。执行以下命令：

DELIMITER //

CREATE PROCEDURE InsertUser(

IN p_username VARCHAR(50),

IN p_email VARCHAR(100)

)

BEGIN

IF p_username IS NULL OR p_email IS NULL THEN

SIGNAL SQLSTATE '45000'

SET MESSAGE_TEXT = 'Username and email cannot be null';

ELSEIF LENGTH(p_username) < 3 THEN

SIGNAL SQLSTATE  '45000'

SET MESSAGE_TEXT = 'Username must be at least 3 characters long';

ELSEIF NOT  p_email LIKE '%_@__%._%' THEN

SIGNAL SQLSTATE '45000'

SET MESSAGE_TEXT = 'Invalid email format';

ELSE

INSERT INTO users (username, email) VALUES (p_username, p_email);

END IF;

END //

DELIMITER;

  

在上面的例子中，如果p_username或p_email为NULL，或用户名长度不足3位，或邮箱格式不符，则会抛出错误。

核查参数化查询的使用，在应用程序代码中，应使用参数化查询（预编译语句）而非字符串拼接来构建SQL语句。以PHP(PDO)为例：

$sql = "SELECT * FROM users WHERE username = :username AND email = :email";

$stmt = $pdo->prepare($sql);

$stmt->execute([

'username' => $input_username,

email' => $input_email

]);



### 1.16.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。首先，检查缓存中的查询计划，识别潜在的SQL注入风险，在查询窗口下执行以下命令：

SELECT

dest.text AS QueryText,

deqp.query_plan,

decp.objtype,

decp.usecounts,

decp.size_in_bytes

FROM sys.dm_exec_cached_plans AS decp

CROSS APPLY sys.dm_exec_sql_text(decp.plan_handle) AS dest

CROSS APPLY sys.dm_exec_query_plan(decp.plan_handle) AS deqp

WHERE dest.text LIKE '%SELECT%@%'

OR dest.text LIKE '%INSERT%@%'

OR dest.text LIKE '%UPDATE%@%'

OR dest.text LIKE '%DELETE%@%'

ORDER BY decp.usecounts DESC;



结果示例如下：



接上图，



核查存储过程输入验证，在查询窗口下执行以下命令： 

FROM sys.procedures

WHERE OBJECT_DEFINITION(object_id) LIKE '%IS NULL%'

OR OBJECT_DEFINITION(object_id) LIKE '%LENC%'

    OR OBJECT_DEFINITION(object_id) LIKE '%RAISERROR%'

OR OBJECT_DEFINITION(object_id) LIKE '%THROW%'

ORDER BY ProcedureName; 



以下为操作具体示例:

CREATE PROCEDURE dbo. ValidateUserInput

@UserName NVARCHAR(50),

@Email NVARCHAR(100),

@Age INT

AS

BEGIN

SET NOCOUNT ON;

IF @UserName IS NULL OR LTRIM(RTRIMC@UserName)) =' '

BEGIN

THROW 50001,'用户名不能为空',1;

RETURN;

END

IF LEN(@UserName) < 3 OR LEN(@UserName) > 50

BEGIN

THROW 50002,'用户名长度必须在3-50个字符之间'，1;

RETURN;

END

IF @Email NOT LIKE '%_@__ %.__%'

BEGIN

THROW 50003，'邮箱格式不正确'，1;

RETURN;

END

IF @Age < 0 OR @Age > 150

BEGIN

THROW 50004，'年龄必须在0-150之间'，1;

RETURN;

END

INSERT INTO dbo.Users (UserName, Email, Age, CreatedDate)

VALUES (@UserName, @Email, @Age, GETDATEC));

PRINT '用户创建成功';

END;



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，首先，检查缓存中的查询计划，识别潜在的SQL注入风险，在命令行输入以下语句：

SELECT

dest.text AS QueryText,

deqp.query_plan,

decp.objtype,

decp.usecounts,

decp.size_in_bytes

FROM sys.dm_exec_cached_plans AS decp

CROSS APPLY sys.dm_exec_sql_text(decp.plan_handle) AS dest

CROSS APPLY sys.dm_exec_query_plan(decp.plan_handle) AS deqp

WHERE dest.text LIKE '%SELECT%@%'

OR dest.text LIKE '%INSERT%@%'

OR dest.text LIKE '%UPDATE%@%'

OR dest.text LIKE '%DELETE%@%'

ORDER BY decp.usecounts DESC;



结果示例如下：



接上图，



核查存储过程输入验证，在命令行输入以下语句：  

FROM sys.procedures

WHERE OBJECT_DEFINITION(object_id) LIKE '%IS NULL%'

OR OBJECT_DEFINITION(object_id) LIKE '%LENC%'

    OR OBJECT_DEFINITION(object_id) LIKE '%RAISERROR%'

OR OBJECT_DEFINITION(object_id) LIKE '%THROW%'

ORDER BY ProcedureName; 



以下为操作具体示例:

CREATE PROCEDURE dbo. ValidateUserInput

@UserName NVARCHAR(50),

@Email NVARCHAR(100),

@Age INT

AS

BEGIN

SET NOCOUNT ON;

IF @UserName IS NULL OR LTRIM(RTRIMC@UserName)) =' '

BEGIN

THROW 50001,'用户名不能为空',1;

RETURN;

END

IF LEN(@UserName) < 3 OR LEN(@UserName) > 50

BEGIN

THROW 50002,'用户名长度必须在3-50个字符之间'，1;

RETURN;

END

IF @Email NOT LIKE '%_@__ %.__%'

BEGIN

THROW 50003，'邮箱格式不正确'，1;

RETURN;

END

IF @Age < 0 OR @Age > 150

BEGIN

THROW 50004，'年龄必须在0-150之间'，1;

RETURN;

END

INSERT INTO dbo.Users (UserName, Email, Age, CreatedDate)

VALUES (@UserName, @Email, @Age, GETDATEC));

PRINT '用户创建成功';

END;



### 1.16.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看所有可配置参数。

set linesize 120 pagesize 20

col name for a40

col value for a20

col sys_value for a20

col description for a50

select

name,

value,

sys_value,

description

from v$parameter

where name like '%VALID%'

or name like '%CHECK%'

or name like '%VERIFY%'

order by name;



结果示例如下：



接着，在disql提示符下输入以下命令，查看是否注入防护参数。

select

name,

value,

sys_value,

case

when value = sys_value then '默认值'

else'已修改'

end as 修改状态

from v$parameter

where name like '%INJECTION%'

or name like '%SQL%SECURITY%'

or name like '%BIND%'

order by name;



结果示例如下：



### 1.16.4 Redis

Windows操作系统下的核查方法

检查bind配置：打开Redis配置文件（通常是redis.xxx.conf），找到bind指令。安全的配置是将其设置为127.0.0.1（仅本地访问）或特定的内网IP，避免设置为0.0.0.0。



检查protected-mode配置：在配置文件中确认protected-mode选项设置为yes。这可以在未设置bind和密码时，阻止外部访问。



修改默认端口：在配置文件中找到port指令，将默认的6379修改为其他端口。



检查密码认证：在配置文件中找到requirepass指令，确保其未被注释，并且后面设置了高强度密码（长度8位以上，包含大小写字母、数字和特殊字符）。如被注释，redis登录无密码。



检查运行账户：使用win+R打开控制行，输入cmd回车打开命令行，输入“services.msc”，在服务列表中找到Redis服务（名称通常是 "Redis" 或 "Redis Server"）。双击打开其属性，在"登录"选项卡下，即可看到"登录身份"。







中标麒麟、银河麒麟操作系统下的核查方法

检查bind配置：打开Redis配置文件（通常是redis.conf），找到bind指令。安全的配置是将其设置为127.0.0.1（仅本地访问）或特定的内网IP，避免设置为0.0.0.0。



检查protected-mode配置：在配置文件中确认protected-mode选项设置为yes。这可以在未设置bind和密码时，阻止外部访问。



修改默认端口：在配置文件中找到port指令，将默认的6379修改为其他端口。



检查密码认证：在配置文件中找到requirepass指令，确保其未被注释，并且后面设置了高强度密码（长度8位以上，包含大小写字母、数字和特殊字符）。如被注释，redis登录无密码。



检查运行账户：在桌面鼠标右键-打开终端，在命令行界面输入命令“ps -ef | grep redis-server | grep -v grep”，可以看到当前运行的 Redis 服务是由哪个用户启动的。



## 1.17 应限制操作系统开放的远程管理服务或端口

判定标准：仅保留必要的远程管理服务和端口，Telnet、远程桌面等不必要的远程管理服务已停止并禁用，多余监听端口已关闭；确需保留的服务有明确用途说明。

中标麒麟、银河麒麟操作系统下的核查方法

输入“firewall-cmd --list-all”或“ufw status verbose”查看防火墙开放端口，确认仅开放必要的远程管理端口。

输入“systemctl list-units --type=service --state=running”查看正在运行的服务，识别telnet、sshd、vsftpd等远程管理服务；对不必要服务输入“systemctl stop 服务名”停止服务，再输入“systemctl disable 服务名”禁用其开机启动。

在桌面鼠标右键-打开终端，命令行输入“ss -tlnp”或“netstat -tlnp”查看当前监听的TCP端口及对应进程。

Windows操作系统（Win7/XP）下的核查方法

输入“services.msc”打开服务管理器，查看Telnet、Remote Desktop Services（TermService）等远程管理服务的启动状态；对不必要服务，输入“sc stop 服务名”停止服务，再输入“sc config 服务名 start= disabled”设置为禁用。确需使用的远程管理服务应说明用途。

输入“tasklist /svc”查看进程与服务对应关系，或输入“tasklist /fi "PID eq 进程号"”确认监听端口的进程与服务名称，识别Telnet（23）、远程桌面（3389）、FTP（21）等远程管理服务。

使用win+R组合键打开运行框，输入cmd，在命令行输入“netstat -ano”查看当前所有处于监听状态（LISTENING）的端口及对应进程PID。

## 1.18 应限制用户对服务器资源的最大或最小使用限度

### 1.18.1 Windows7、WindowsXP

通过“组策略”（gpedit.msc）：开始 -> 搜索框搜索计算机管理 -> 本地用户和组 ->组，来配置用户权限。



命令行核查：磁盘配额在命令行输入“fsutil quota query C:”查看卷配额状态；用户权限分配可打开“本地安全策略”（secpol.msc），在“本地策略”→“用户权限分配”中核对各用户或组对系统资源的使用权限设置。

### 1.18.2 中标麒麟、银河麒麟

判定标准：/etc/security/limits.conf 或 limits.d 已针对用户或用户组配置nproc、nofile、内存等最大使用限度，硬限制不低于软限制；关键服务已通过cgroup等机制限制CPU、内存使用，避免单个用户或进程耗尽服务器资源。

对关键服务可用cgroup限制CPU和内存，输入“cat /etc/cgconfig.conf”或“systemd-cgtop”查看已配置的cgroup资源限制。

核查资源限制配置文件，输入“cat /etc/security/limits.conf”和“ls /etc/security/limits.d/”查看是否对用户或用户组设置了软限制（soft）与硬限制（hard），例如“* soft nproc 1024”“* hard nproc 4096”“* soft nofile 1024”“* hard nofile 65536”。

在桌面鼠标右键-打开终端，命令行输入“ulimit -a”查看当前用户的各类资源限制（core file size、data seg size、open files、max user processes等）。

## 1.19 应更换数据库管理系统的默认服务端口、管理员用户名和口令

判定标准：数据库服务端口、管理员用户名和口令均已更改，非出厂默认值；端口、用户名、口令任一项保持默认即不合规。

### 1.19.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

更换数据库管理系统的默认服务端口，在MySQL\MySQL Server X.X\my.ini目录下，在[mysqld]配置段下修改或添加port参数。命令行输入“SHOW VARIABLES LIKE 'port';”，查看占用端口。



核查并修改默认管理员用户名，命令行输入“RENAME USER 'root'@'localhost' TO 'new_admin'@'localhost';FLUSH PRIVILEGES;”重命名为其他用户名。

核查并修改管理员口令，命令行输入“ALTER USER '你的管理员用户名'@'主机名' IDENTIFIED BY '你的新复杂密码';FLUSH PRIVILEGES;”。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

更换数据库管理系统的默认服务端口，在MySQL\MySQL Server X.X\my.ini目录下，在[mysqld]配置段下修改或添加port参数。命令行输入“SHOW VARIABLES LIKE 'port';”，查看占用端口。



核查并修改默认管理员用户名，命令行输入“RENAME USER 'root'@'localhost' TO 'new_admin'@'localhost';FLUSH PRIVILEGES;”重命名为其他用户名。

核查并修改管理员口令，命令行输入“ALTER USER '你的管理员用户名'@'主机名' IDENTIFIED BY '你的新复杂密码';FLUSH PRIVILEGES;”。

### 1.19.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接到SQLServer。

更换数据库管理系统的默认服务端口，在"TCP/IP属性"的"IP地址"选项卡下，"IPAll" 部分的"TCP端口"处设置新端口。



核查并修改默认管理员用户名，在SSMS中新建查询，输入语句“ALTER LOGIN sa WITH PASSWORD = 'YourNewPassword';”。



核查并修改管理员口令，在SSMS中新建查询，输入语句”ALTER LOGIN sa WITH PASSWORD = 'YourNewStrongPassword123!';”。



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例。

更换数据库管理系统的默认服务端口，在"TCP/IP属性"的"IP地址"选项卡下，"IPAll" 部分的"TCP端口"处设置新端口。



核查并修改默认管理员用户名，在命令行输入语句“ALTER LOGIN sa WITH PASSWORD = 'YourNewPassword';”。



核查并修改管理员口令，在命令行输入语句”ALTER LOGIN sa WITH PASSWORD = 'YourNewStrongPassword123!';”。



### 1.19.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看数据库监听端口配置。

set linesize 120 pagesize20

col name for a30

col value for a20

col description for a50

select

name,

value,

description

from v$parameter

where name in ('PORT_NUM', 'WEB_PORT', 'AP_PORT', 'MAL_PORT')

order by name;



结果示例如下：



根据需求修改默认服务端口。

接着，在disql提示符下输入以下命令，检查管理员用户名更改

col username for a20

col account_status for a15

col created for a20

col profile for a15

select

username,

account_status,

to_char(created, 'YYYY-MM-DD') as created_date,

profile,

case

when username in ('SYSDBA','SYSAUDITOR','SYSDB0') then '默认管理员'

when username like '%DBA%' or username like '%ADMIN%' then '自定义管理员'

else'普通用户'

end as 账户类型

from dba users

where username not in ('PUBLIC')

order by created_date;



结果示例如下：



最后，在disql提示符下输入以下命令，查看密码修改历史。

col username for a20

col password_date for a20

select

username,

to_char(password_date, 'YYYY-MM-DD HH24:MI:SS') as last_pwd_change,

round(sysdate - password_date) as days_since_change

from sysusers

where username in ('SYSDBA', 'SYSAUDITOR', 'SYSDBO')

or username 1ike '%ADMIN%'

order by password_date desc;



结果示例如下：



### 1.19.4 Redis

Windows操作系统下的核查方法

更换数据库管理系统的默认服务端口，在redis安装目录下找到redis.conf文件并打开，找到port参数并修改。



在6.0以下版本，没有用户概念，只有默认的一个管理员身份，通过密码进行认证。6.0及以上版本，即使使用了ACL，通常也会有一个默认用户（默认用户无需用户名，直接使用密码认证）。

中标麒麟、银河麒麟操作系统下的核查方法

更换数据库管理系统的默认服务端口，在redis安装目录下找到redis.conf文件并打开，找到port参数并修改。



在6.0以下版本，没有用户概念，只有默认的一个管理员身份，通过密码进行认证。6.0及以上版本，即使使用了ACL，通常也会有一个默认用户（默认用户无需用户名，直接使用密码认证）。

## 1.20 数据库管理系统应配置安全策略

### 1.20.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

设置密码复杂度策略，建议密码长度应至少8位，密码必须从字符（a-z,A-Z)、数字（0-9）、符号（~！@#￥%^&*()_<>）中至少选择两种进行组合。接着安装validate_password 插件进行密码强度检验，使用了该插件会检查设置的密码是否符合当前设置的强度规则，若不满足则拒绝设置。安装命令：“INSTALL PLUGIN validate_password SONAME 'validate_password.so';”卸载命令：“UNINSTALL PLUGIN  validate_password;”。再执行以下语句查看是否安装成功。



设置策略脚本。



查看策略及策略参数说明。



参数说明：validate_password_check_user_name ：默认关闭，设置为ON时可以将密码设置成当前用户名。validate_password_dictionary_file ：检查密码的字典文件的路径名。validate_password_length：密码最小长度。validate_password_mixed_case_count：最少需要拥有的大小写字母数（同时拥有大写和小写字母）。validate_password_number_count：最少需要拥有的数字个数。validate_password_special_char_count：最少需要拥有的特殊字符数。validate_password_policy：表示密码策略，默认为MEDIUM，有三个取值范围。

使用“GRANT ALL PRIVILEGES ON *.* TO 'km'@'%' IDENTIFIED BY 'km'”查看策略是否生效。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。

设置密码复杂度策略，建议密码长度应至少8位，密码必须从字符（a-z,A-Z)、数字（0-9）、符号（~！@#￥%^&*()_<>）中至少选择两种进行组合。接着安装validate_password 插件进行密码强度检验，使用了该插件会检查设置的密码是否符合当前设置的强度规则，若不满足则拒绝设置。安装命令：“INSTALL PLUGIN validate_password SONAME 'validate_password.so';”卸载命令：“UNINSTALL PLUGIN  validate_password;”。再执行以下语句查看是否安装成功。



设置策略脚本。



查看策略及策略参数说明。



参数说明：validate_password_check_user_name ：默认关闭，设置为ON时可以将密码设置成当前用户名。validate_password_dictionary_file ：检查密码的字典文件的路径名。validate_password_length：密码最小长度。validate_password_mixed_case_count：最少需要拥有的大小写字母数（同时拥有大写和小写字母）。validate_password_number_count：最少需要拥有的数字个数。validate_password_special_char_count：最少需要拥有的特殊字符数。validate_password_policy：表示密码策略，默认为MEDIUM，有三个取值范围。

使用“GRANT ALL PRIVILEGES ON *.* TO 'km'@'%' IDENTIFIED BY 'km'”查看策略是否生效。



### 1.20.2 SQLServer

判定标准：SQL Server登录已启用密码策略（is_policy_checked=1）与密码过期策略；登录审核级别为成功和失败（AuditLevel=3）；xp_cmdshell等危险扩展存储过程已禁用（config_value=0）；已设置合理的最大并发连接数与内存上限。

sp_configure 'user connections';

sp_configure 'xp_cmdshell';

EXEC xp_instance_regread N'HKEY_LOCAL_MACHINE', N'SOFTWARE\Microsoft\MSSQLServer\MSSQLServer', N'AuditLevel';

SELECT name, is_policy_checked, is_expiration_checked FROM sys.sql_logins;

在桌面鼠标右键-打开终端，在命令行界面输入命令“sqlcmd -S localhost -U SA -P 'SA账户密码'”连接到SQLServer实例，连接成功后，依次执行以下命令：

中标麒麟、银河麒麟操作系统下的核查方法

核查并发连接限制，执行“sp_configure 'user connections';”和“sp_configure 'max server memory';”查看最大并发用户连接数与内存上限。

核查并禁用危险扩展存储过程，执行“sp_configure 'show advanced options', 1; RECONFIGURE; sp_configure 'xp_cmdshell', 0; RECONFIGURE;”禁用xp_cmdshell，再执行“sp_configure 'xp_cmdshell';”确认config_value为0。

EXEC xp_instance_regread N'HKEY_LOCAL_MACHINE', N'SOFTWARE\Microsoft\MSSQLServer\MSSQLServer', N'AuditLevel';

核查登录审核配置，执行以下命令读取登录审核级别（0=无、1=仅成功、2=仅失败、3=成功和失败）：

对未启用的登录，执行“ALTER LOGIN 登录名 WITH CHECK_POLICY = ON, CHECK_EXPIRATION = ON;”强制其遵循Windows密码复杂度与过期策略。

SELECT name, is_policy_checked, is_expiration_checked FROM sys.sql_logins;

核查登录密码策略，在查询窗口执行以下命令，查看各SQL Server登录是否启用密码策略与密码过期策略：

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

Windows操作系统下的核查方法

### 1.20.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看安全相关系统参数。

set linesize 120 pagesize 20

col name for a40

col value for a20

col sys_value for a20

col description for a5e

select

name,

value,

sys_value,

description

from v$parameter

where name like '%SECURITY%'

or name like '%AUDIT%'

or name like '%PASSWORD%'

or name like '%ENCRYPT%'

order by name;



结果示例如下：



接着，检查密码安全策略，首先，在disql提示符下输入以下命令， 查看密码策略配置。

col profile for a20

col resource_name for a35

col limit for a25

col description for a50

select

profile,

resource_name,

limit,

description

from dba_profiles

where resource_type = 'PASSWORD'

order by profile, resource_name;



结果示例如下：



然后，在disql提示符下输入以下命令，查看密码复杂度验证函数定义。

set long 2000

col text for a100

select

name,

line,

text

from dba_source

where name = 'VERIFY_FUNCTION_STRONG'

and type ='FUNCTION'

order by line;



结果示例如下：



随后，在disql提示符下输入以下命令，查看审计配置参数。

select

name,

value,

description

from v$parameter

where name like '%AUDIT%'

order by name;



结果示例如下：



最后，在disql提示符下输入以下命令，检查加密安全策略。

select

name,

value,

description

from v$parameter

where name like '%ENCRYPT%'

or name like '%SSL%'

or name like '%CIPHER%'

order by name;



结果示例如下：



### 1.20.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，查看配置文件。



编辑conf文件，关键字bind和protected-mode,设置“bind 127.0.0.1”仅允许本机访问，“protected-mode yes”默认开启安全保护模式，拒绝外网请求。



配置 Redis 登录密码（requirepass），编辑conf文件增加“requirepass "YourStrongPassword123!"”,客户端连接时输入“redis-cli -a "YourStrongPassword123!"”。

禁止危险命令，在conf文件中找到以下关键字。



限制最大客户端数，在conf文件中找到maxclients，将maxclient改为10000，配置内存上限。



中标麒麟、银河麒麟操作系统下的核查方法

打开redis安装目录，查看配置文件。



编辑conf文件，关键字bind和protected-mode,设置“bind 127.0.0.1”仅允许本机访问，“protected-mode yes”默认开启安全保护模式，拒绝外网请求。





配置 Redis 登录密码（requirepass），编辑conf文件增加“requirepass "YourStrongPassword123!"”,客户端连接时输入“redis-cli -a "YourStrongPassword123!"”。

禁止危险命令，在conf文件中找到以下关键字。



限制最大客户端数，在conf文件中找到maxclients，将maxclient改为10000，配置内存上限。



## 1.21 数据库管理系统应具有行级或列级审计功能

判定标准：数据库已启用细粒度审计，能够对指定表的行级、列级访问留痕，审计记录包含操作时间、账户、语句与执行结果；仅有实例级日志而无对象级留痕即不合规。

### 1.21.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入“SET GLOBAL general_log = 'ON'”打开审计功能。输入“ SHOW VARIABLES LIKE '%general_log%';”查看审计功能是否开启。general	_log为ON则是开启状态。



首先创建一个employee表用于后续核查。

CREATE TABLE employees (

id INT AUTO_INCREMENT PRIMARY KEY,

name VARCHAR(100) NOT NULL,

salary DECIMAL(10,2) NOT NULL,

department VARCHAR(50),

position VARCHAR(50),

hire date DATE,

email VARCHAR(100),

phone VARCHAR(20),

created at TIMESTAMP DEFAULT CURRENT TIMESTAMP

updated at TIMESTAMP DEFAULT CURRENT TIMESTAMP ON UPDATE CURRENT TIMESTAMP

);



在employee表中插入数据，

INSERT INTO employees (name, salary, department, position, hire date, email, phone) VAl UES

('张三',8000.00,'技术部','软件工程师','2022-03-15','zhangsan@company.com','13800138001'),

('李四',12000.00,'技术部','高级工程师','2021-06-20','lisi@company.com','13800138002'),

('王五',15000.00,'技术部','技术经理',‘2020-01-10',‘wangwu@company.com',‘13800138003),

('赵六'，6000.00，'财务部','会计','2023-02-28','zhaoliu@company.com','13800138004'),

('钱七',9000.00,'财务部','财务主管','2021-11-05','qianqi@company.com','13800138005'),

('孙八',7000.00,'人事部','招聘专员','2022-09-12','sunba@company.com','13800138006'),

('周九',11000.00,'人事部','人事经理', '2020-07-22','zhoujiu@company.com','13800138007');



创建审计日志表来存储审计信息语句如下。

CREATE TABLE salary_audit_log (

id INT AUTO_INCREMENT PRIMARY KEY,

employee_id INT NOT NULL,

employee_name VARCHAR(100),

old_salary DECIMAL(10,2),

new_salary DECIMAL(10,2),

changed_by VARCHAR(100),

change_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

operation_type ENUM('INSERT', 'UPDATE', 'DELETE'),

FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE

);



在employee表上创建一个创建触发器（用于薪水列审计），监控price列的变化。语句如下。

```

-- 创建薪水更新审计触发器
DELIMITER //

CREATE TRIGGER after_employee_salary_update AFTER UPDATE ON employees
FOR EACH ROW BEGIN

    -- 只有当薪水发生变化时才记录

    IF OLD.salary <> NEW.salary THEN

        INSERT INTO salary_audit_log (employee_id, employee_name, old_salary, new_salary, changed_by, operation_type)

        VALUES (OLD.id, OLD.name, OLD.salary, NEW.salary, CURRENT_USER(), 'UPDATE');

    END IF;
END//

```



测试薪水更新审计。语句如下。

UPDATE employees SET salary = 8500.00 WHERE id = 1;

UPDATE employees SET salary = 13000.00 WHERE id = 2;

SELECT * FROM salary_audit_log;



结果如图，可以看到行级或列级审计功能正常。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。命令行输入“SET GLOBAL general_log = 'ON';”打开审计功能。输入“ SHOW VARIABLES LIKE '%general_log%';”查看审计功能是否开启。general	_log为ON则是开启状态。



首先创建一个employee表用于后续核查。

CREATE TABLE employees (

id INT AUTO_INCREMENT PRIMARY KEY,

name VARCHAR(100) NOT NULL,

salary DECIMAL(10,2) NOT NULL,

department VARCHAR(50),

position VARCHAR(50),

hire date DATE,

email VARCHAR(100),

phone VARCHAR(20),

created at TIMESTAMP DEFAULT CURRENT TIMESTAMP

updated at TIMESTAMP DEFAULT CURRENT TIMESTAMP ON UPDATE CURRENT TIMESTAMP

);



在employee表中插入数据，

('张三',8000.00,'技术部','软件工程师','2022-03-15','zhangsan@company.com','13800138001'),

('李四',12000.00,'技术部','高级工程师','2021-06-20','lisi@company.com','13800138002'),

('王五',15000.00,'技术部','技术经理',‘2020-01-10',‘wangwu@company.com',‘13800138003),

('赵六'，6000.00，'财务部','会计','2023-02-28','zhaoliu@company.com','13800138004'),

('钱七',9000.00,'财务部','财务主管','2021-11-05','qianqi@company.com','13800138005'),

('孙八',7000.00,'人事部','招聘专员','2022-09-12','sunba@company.com','13800138006'),

('周九',11000.00,'人事部','人事经理', '2020-07-22','zhoujiu@company.com','13800138007');



创建审计日志表来存储审计信息语句如下。

CREATE TABLE salary_audit_log (

id INT AUTO_INCREMENT PRIMARY KEY,

employee_id INT NOT NULL,

employee_name VARCHAR(100),

old_salary DECIMAL(10,2),

new_salary DECIMAL(10,2),

changed_by VARCHAR(100),

change_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

operation_type ENUM('INSERT', 'UPDATE', 'DELETE'),

FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE

);



在employee表上创建一个创建触发器（用于薪水列审计），监控price列的变化。语句如下。

```

-- 创建薪水更新审计触发器
DELIMITER //

CREATE TRIGGER after_employee_salary_update AFTER UPDATE ON employees
FOR EACH ROW BEGIN

    -- 只有当薪水发生变化时才记录

    IF OLD.salary <> NEW.salary THEN

        INSERT INTO salary_audit_log (employee_id, employee_name, old_salary, new_salary, changed_by, operation_type)

        VALUES (OLD.id, OLD.name, OLD.salary, NEW.salary, CURRENT_USER(), 'UPDATE');

    END IF;
END//

```



测试薪水更新审计。语句如下。

UPDATE employees SET salary = 8500.00 WHERE id = 1;

UPDATE employees SET salary = 13000.00 WHERE id = 2;

SELECT * FROM salary_audit_log;



结果如图，可以看到行级或列级审计功能正常。



### 1.21.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接到SQLServer，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。使用DML触发器记录行数据变化，为需要审计的表创建AFTER INSERT, UPDATE, DELETE触发器，将变化的数据记录到专门的审计表中。语句如下：

CREATE TRIGGER Tr_YourTable_AuditUpdate

ON YourTable

AFTER UPDATE

AS

BEGIN

SET NOCOUNT ON;

INSERT INTO YourAuditTable (AuditTime, UserName, Operation, AffectedRowID, OldData, NewData)

SELECT

GETDATE(),

USER_NAME(),

'UPDATE',

i.ID,

(SELECT * FROM deleted FOR JSON AUTO),

(SELECT * FROM inserted FOR JSON AUTO)

FROM inserted i

INNER JOIN deleted d ON i.ID = d.ID;

END;



结果示例如下：



触发器状态验证，在查询窗口中执行以下命令：

SELECT

name,

type_desc,

is_disabled,

is_instead_of_trigger,

create_date,

modify_date

FROM sys.triggers

WHERE name = 'Tr_YourTable_AuditUpdate';



验证结果示例：



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，使用DML触发器记录行数据变化，为需要审计的表创建AFTER INSERT, UPDATE, DELETE触发器，将变化的数据记录到专门的审计表中。在命令行输入以下语句：

CREATE TRIGGER Tr_YourTable_AuditUpdate

ON YourTable

AFTER UPDATE

AS

BEGIN

SET NOCOUNT ON;

INSERT INTO YourAuditTable (AuditTime, UserName, Operation, AffectedRowID, OldData, NewData)

SELECT

GETDATE(),

USER_NAME(),

'UPDATE',

i.ID,

(SELECT * FROM deleted FOR JSON AUTO),

(SELECT * FROM inserted FOR JSON AUTO)

FROM inserted i

INNER JOIN deleted d ON i.ID = d.ID;

END;



结果示例如下：



触发器状态验证，在命令行输入以下语句：

SELECT

name,

type_desc,

is_disabled,

is_instead_of_trigger,

create_date,

modify_date

FROM sys.triggers

WHERE name = 'Tr_YourTable_AuditUpdate';



验证结果示例：



### 1.21.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看审计参数配置。

set linesize 120 pagesize 20

col name for a35

col value for a20

col description for a60

select

name,

value,

description

from v$parameter

where name like '%AUDIT%'

order by name;



结果示例如下：



接着，检查行级审计策略，首先，在disql提示符下输入以下命令， 查看行级审计策略配置。

col policy_name for a35

col object_schema for a15

col object_name for a25

col policy_column for a30

col policy_condition for a50

select

policy_name,

object_schema,

object_name,

policy_column,

policy_condition,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_audit_row_policies

order by object_schema, object_name, policy_name;



结果示例如下：



在disql提示符下继续输入以下命令，查看行级审计详细配置。

col audit_operation for a30

col audit_trail_level for a20

col audit_condition for a60

select

policy_name,

audit_operation,

audit_trail_level,

audit_condition,

record_columns

from dba_audit_row_policy_details

where enabled = 'YES'

order by policy_name;



结果示例如下：



最后，检查列级审计策略，首先，在disql提示符下输入以下命令，查看列级审计策略配置。

col policy_name for a35

col object_schema for a15

col object_name for a25

col column_list for a5e

col audit_type for a20

select

policy_name,

object_schema,

object_name,

column_list,

audit_type,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_audit_column_policies

order by object_schema, object_name, policy_name;



结果示例如下：



继续在disql提示符下输入以下命令，查看列级审计详细配置。

col access_action for a30

col alert_level for a15

col retention_period for a10

select

policy_name,

access_action,

alert_level,

retention_period,

mask_enabled,

mask_format,

notification_enabled

from dba_audit_column_policy_details

where enabled = 'YES'

order by policy_name;



结果示例如下：



### 1.21.4 Redis

Redis作为键值存储，没有传统关系型数据库的行列概念，不支持行级和列级审计。

## 1.22 数据库管理系统应采取单独、安全监控、审计指施

判定标准：数据库审计由独立审计账户或独立审计设备承载，审计数据存放于审计员控制范围，数据库管理员无法删除或篡改审计记录；审计与数据库管理权限未分离即不合规。

### 1.22.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。执行“SHOW VARIABLES LIKE '%general_log%';”核查审计功能状态。值为ON为启动。“SET GLOBAL general_log = 'ON';”启动日志。



核查审计进程保护，尝试使用一个非授权的数据库账户（无SUPER权限）执行 “SET GLOBAL audit_logging = OFF; ”或 “SET GLOBAL general_log = OFF;”操作应失败。这确保了审计进程不会被随意中断，符合安全要求。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。执行“SHOW VARIABLES LIKE '%general_log%';”核查审计功能状态。值为ON为启动。“SET GLOBAL general_log = 'ON';”启动日志。



核查审计进程保护，尝试使用一个非授权的数据库账户（无SUPER权限）执行 “SET GLOBAL audit_logging = OFF; ”，操作应失败。这确保了审计进程不会被随意中断，符合安全要求。



### 1.22.2 SQLServer

Windows操作系统下的核查方法

在 SSMS 中，对象资源管理器 -> “安全性” -> “审核” -> 右键“新建审核”。指定审核名称、文件路径（确保路径存在且 SQL Server 服务账户有读写权限）、最大文件大小等。关键：将 On Audit Log Failure 设置为 SHUTDOWN SERVER 以确保审计失败时服务停止，防止审计被绕过。





“安全性” -> “服务器审核规范” -> 右键“新建服务器审核规范”。将其绑定到上一步创建的服务器审核。添加需要记录的服务器级操作，如：FAILED_LOGIN_GROUP（记录所有失败登录）SUCCESSFUL_LOGIN_GROUP（记录所有成功登录）SERVER_ROLE_MEMBER_CHANGE_GROUP（服务器角色成员变更）SERVER_PERMISSION_CHANGE_GROUP（服务器权限变更）。





创建数据库审核规范：在具体的用户数据库下，“安全性” -> “数据库审核规范” -> 右键“新建数据库审核规范”。将其绑定到同一个服务器审核。添加需要记录的数据库级操作，如：SELECT, INSERT, UPDATE, DELETE ON SCHEMA::dbo BY public（记录所有对 dbo 架构的增删改查）DATABASE_OBJECT_CHANGE_GROUP（记录所有 DDL 语句，如 CREATE/ALTER/DROP）。



依次启用服务器审核、服务器审核规范、数据库审核规范。执行一些测试操作（如失败登录、查询数据），然后通过以下方式查看日志：在 SSMS 中右键审核对象 -> “查看审核日志”。使用函数 sys.fn_get_audit_file ('filepath/*', NULL, NULL); 查询审计文件。将审计文件路径设置为一个安全、容量充足的磁盘。制定日志归档和清理策略。可以将审计日志实时传输到专用的 SIEM（安全信息和事件管理）系统。

中标麒麟、银河麒麟操作系统下的核查方法

在 SSMS 中，对象资源管理器 -> “安全性” -> “审核” -> 右键“新建审核”。指定审核名称、文件路径（确保路径存在且 SQL Server 服务账户有读写权限）、最大文件大小等。关键：将 On Audit Log Failure 设置为 SHUTDOWN SERVER 以确保审计失败时服务停止，防止审计被绕过。





“安全性” -> “服务器审核规范” -> 右键“新建服务器审核规范”。将其绑定到上一步创建的服务器审核。添加需要记录的服务器级操作，如：FAILED_LOGIN_GROUP（记录所有失败登录）SUCCESSFUL_LOGIN_GROUP（记录所有成功登录）SERVER_ROLE_MEMBER_CHANGE_GROUP（服务器角色成员变更）SERVER_PERMISSION_CHANGE_GROUP（服务器权限变更）。





创建数据库审核规范：在具体的用户数据库下，“安全性” -> “数据库审核规范” -> 右键“新建数据库审核规范”。将其绑定到同一个服务器审核。添加需要记录的数据库级操作，如：SELECT, INSERT, UPDATE, DELETE ON SCHEMA::dbo BY public（记录所有对 dbo 架构的增删改查）DATABASE_OBJECT_CHANGE_GROUP（记录所有 DDL 语句，如 CREATE/ALTER/DROP）。



依次启用服务器审核、服务器审核规范、数据库审核规范。执行一些测试操作（如失败登录、查询数据），然后通过以下方式查看日志：在 SSMS 中右键审核对象 -> “查看审核日志”。使用函数 sys.fn_get_audit_file ('filepath/*', NULL, NULL); 查询审计文件。将审计文件路径设置为一个安全、容量充足的磁盘。制定日志归档和清理策略。可以将审计日志实时传输到专用的 SIEM（安全信息和事件管理）系统。

### 1.22.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看审计服务器配置参数。

set linesize 120 pagesize 20

col name for a40

col value for a30

col description for a60

select

name,

value,

description

from v$parameter

where name like '%AUDIT%SERVER%'

or name like '%AUDIT%REMOTE%'

or name like '%AUDIT%CENTRAL%'

order by name;



结果示例如下：



接着，检查独立审计存储配置，在disql提示符下输入以下命令，查看审计表空间配置。

col tablespace_name for a25

col file_name for a60

col status for a10

select

tablespace_name,

file_name,

bytes/1024/1024 as size_mb,

status,

autoextensible

from dba_data_files

where tablespace_name like '%AUDIT%'

or tablespace_name like '%AUD%"

order by tablespace_name;



结果示例如下：



然后，在disql提示符下输入以下命令，查看审计日志加密配置。

select

name,

value,

description

from v$parameter

where name like '%AUDIT%ENCRYPT%'

or name like '%AUDIT%INTEGRITY%'

or name 1ike '%AUDIT%SIGN%'

order by name;



结果示例如下：



### 1.22.4 Redis

监控 Redis 运行指标，首先部署redis-exporter。

## 1.23 数据库管理系统仅为应用服务器提供访问服务

判定标准：数据库仅监听应用服务器网段，防火墙、白名单或监听地址绑定限制来源仅限应用服务器；任意终端均可直连数据库即不合规。

### 1.23.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。输入命令“SHOW VARIABLES LIKE 'bind_address';”，然后查看结果，Value 字段显示为应用服务器的IP地址。



检查用户及访问来源，，不应存在'%'作为host的用户（允许任意来源），用户host应为具体的应用服务器IP或内网网段，执行以下语句。

SELECT user, host FROM mysql.user;

SELECT user, host, authentication_string, account_locked

FROM mysql.user

WHERE host NOT IN ('localhost','127.0.0.1');



执行“SHOW GRANTS FOR 'app_user'@'应用服务器IP';”核查应用账户权限。执行“DROP USER '用户名'@'%';”删除允许任意主机访问的用户。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库。输入命令“SHOW VARIABLES LIKE 'bind_address';”，然后查看结果，Value 字段显示为应用服务器的IP地址。



检查用户及访问来源，，不应存在'%'作为host的用户（允许任意来源），用户host应为具体的应用服务器IP或内网网段，执行以下语句。

SELECT user, host FROM mysql.user;

SELECT user, host, authentication_string, account_locked

FROM mysql.user

WHERE host NOT IN ('localhost','127.0.0.1');



执行“SHOW GRANTS FOR 'app_user'@'应用服务器IP';”核查应用账户权限。执行“DROP USER '用户名'@'%';”删除允许任意主机访问的用户。

### 1.23.2 SQLServer

中标麒麟、银河麒麟操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。

在查询窗口输入以下命令：

SELECT local_tcp_port, client_net_address, auth_scheme

FROM sys.dm_exec_connections

WHERE session_id = @@SPID;



结果示例如下：



TCP 动态端口应为空，且 TCP 端口正确。同时，上方每个具体IP地址的已启用应为“是”，且 TCP 端口正确，但活动的IP应主要为应用服务器所在网段。

### 1.23.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看数据库监听配置。

set linesize 120 pagesize 20

col name for a30

col value for a40

col description for a60

select

name,

value,

description

from v$parameter

Where name in ('LISTEN ADDRESS', 'LISTEN ADDRESS1', 'LISTEN_ADDRESS2', 'PORT_ NUM')

order by name;



结果示例如下：



接着，在disql提示符下输入以下命令，查询所有数据库会话及来源。

col sid for 99999

col serial

col username for a20

col program for a30

col terminal for a20

col machine for a30

col ip_address for a20

select

sid,

serial,

username,

program,

terminal,

machine,

substr(client_ip, 1, 15) as ip_address,

status,

to_char(logon_time, 'YYYY-MM-DD HH24:MI:SS') as logon_time

from v$sessions

where username is not null

order by logon_time desc;



结果示例如下：



然后，在disql提示符下输入以下命令，查看应用专用账户。

col username for a20

col account_status for a15

col default_tablespace for a20

col profile for a15

select

username,

account_status,

default_tablespace,

profile,

to_char(created, 'WYY-MM-DD') as created_date

from dba_userswhere username like '%APP%'

or username like '%WEB%

or username like '%SERVICE%'

or username in ('APP_USER','WEB_USER', 'SERVICE_USER')

order by created_date;



结果示例如下：



最后，在disql提示符下输入以下命令，查看应用账户权限。

select

grantee,

privilege,

admin_option

from dba_sys_privs

where grantee in ('APP _USER', 'WEB_SERVICE', 'API_USER', 'REPORT_SERVICE')

order by grantee, privilege;



结果示例如下：



### 1.23.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，查看配置文件。



应设置为应用服务器IP或局域网网段（如 bind 192.168.1.100）。禁止设置为 0.0.0.0（允许所有IP）或仅 127.0.0.1（仅允许本机），查看bind指令，bind应包含127.0.0.1和应用服务器IP。



如果未设置密码且bind设置为公网IP，此参数应设为no以关闭保护模式 。但强烈建议通过设置密码并正确配置bind来保障安全，而非依赖关闭此模式。protected-mode配置protected-mode应为yes。



中标麒麟、银河麒麟操作系统下的核查方法

打开redis安装目录，查看配置文件。



应设置为应用服务器IP或局域网网段（如 bind 192.168.1.100）。禁止设置为 0.0.0.0（允许所有IP）或仅 127.0.0.1（仅允许本机），查看bind指令，bind应包含127.0.0.1和应用服务器IP。



如果未设置密码且bind设置为公网IP，此参数应设为no以关闭保护模式 。但强烈建议通过设置密码并正确配置bind来保障安全，而非依赖关闭此模式。protected-mode配置protected-mode应为yes。



## 1.24 应具备日志审计能力，审计日志至少保留180天

### 1.24.1操作系统日志审计核查

#### 1.24.1.1 Windows XP / Windows 7

使用具有管理员权限的账号本地登录系统，开始-搜索框-搜索“终端”-右键以管理员身份运行打开命令提示符，首先，核查审计策略是否启用，输入命令“auditpol /get /category:*”。



由于Windows事件日志本身通常无法直接设置长达180天的保留时间，我们通常通过自动归档和保存历史日志文件的方式来实现。开始-搜索框搜索“事件查看器”，展开 "Windows 日志"，右键点击需要设置的日志（如 "安全"），选择 "属性"。在 "日志最大大小" 处，设置一个合适的大小（例如 1024 MB），确保在备份周期内不会写满。

在 "达到事件日志最大大小时" 部分，选择 "不覆盖事件(手动清除日志)" 或 "按需要覆盖事件(日志已满时归档)"。这可以确保在备份前日志不会被新事件覆盖。



将以下代码保存为 EventLogBackup.bat。请根据你的实际情况修改 BACKUP_PATH（本地临时备份目录）和 RETENTION_DAYS（日志保留天数，例如180）。

@echo off

setlocal EnableDelayedExpansion

set BACKUP_PATH=C:\EventLogBackups

set RETENTION_DAYS=180

Set COMPUTERNAME=%COMPUTERNAME%

for /f "tokens=1-3 delims=/ " %%a in ('date /t') do (set DATE_STR=%%c-%%a-%%b)

set BACKUP_DIR=%BACKUP_PATH%\%DATE_STR%

if not exist "%BACKUP_DIR%" mkdir "%BACKUP_DIR%"

echo %date% %time%: Starting event log backup. >> "%BACKUP PATH%\backup.log"

for %%L in (Security, System, Application) do (

echo Backing up %%L log...

Wevtuti1 epl "%%L " "%BACKUP_DIR%\%COMPUTERNAME%_%%L_%DATE_STR%.evtx" 2>> "%BACKUP_PATH%\backup_error.log"

if !errorlevel! equ 0 (

echo Successfully backed up %%L. >> "%BACKUP_PATH%\backup.log"

)else(

echo ERROR: Failed to backup %L. Check backup_error. log. >> "%BACKUP PATH% \backup. log"

)

)

echo Cleaning up backups older than %RETENTION DAYS% days... >> "%BACKUP PATH% \backup. log"

forfiles /p "%BACKUP PATHt%" /s /m * /d -%RETENTION DAYs% /c "cmd /c if @isdir==FALSE echo Deleting

@file ... && del @path" >> "%BACKUP_PATH%\backup.log"

echo %date% %time%: Backup and cleanup completed. >> "%BACKUP_PATH%\backup.log"

endlocal



为了让脚本定期运行，需要创建计划任务。打开 "任务计划程序" （可以通过"开始"菜单搜索）。点击右侧的 "创建基本任务..."。按照向导配置：触发器：设置为 "每天" 执行，并选择一个业务低峰时间（例如午夜）。操作：选择 "启动程序"，然后浏览并选择你刚才保存的 EventLogBackup.bat 脚本。创建完成后，在任务属性中，最好勾选 "不管用户是否登录都要运行" 并使用具有适当权限的账户（如管理员账户）。





#### 1.24.1.2 中标麒麟 / 银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查审计服务状态。

sudo systemctl status auditd



示例如下。



创建脚本，输入如下命令，设置审计日志保留180天。

### 1.24.2数据库日志审计核查

#### 1.24.2.1MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，按下回车，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，依次执行以下命令：

SHOW PLUGINS;

SHOW VARIABLES LIKE 'general_log%';

SHOW VARIABLES LIKE 'slow_query_log%';

SHOW VARIABLES LIKE 'binlog_expire_logs_seconds';



然后，查看结果，合规状态：审计插件状态为ACTIVE，或相关日志功能为ON。binlog_expire_logs_seconds 值应大于 15552000 秒（180天）。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，依次执行以下命令：

SHOW PLUGINS;

SHOW VARIABLES LIKE 'general_log%';

SHOW VARIABLES LIKE 'slow_query_log%';

SHOW VARIABLES LIKE 'binlog_expire_logs_seconds';



然后，查看结果，合规状态：审计插件状态为ACTIVE，或相关日志功能为ON。binlog_expire_logs_seconds 值应大于 15552000 秒（180天）。







#### 1.24.2.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。首先，检查服务器级审计，在查询窗口下执行以下命令，其中文件地址注意替换为指定文件地址：

SELECT * FROM sys.dm_server_audit_status;

EXEC xp_instance_regread N'HKEY_LOCAL_MACHINE', N'Software\Microsoft\ MSSQLServer\MSSQLServer', N'NumErrorLogs';



结果示例如下：





然后，检查结果。合规状态应为：sys.dm_server_audit_status 应返回审计记录，且状态为“1”（已启动）。NumErrorLogs 值应足够大，以确保旧日志在180天内不会被覆盖。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，首先，检查服务器级审计，在命令行输入以下语句，其中文件地址注意替换为指定文件地址：

SELECT * FROM sys.dm_server_audit_status;

EXEC xp_instance_regread N'HKEY_LOCAL_MACHINE', N'Software\Microsoft\ MSSQLServer\MSSQLServer', N'NumErrorLogs';



结果示例如下：





然后，检查结果。合规状态应为：sys.dm_server_audit_status 应返回审计记录，且状态为“1”（已启动）。NumErrorLogs 值应足够大，以确保旧日志在180天内不会被覆盖。

#### 1.24.2.3达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看审计参数配置。

set linesize 120 pagesize 20

col name for a40

col value for a20

col sys_value for a20

col description for a60

select

name,

value,

sys_value,

description

from v$parameter

where name like '%AUDIT%'

order by name;



结果示例如下：



接着，在disql提示符下输入以下命令，查看审计日志保留配置。

col parameter_name for a45

col parameter_value for a30

col description for a50

select

parameter_name,

parameter_value,

description

from dba_audit_mgmt_config_params

where parameter_name like '%RETENTION%'

or parameter_name like '%PURGE%'

or parameter_name like '%ARCHIVE%'

order by parameter_name;



结果示例如下：



最后，在disql提示符下输入以下命令，查看审计日志最早和最晚记录时间。

select

'审计日志记录时间范围’as检查项，

to_char(min(ptime),'YYYY-MM-DD')as 最早记录日期,

to_char(max(ptime),'YYYY-MM-DD')as 最晚记录日期,

round(sysdate - min(ptime)) as 已保留天数,

case

when round(sysdate - min(ptime)) >= 180

then '√符合要求(≥180天)'

else '×不符合要求(<180天)'

end as 合规状态

from dba_audit_trail;



结果示例如下：



#### 1.24.2.4Redis

判定标准：日志功能已开启且logfile指向具体文件，loglevel配置合理；慢查询日志已配置；AOF持久化已按需开启；日志已配置轮转与归档，保留时间不低于180天。

输入“grep -E "logfile|loglevel" /etc/redis/redis.conf”查看日志文件路径，输入“cat /etc/logrotate.d/redis”或查看系统日志轮转配置，确认日志是否按天轮转并保留，保留天数应不低于180天。

输入“CONFIG GET dir”“CONFIG GET appendonly”“CONFIG GET appendfilename”“INFO persistence”核查数据目录与AOF持久化配置。

在桌面鼠标右键-打开终端，命令行输入“redis-cli -a 密码”连接Redis服务，依次输入“CONFIG GET logfile”“CONFIG GET loglevel”“CONFIG GET slowlog-log-slower-than”“CONFIG GET slowlog-max-len”“SLOWLOG LEN”“SLOWLOG GET 10”核查日志与慢查询配置。

中标麒麟、银河麒麟操作系统下的核查方法

核查日志保留策略，查看redis.conf中logfile配置及对应日志文件的轮转与保留设置，或通过操作系统计划任务对日志文件定期归档，确保日志保留不低于180天。

核查持久化与日志文件位置，输入“CONFIG GET dir”“CONFIG GET appendonly”“CONFIG GET appendfilename”“INFO persistence”查看数据目录、AOF是否开启及文件名称。

核查慢查询日志，依次输入“CONFIG GET slowlog-log-slower-than”“CONFIG GET slowlog-max-len”“SLOWLOG LEN”“SLOWLOG GET 10”查看慢查询阈值、条数上限及已记录的慢查询。

核查日志开启情况，依次输入“CONFIG GET logfile”“CONFIG GET loglevel”查看日志文件路径与日志级别；logfile为空表示日志输出到控制台，应配置为具体文件路径，loglevel建议设置为notice或warning。

打开Redis安装目录，在redis-cli.exe程序的当前目录下鼠标右键打开终端，命令行输入“redis-cli.exe -a 密码”连接Redis服务。

Windows操作系统下的核查方法

## 1.25 检查是否具备边界保护能力,是否可以抗攻击，防纂改

判定标准：网络边界已部署防火墙、IPS、防篡改等边界防护能力，典型攻击行为与篡改尝试可被识别、告警或阻断且防护日志留存完整；无任何边界防护措施即不合规。

### 1.25.1操作系统抗攻击与防篡改核查

#### 1.25.1.1Windows XP / Windows 7

使用具有管理员权限的账号本地登录系统，打开命令提示符：使用 Win + R 组合键打开“运行”框，输入 cmd 并回车。命令行输入“net user”，列出所有账户。仔细核对列表，发现任何未知或可疑账户（如名称怪异、非自己创建的），需使用 “net user "用户名" /del ”立即删除。



然后，核查防病毒与防护，输入命令“sc query Windefend”。确认服务状态为 RUNNING。



开始-搜索框搜索PowerShell，运行“ Get-Service -Name windefend”，确认状态为 Running。



在PowerShell中运行 Get-MpComputerStatus，重点关注 AntivirusEnabled 和 RealTimeProtectionEnabled 等字段是否为 True。



#### 1.25.1.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，首先在命令行输入命令“sudo ufw status verbose”检查防火墙状态。



然后在命令行输入命令，检查外部可访问服务，

sudo ss -tulpn | grep LISTEN |awk '{print $5}'| cut -d: -f2 | sort -nu





结果中返回的数字代表外部可访问的服务端口号。

接着，检查文件完整性，安装和使用AIDE，依次执行如下命令：

sudo aide --check

sudo apt update

sudo apt install aide -y





最后，检查系统完整性，输入命令“sudo mokutil --sb-state”检查安全启动机制,然后检查内核保护机制，输入命令“dmesg | grep -i “protection” | head -5”。

### 1.25.2数据库抗攻击与防篡改核查

#### 1.25.2.1MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，按下回车，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行以下命令：

SHOW VARIABLES LIKE 'bind_address';

SELECT user, host, authentication_string FROM mysql.user WHERE authentication_string=' '

OR LENGTH(authentication_string) < 20;

SHOW VARIABLES LIKE 'log_bin';



然后查看结果，bind_address 不应为 0.0.0.0；无空密码或弱密码账户；log_bin 值为 ON。







中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行以下命令：

SHOW VARIABLES LIKE 'bind_address';

SELECT user, host, authentication_string FROM mysql.user WHERE authentication_string=' '

OR LENGTH(authentication_string) < 20;

SHOW VARIABLES LIKE 'log_bin';



然后查看结果，bind_address 不应为 0.0.0.0；无空密码或弱密码账户；log_bin 值为 ON。





#### 1.25.2.2 SQLServer

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口。在查询窗口下执行以下命令：

SELECT SERVERPROPERTY('IsIntegratedSecurityOnly');

EXEC sp_readerrorlog 0, 1, 'audit';



结果示例如下：





然后，查看结果，合规状态应为：IsIntegratedSecurityOnly 值为 1 更安全；错误日志中应存在审计相关的启动记录。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令：

SELECT SERVERPROPERTY('IsIntegratedSecurityOnly');

EXEC sp_readerrorlog 0, 1, 'audit';



结果示例如下：





然后，查看结果，合规状态应为：IsIntegratedSecurityOnly 值为 1 更安全；错误日志中应存在审计相关的启动记录。

#### 1.25.2.3达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看网络边界防护参数。

set linesize 120 pagesize 20

col name for a40

col value for a30

col description for a60

select

name,

value,

description

from v$parameter

where name 1ike '%IP%CONTROL%'

or name like '%ACCESS%CONTROL%'

or name like '%BOUNDARY%'

or name like '%SECURITY%'

order by name;



结果示例如下：



接着，在disql提示符下输入以下命令，查看防SQL注入配置。

select

name,

value,

description

from v$parameter

where name like '%INJECTION%'

or name like '%SQL%SECURITY%'

or name 1ike '%BIND%'

order by name;



结果示例如下：



然后，在disql提示符下输入以下命令，查看防暴力破解配置。

select

profile,

resource_name,

limit,

description

from dba_profiles

where resource_name in ('FAILED_LOGIN_ ATTEMPTS', 'PASSWORD_LOCK_TIME')

order by profile, resource_name;



结果示例如下：



随后，在disql提示符下输入以下命令，查看审计日志防篡改配置。

select

name,

value,

description

from v$parameter

where name like '%AUDIT%ENCRYPT%'

or name like '%AUDIT%INTEGRITY%'

or name like '%AUDIT%SIGN%'

order by name;



结果示例如下：



最后，在disql提示符下输入以下命令，查看数据文件防篡改。

col protection_name for a35

col protection_type for a25

col protection_level for a15

select

protection_name,

protection_type,

protection_level,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_data_protection_policies

where enabled = 'YES'

order by protection_type;



结果示例如下：



#### 1.25.2.4 Redis

Windows操作系统下的核查方法

打开redis安装目录，先双击redis-server.exe，再双击redis-cli.exe连接到redis。 





找到redis.xx.conf配置文件，然后检查结果，合规状态应为：已设置强密码 requirepass；已重命名或禁用危险命令；bind 指令未绑定到 0.0.0.0。



中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。 



找到redis.conf配置文件，然后检查结果，合规状态应为：已设置强密码 requirepass；已重命名或禁用危险命令；bind 指令未绑定到 0.0.0.0。



## 1.26 检查是否有防病毒日志、补丁日志、记录相关信息记录信息的完整，有效

判定标准：防病毒日志、补丁日志等记录信息完整（时间、操作、结果、操作人等要素齐全）且留存不少于180天，各类日志之间可相互印证；记录缺失、要素不全或可被随意删除即不合规。

### 1.26.1操作系统安全日志核查

#### 1.26.1.1 Windows XP / Windows 7

使用具有管理员权限的账号本地登录系统，打开命令提示符：使用 Win + R 组合键打开“运行”框，输入 cmd 并回车，在命令行界面依次输入以下命令：

检查防病毒软件运行状态：“wmic /namespace:\\root\securitycenter2 path antivirusproduct get displayName, productState, pathToSignedProductExe”；



查看系统补丁安装记录：“wmic qfe list brief”；



检查系统安全事件日志（最近24小时）：开始-搜索框搜索终端-右键以管理员身份运行，命令行输入“wevtutil qe Security /c:10 /f:text /q:"*[System[TimeCreated[timediff(@SystemTime) <= 86400000]]]"”。



最后查看结果：防病毒软件应显示已安装的防病毒软件名称且状态正常；补丁记录应显示完整的补丁列表，包含安装日期、描述等信息。安全日志应能返回最近的安全事件记录，证明日志记录功能正常。

#### 1.26.1.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“systemctl list-units | grep -i "antivirus\|clamav\|rkhunter" | head -5”检查防病毒服务状态。输入“sudo apt update”检查可用更新。



然后检查认证日志文件，输入命令“sudo ls -la /var/log/auth.log*”。



检查系统日志，输入命令“sudo tail -10 /var/log/syslog”。

### 1.26.2数据库安全日志核查

#### 1.26.2.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，按下回车，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行以下命令：

SHOW VARIABLES LIKE 'log_error';

SHOW VARIABLES LIKE 'general_log%';

SHOW VARIABLES LIKE 'log_bin';



然后查看结果，合规状态应为：错误日志文件存在且可访问；通用查询日志或二进制日志至少有一项开启；日志内容包含时间戳和详细操作信息。



中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行以下命令：

SHOW VARIABLES LIKE 'log_error';

SHOW VARIABLES LIKE 'general_log%';

SHOW VARIABLES LIKE 'log_bin';



然后查看结果，合规状态应为：错误日志文件存在且可访问；通用查询日志或二进制日志至少有一项开启；日志内容包含时间戳和详细操作信息。







#### 1.26.2.2 SQL Server

Windows操作系统下的核查方法

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口，在查询窗口下执行以下命令：

EXEC xp_readerrorlog 0, 1, NULL, NULL, NULL, NULL, N'desc';

SELECT * FROM sys.dm_server_audit_status;



结果示例如下：



接着，查看结果，合规状态应为：错误日志能正常读取且包含近期记录；审计功能状态正常。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令：

EXEC xp_readerrorlog 0, 1, NULL, NULL, NULL, NULL, N'desc';

SELECT * FROM sys.dm_server_audit_status;



结果示例如下：



接着，查看结果，合规状态应为：错误日志能正常读取且包含近期记录；审计功能状态正常。

#### 1.26.2.3达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看补丁历史记录。

set linesize 120 pagesize 20

col patch_name for a30

col patch_desc for a50

col apply_time for a25

col patch_type for a20

col patch_version for a20

select

patch_name,

patch_desc,

patch_type,

patch_version,

to_char(apply_time, 'YYYY-MM-DD HH24:MI:SS') as apply_time,

apply_user,

verify_status

from v$patch_history

order by apply_time desc;



结果示例如下：



接着，在disql提示符下输入以下命令，查看数据库内置病毒扫描日志。

col scan_id for 99999

col scan_type for a20

col scan_target for a40

col virus_name for a30

col action_taken for a20

select

scan_id,

scan_type,

scan_target,

virus_name,

action_taken,

to_char(scan_time,'YYYY-MM-DD HH24:MI:SS') as scan_time,

scan_result

from dba_antivirus_logs

order by scan_time desc

fetch first 15 rows only;



结果示例如下：



紧接着，在disql提示符下输入以下命令，查看防病毒策略配置。

col policy_name for a30

col scan_schedule for a30

col action for a30

select

policy_name,

scan_type,

scan_schedule,

scan_path,

file_extension,

action,

enabled,

to_char(created, 'YYYY-MM-DD') as created_date

from dba_antivirus_policies

where enabled = 'YES'

order by policy_name;



结果示例如下：



最后，在disql提示符下输入以下命令，查看审计日志完整性验证。

col log_file for a60

col integrity_status for a15

col sign_algorithm for a15

col verify_status for a15

select

log_file,

round(file_size/1024/1024, 2) as size_mb,

integrity_status,

sign_algorithm,

verify_status,

to_char(create_time, 'YYYY-MM-DD HH24:MI:SS') as create_time,

to_char(last_verify_time, 'YYYY-MM-DD HH24:MI:SS') as last_verify

from dba_audit_log_integrity

order by create_time desc

fetch first 15 rows only;



结果示例如下：



#### 1.26.2.4 Redis

Windows操作系统下的核查方法

使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“redis-cli”连接到服务器。连接后，使用文本编辑器打开，输入以下命令：

logfile /var/log/redis/redis-server.log

loglevel notice



接着，查看Redis日志最近内容，在redis安装目录下的server_log.txt文件中。





中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis。连接后，使用文本编辑器打开，输入以下命令：

logfile /var/log/redis/redis-server.log

loglevel notice



接着，查看Redis日志最近内容，在redis安装目录下的server_log.txt文件中。





## 1.27 操作系统、数据库管理系统、中间件、办公软件等基础软件应采用具有完备的售后技术支持与服务的正版或定制软件

判定标准：操作系统、数据库、中间件、办公软件等均有正版授权凭证或定制开发合同，版本仍在支持期内且可获得售后技术支持；使用无授权或已停止技术支持的软件即不合规。

### 1.27.1操作系统

Windows7/WindowXP

任务栏搜索框输入“cmd”或“命令提示符”，右键搜索结果，选择“以管理员身份运行”，点击“是”确认。首先，输入“slmgr /dli”查看系统版本和授权状态，结果示例如下：



接着，输入“systeminfo | findstr /B /C:"OS Name" /C:"OS Version" /C:"System Manufacturer" /C:"System Model" ”查看系统基本信息，结果示例如下：



最后，输入“wmic os get name,version,productid,installdate,manufacturer”查看产品id，结果示例如下：



中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“cat /etc/os-release”查看系统版本信息，结果示例如下：



接着输入“cat /etc/.kyinfo 2>/dev/null”查看授权信息，结果示例如下：



然后，检查技术支持状态，输入“cat /etc/yum.repos.d/NeoKylin.repo”检查YUM源配置是否为官方源，结果示例如下：



1.27.2数据库管理系统

MySQL

使用win+R组合键打开运行框，输入cmd，按下回车，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，输入以下命令“ SHOW VARIABLES LIKE 'version%';”“SELECT VERSION();”“SHOW VARIABLES LIKE 'license';”查看MySQL版本和授权信息，结果示例如下：



接着，输入命令“systemctl status mysqld”检查MySQL服务状态。

SQL Server

登录到SQL Server Management Studio (SSMS)，输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口，在查询窗口下执行以下命令，查看SQL Server版本和授权信息：

SELECT 

SERVERPROPERTY('Edition') AS Edition,

SERVERPROPERTY('ProductVersion') AS ProductVersion,

SERVERPROPERTY('ProductLevel') AS ProductLevel,

SERVERPROPERTY('LicenseType') AS LicenseType,

SERVERPROPERTY('NumLicenses') AS NumLicenses;



接着输入“SELECT @@VERSION;”查看详细版本信息，结果示例如下：



达梦

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入以下命令，查看版本和授权信息：

SELECT * FROM V$VERSION;

SELECT * FROM V$LICENSE;

SELECT * FROM V$OPTION;



结果示例如下：



接着，输入“systemctl status DmServiceDMSERVER”查看达梦服务状态，结果示例如下：



最后，输入“cat /home/dmdba/dmdbms/bin/dm.key”查看达梦安装目录授权文件，结果示例如下：



Redis

使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“redis-cli”连接到服务器。连接后，使用文本编辑器打开，输入命令“127.0.0.1:6379> INFO SERVER”查看版本信息，查看完毕后输入“127.0.0.1:6379> QUIT”退出。



接着，输入“cat /usr/local/redis/COPYING”查看Redis许可证文件，结果示例如下：



1.27.3 中间件

Nginx

核查Nginx是否具备完备的售后技术支持与服务。若使用开源版Nginx，由社区免费维护，应核查其官方发布页面及安全公告，确认当前版本处于官方维护周期内，不存在已知未修复的高危漏洞；若使用Nginx Plus商业版本，应在安装目录下核查其许可证文件（nginx-repo.crt、nginx-repo.key）及有效的商业技术支持合同。

Tomcat

核查Tomcat是否具备完备的售后技术支持与服务。Apache Tomcat为Apache软件基金会维护的开源社区软件，不提供官方商业售后合同，应核查是否已与第三方厂商签订商业技术支持服务合同（如有）；无论是否签订合同，均应核查当前使用版本是否在Apache官方安全公告页面所列的受支持版本范围内，是否存在已知未修复的安全漏洞。

1.27.4办公软件

Office

任务栏搜索框输入“cmd”或“命令提示符”，右键搜索结果，选择“以管理员身份运行”，输入命令“slmgr /dli”查看Office的激活状态及许可证类型；同时核查采购合同、正版授权证书或电子发货单等资料，确认所使用的Office为正版授权产品。

WPS

打开WPS Office，点击左上角“首页”图标进入“帐号”页面，查看登录账号及会员/授权状态；同时核查采购合同、正版授权证书或序列号注册凭证等资料，确认所使用的WPS为正版授权产品。

## 2 用户安全

## 2.1 服务器和用户计算机应设置登录口令

判定标准：服务器与用户计算机所有本地账户均已设置登录口令， Administrator 等内置账户口令非空且非默认值，来宾（Guest）账户已禁用；存在无口令账户、默认口令或空口令即不合规。

### 2.1.1服务器登录口令核查

#### 2.1.1.1Windows7/WindowXP

方法一：通过图形界面查看，开始-设置-账户-登陆选项-密码，如果此处显示“添加”按钮，则表示未设置密码。如果显示“更改”按钮，则表示已设置。按照提示输入新密码。





方法二：开始菜单搜索"PowerShell"，右键点击以管理员身份运行。在命令行界面输入命令“net users”，查看所有用户，输入“wmic useraccount where name='<用户名>' get name, PasswordRequired”命令查询特定用户，如果 PasswordRequired 返回 TRUE，表示需要密码。如果返回 FALSE，则表示该账户可以无密码登录。如需修改密码，命令行输入“Set-LocalUser -Name "用户名" -Password (ConvertTo-SecureString "新密码" -AsPlainText -Force)”。





命令行输入“net user 用户名”逐个查看账户详情，核对“密码上次设置时间”“密码可更改时间”“密码到期时间”与口令策略是否一致；输入“net accounts”查看本机口令策略（最小长度、最长使用期限、锁定阈值）。

#### 2.1.1.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“cat /etc/passwd”查看当前系统用户列表（需要登录口令），对于想要设置或修改口令的用户，执行命令“passwd username”，输入新口令并确认。



命令行输入“sudo passwd -S -a”查看所有账户的口令状态，第二列应为 P（已设置口令），出现 L（锁定）以外的 NP（无口令）即表示存在未设口令的账户；输入“cat /etc/shadow | awk -F: '($2==""){print $1}'”可直接筛出空口令账户。

### 2.1.2用户计算机登录口令核查

按照上述服务器登录口令核查，用户根据自己的操作系统选择具体核查方式。

## 2.2 用户计算机应根据需要安装补丁程序

判定标准：用户计算机补丁版本与终端管理平台要求一致，无长期未安装的重要或高危补丁；离线环境应有统一离线补丁导入与登记记录。

### 2.2.1Windows7/WindowXP

方法一：通过图形界面查看，开始-Windows更新设置-Windows更新，点击更新历史记录。查看历史更新。



点击检查更新，查看可更新的补丁。



方法二：使用Win+R打开控制框，输入cmd回车，命令行输入“systeminfo | findstr /B /C:"[补丁程序]" /C:"[更新]"”检查更新情况，或者输入：“systeminfo”查看更详细信息。



命令行输入“wmic qfe list brief /format:table”查看已安装补丁。



### 2.2.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt update”检查系统是否有可用的补丁和更新。



输入命令“dpkg --get-selections | grep -v deinstall”查看已安装的补丁和更新。



若系统有可用的更新，可以通过以下命令安装更新“sudo apt upgrade”。

## 2.3 用户应设置用户应用口令，通过认证后使用信息服务

判定标准：账户均通过口令认证后才能使用信息服务，无空口令账户，未启用自动登录；绕过口令直接使用信息服务即不合规。

### 2.3.1Windows7/WindowXP

使用win+R打开输入框，输入cmd，打开命令行，输入“net user”批量查看用户列表。



再输入“net user 用户名”逐个查看：



检查系统是否强制用户设置密码，使用win+R打开输入框，输入secpol.msc打开本地安全策略，检查以下内容。



检查是否存在自动登录，使用win+R打开输入框，输入regedit打开注册表编辑器，查看以下路径“HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon”。检查以下字段。



如果 AutoAdminLogon = 1 → 表示系统启动后可自动登录，无需口令 → 违反要求。



检查用户登录审计（验证是否有人绕过认证），在windows搜索框中，输入“事件查看器”。事件查看器 → Windows 日志 → 安全。



核查：是否存在未认证/绕过密码的登录（类型 3、4、5 异常使用），是否存在大量 4625（密码猜测攻击）。



### 2.3.2中标麒麟/银河麒麟

麒麟系统安装时配置的初始口令在安装完成后，配置自动生效，若安装后需新增用户口令或修改已有口令，则进行以下操作：

在桌面鼠标右键-打开终端，普通用户修改自身口令在命令行输入命令“passwd”，按提示依次输入当前口令和新口令，两次确认新口令后完成修改。



root 管理员修改其他用户口令，输入“sudo passwd 目标用户名”，无需验证原口令，直接输入并确认新口令即可。



## 2.4 用户计算机应具有互不相同的用户名和口令

判定标准：各用户计算机使用互不相同且与他人不共享的用户名和口令，无共用账户、通用账户；两人以上使用同一组凭据即不合规。

### 2.4.1Windows7/WindowXP

核查用户名是否互不相同，使用Win+R打开输入框，输入lusrmgr.msc查看用户列表。查看用户列表，确保每个账号都独立、不相同。检查是否存在重复名称，或变相重复，例如：admin / administrator以及user / user1 / user2（要核实是否多人共用）。

使用Win+R打开输入框，输入“cmd”打开命令行输入“net user”检查输出中是否出现重复或变形共享账号。



Windows无法直接查看用户密码，因此审计采用间接方式核查口令唯一性。使用“net user 用户名”查看以下内容。





如果出现：“密码上次设置从未设置”“密码不需要”说明是空密码，违反要求。

### 2.4.2中标麒麟/银河麒麟

核查用户名互不相同在桌面鼠标右键-打开终端，在命令行输入命令“cat /etc/passwd”查看所有用户，随后输入命令“sudo awk -F: '{print $1}' /etc/shadow”单独提取所有用户的用户名，更直观地排查重名问题。





若发现重名用户，使用“userdel -r 重复用户名”命令删除冗余账户（加-r可同时删除用户家目录），或用“usermod -l 新用户名 旧用户名”命令修改重复用户名，确保所有用户标识唯一。

### 2.4.3核查口令互不相同

输入“cat /etc/shadow”命令，查看文件中第二个字段，若两个用户该字段内容完全一致，说明口令相同；对已存在相同口令的用户，使用“sudo passwd 用户名”命令强制重置其口令。

## 2.5 用户计算机应关闭冗余系统服务和端口

判定标准：用户计算机仅保留业务必需的服务与端口，多余系统服务已停止并禁用，无闲置监听端口；保留项均有明确用途。

### 2.5.1Windows7/WindowXP

使用Win+R打开控制行，输入services.msc,查看“状态”列中正在运行的服务，找出不必要服务。可以检查以下启动类型：自动，重点检查，很多都是系统默认但不一定需要；手动，可能不常用；禁用，正常。



常见可关闭的冗余服务（按等保/企业安全标准）。



命令行输入“netstat -ano”检查端口。



按照以下判断依据判断端口是否需要关闭（按等保/企业标准）。只保留业务必须的端口，屏蔽所有其他端口。



### 2.5.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，查看所有服务的自启动状态，在命令行输入命令“sudo systemctl list-unit-files --type=service”筛选出启用的服务。



若需要查看单个服务是否启用，输入命令“sudo systemctl is-enabled <服务名>”，查看单个服务当前运行状态，输入命令“sudo systemctl is-active <服务名>”。

若有冗余服务，可以临时关闭正在运行的冗余服务“sudo systemctl stop <服务名>”，该操作重启后失效；禁止服务开机自启“sudo systemctl disable <服务名>”；若需同时完成停止和禁用“sudo systemctl disable --now <服务名>”。

命令行输入“sudo ss -tuln”，查看所有监听端口及对应进程。停止端口对应服务，若端口由某服务占用，执行sudo systemctl stop <服务名>，再用sudo systemctl disable <服务名>禁止自启，端口会随服务停止而关闭。



## 2.6 用户计算机应具备阻断和告警非法连接互联网的能力

判定标准：已部署终端管控或防非法外联软件，对违规外联能够告警并阻断，外联日志留存完整；无任何外联阻断与告警能力即不合规。

### 2.6.1Windows7/WindowXP

使用Win+R打开控制行，输入cmd打开命令行，输入“netsh advfirewall show allprofiles”检查防火墙是否启用并运行。检查字段State=ON（必须开启），OutboundBlockAction=Block（是否默认拒绝出战）。





查看出站规则，使用Win+R打开控制行，输入“wf.msc”查看“出站标准”。





核查是否具备外联审计和告警能力，使用Win+R打开控制行输入“gpedit.msc”，进入：计算机配置→Windows设置→安全设置→高级审核策略。启用:对象访问→筛选平台连接，对象访问→筛选平台数据包丢弃。

查看日志，使用Win+R打开控制行输入“eventvwr.msc”，位置“应用和服务日志→Microsoft→Windows→Fault-Tolerant-Heap→ Operational”。如果能看到类似：Filter dropped packet  (封堵非法连接)，Filtering Platform Connection Rejected (拒绝外联)，说明外联阻断已生效并有日志。



核查是否有非法外联告警能力，打开windows搜索框，输入“任务计划程序”，创建任务→触发器，选择时间来源：Microsoft-Windows-FilteringPlatform/Operational事件 ID：5157（连接被阻止）。



### 2.6.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，先开启防火墙，在命令行输入命令“sudo kylin-firewall -s on”，再输入“sudo kylin-firewall -g”，验证防火墙处于开启状态。



然后添加阻断规则，例如拒绝外部访问 23 端口，命令为“sudo kylin-firewall -A -n Deny-Telnet -p tcp -R 23 -a deny”。最后查看防火墙规则执行情况，命令为“sudo kylin-firewall -S”，可列出所有规则的状态及动作，确认阻断规则是否正常启用。



## 2.7 用户计算机应具有文件保护功能

判定标准：用户文件已通过 NTFS 权限、加密文件系统（EFS）或文档权限设置实现访问保护，非授权用户无法读取他人受保护文件；任意用户可自由访问他人文件即不合规。

### 2.7.1Windows7/WindowXP

检查分区格式是否为 NTFS（必须），打开“此电脑”，右键C盘或其他盘，在“常规”页查看文件系统，若显示NTFS就是合规的。



检查系统关键目录权限是否受保护，打开 C:\Windows\System32 → 右键 → “属性” → “安全”，确认只有 TrustedInstaller、SYSTEM、Administrators 具有写入权限，普通用户（Users）无写权限。判定标准：普通用户不可修改或删除系统文件。





检查系统文件保护功能是否正常（SFC），搜索框搜索cmd，以管理员身份运行命令提示符，执行：sfc/scannow。判定标准：扫描可正常执行，文件保护未被关闭。



检查 Windows Defender 受控文件夹访问（防止勒索/恶意修改）
打开：设置 → Windows 安全中心 → 病毒和威胁防护 → 勒索软件防护，确认“受控文件夹访问”已开启。判定标准：受控文件夹访问已启用。



### 2.7.2中标麒麟/银河麒麟

方法1：

在桌面鼠标右键-打开终端，在命令行输入命令“md5sum /bin/bash”执行md5sum 目标文件，记录哈希值作为基准，定期校验完整性，再次执行md5sum 目标文件，对比哈希值是否一致，若不一致，说明文件被篡改。



方法2：

配置 auditd 监控文件变更，输入命令“sudo apt-get install auditd”安装auditd服务。输入命令“sudo auditctl -w /etc/passwd -p wa”添加监控规则。



追溯文件变更的时间、用户和操作，查看审计日志，输入命令“sudo ausearch -f /etc/passwd”。



## 2.8 应采取终端管控措施、具有统一配置、安全加固、网络访问控制、外设接口管控、软件进程管控、无线模块禁用、防止IP地址和MAC地址非授权改动等功能

判定标准：已部署终端管控系统并落实统一配置、安全加固、网络访问控制、外设接口管控、软件进程管控、无线模块禁用及 IP/MAC 防篡改等管控项；未部署终端管控或管控项长期未启用即不合规。

### 2.8.1Windows7/WindowXP

统一配置 / 安全加固核查，使用win+R打开本地组策略编辑器输入gpedit.msc。查看以下策略是否已配置：账户策略（密码策略 / 锁定策略）路径：计算机配置 → Windows 设置 → 安全设置 → 账户策略；安全选项路径：安全设置 → 本地策略 → 安全选项；审计策略路径：安全设置 → 本地策略 → 审计策略。

网络访问控制核查（禁止未授权修改网络位置）核查方式：打开 本地组策略：gpedit.msc。路径：计算机配置 → Windows 设置 → 安全设置 → 网络列表管理策略。核查：不允许普通用户修改网络属性

网络位置配置为“受管理”。普通用户不可随意修改网络参数。

### 2.8.2中标麒麟/银河麒麟

打开麒麟系统的安全中心，进入安全体检页面，点击 “开始体检”，系统会按预置模板扫描安全风险项。扫描完成后点击 “一键修复”，即可完成多项加固操作，加固后可在安全报告中查看结果。







无线模块禁用，命令行禁用 Wi-Fi。在桌面鼠标右键-打开终端，在命令行输入命令“nmcli device status”，查看无线网卡名称，再执行“sudo nmcli device set wlan0 managed no”禁用无线网卡；执行“sudo nmcli connection delete 无线连接名”删除已保存的 Wi-Fi 连接。







## 2.9 用户计算机应禁止安装与工作无关的软件

判定标准：用户计算机仅安装业务必需软件，未发现游戏、娱乐、未经批准的工具等与工作无关软件；安装无关软件且无审批豁免记录即不合规。

### 2.9.1Windows7/WindowXP

使用win+R打开运行窗口，输入“appwiz.cpl”，打开“程序和功能（已安装软件列表）”。检查是否存在非业务软件。





核查软件安装来源，在“程序和功能”中点击任意软件 → 查看软件 “发行者（Publisher）” 字段是否属于以下范围：单位允许的软件供应商、Microsoft 官方组件、本单位内部开发软件、已经批准的业务软件厂家。若发现未知来源或不可信厂商发布的软件，应判定存在风险。





### 2.9.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令查看最近安装的软件。

grep " installed " /var/log/dpkg.log | tail -10



示例如下。



识别桌面游戏应用，输入以下命令。

ls /usr/share/applications/*.desktop 2>/dev/null |xargs grep -1 "Game\|game" 2>/dev/null | head -10



## 2.10 用户计算机USB接口应禁止私自连接对拷线和手机、媒体播放设备等个人移动电子设备

判定标准：USB 接口已通过管控策略禁止私自连接对拷线、手机、媒体播放设备等个人电子设备，仅允许经登记备案的专用设备；管控未启用且存在私自连接记录即不合规。

### 2.10.1Windows7/WindowXP

查看 Windows 的 USB 存储设备策略是否启用，使用win+R打开“运行”，输入“gpedit.msc”。依次打开计算机配置->管理模板->系统->可移动存储访问。

检查以下策略。



若上述策略已启用 → 视为合规。若策略未启用、未配置 → 视为不合规。

检查系统是否识别并启用了手机/WPD 设备驱动，使用win+R打开输入框，输入“devmgmt.msc”。检查是否存在以下类别。







检查事件查看器日志是否存在 USB 设备插拔记录，打开win+R，输入“eventvwr.msc”,进入windows日志，点击系统。



筛选以下事件ID（USB插拔高频码）。





### 2.10.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“lsusb”查看所有USB设备。



在命令行输入以下命令查看USB历史连接记录。

sudo dmesg | grep -i "usb" | tail -20





## 2.11 用户计算机登录应使用基于专用物理部件或生物特征的多因素身份认证方式，应设置超时锁屏，屏幕保护等待时间不超过5min,服务器应设置登录口令，口令超度不得少于10个字符，字符应采用字母大小写、数字及特殊字符混合编制，更换周期不超过30d

### 2.11.1Windows7/WindowXP

判定标准：已启用屏幕保护程序，等待时间不超过5分钟（ScreenSaveTimeOut不大于300），恢复时要求输入密码（ScreenSaverIsSecure=1）；已部署UKey、智能卡或指纹等生物特征的多因素身份认证；登录口令不少于10个字符且包含大小写字母、数字及特殊字符，更换周期不超过30天。

核查登录口令策略。在命令行输入“net accounts”查看最小密码长度、密码最长使用期限等，确认服务器口令不少于10个字符，更换周期不超过30天。

核查多因素身份认证。运行devmgmt.msc打开设备管理器，查看是否安装智能卡读卡器（Smart Card Reader）、UKey设备或生物识别设备（指纹等）；或通过控制面板查看“生物特征设备”“智能卡”相关项，确认已部署基于专用物理部件或生物特征的认证方式。

核查屏幕保护密码与等待时间也可通过组策略：运行gpedit.msc，定位到“用户配置 → 管理模板 → 控制面板 → 个性化”，检查“启用屏幕保护程序”“密码保护屏幕保护程序”“屏幕保护程序超时”三项策略。

reg query "HKCU\Control Panel\Desktop" /v ScreenSaverIsSecure

reg query "HKCU\Control Panel\Desktop" /v ScreenSaveTimeOut

reg query "HKCU\Control Panel\Desktop" /v ScreenSaveActive

也可在命令行直接输入以下命令查询：

核查屏幕保护与锁屏策略。使用win+R组合键打开运行框，输入regedit，定位到“HKEY_CURRENT_USER\Control Panel\Desktop”，核查以下键值：ScreenSaveActive（屏保启用，1为启用）、ScreenSaveTimeOut（屏保等待秒数，应不大于300即5分钟）、ScreenSaverIsSecure（恢复时是否要求密码，1为要求）。

### 2.11.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查PAM认证配置。

sudo grep -E "auth.*required" /etc/pam.d/common-auth | head-10



示例如下。



命令行输入下面命令检查生物特征支持。

1s /usr/lib/x86_64-linux-gnu/security/pam_* | grep -E "(fprint bio|finger)" | head -5



接着，输入命令“gsettings get org.gnome.desktop.session idle-delay”检查锁屏配置。



然后，输入以下命令检查PAM口令策略。

sudo grep -E "pam_pwquality|pam_cracklib|minlen|difok" /etc/pam.d/common-password



示例如下。



## 2.12 用户计算机应物理拆除Wi-Fi、红外、蓝牙等无线模块，确需使用Wi-Fi的应严格采用JY密码等措施保护

判定标准：无使用需求的 Wi-Fi、红外、蓝牙等无线模块已物理拆除或等效禁用，确需 Wi-Fi 的采用相应密码保护措施；无线模块未拆除且无保护措施即不合规。

### 2.12.1Windows7/WindowXP

使用win+R打开输入框，输入“devmgmt.msc”，双击“网络适配器（Network adapters）”。查看设备管理器里面是否有红外设备。出现即为存在。



查看是否存在以下设备：Wireless Adapter / 802.11 / WLAN、Intel Dual Band Wireless-AC/AX …、Realtek Wireless LAN、Qualcomm WLAN、Microsoft Wi-Fi Direct Virtual Adapter。合规：未出现任何无线网卡名称 → 认为 Wi-Fi 已物理拆除 或 BIOS 禁用。不合规：出现任意 WLAN 无线网卡 → 无线模块存在 出现 Wi-Fi Direct 适配器 → 系统级无线功能未关闭。

如存在 Wi-Fi，核查是否采用强加密，查看当前wifi的加密方式，win+R打开输入框，输入“netsh wlan show interfaces”。





### 2.12.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行依次输入以下命令检查所有无线硬件模块。

echo"1.Wi-Fi模块检查:"

lspci | grep -i "network\|wireless"

lsusb | grep -i "wireless\|wlan"

echo"2.蓝牙模块检查:"

lsusb | grep -i "bluetooth"

lsmod | grep -i "bluetooth"

echo"3.红外模块检查:"

Lsusb | grep -i "infrared"

Lsmod | grep -i "ir\linfrared"





接着输入以下命令检查无线服务状态。

echo"1.网络管理器状态:"

systemctl status NetworkManager --no-pager | head-10

echo "2.Wi-Fi服务状态:"

systemctl status wpa_supplicant --no-pager 2>/dev/null | head -5 || echo "wpa_supplicant未运行"

echo"3.蓝牙服务状态:"

systemctl status bluetooth --no-pager |head -10

echo"4.无线网络接口:"

ip link show | grep -E "wl | wlan | wifi"



示例如下。







最后输入下面命令配置严格的JY级别密码保护。

sudo sh -c 'cat > /etc/wpa_supplicant/wpa_supplicant.conf <<EOF

ctrl_interface=/var/run/wpa_supplicant

ctrl_interface_group=root

update_config=1

network={

ssid="受保护网络"

scan_ssid=1

proto=RSN

key_mgmt=WPA-EAP

pairwise=CCMP

group=CCMP

eap=PEAP

identity="授权用户"

password="符合JY要求的强密码"

phase1="peapver=0"

phase2="MSCHAPV2"

ieee80211w=2

ocv=1

}

EOF'



## 2.13 用户计算机应采取非法外联阻断、文件输出管控等措施

判定标准：已采取非法外联阻断与文件输出管控措施，外设输出、打印、刻录等文件外发行为受控并留有日志；文件可随意输出且无任何管控记录即不合规。

### 2.13.1Windows7/WindowXP

非法外联阻断核查，确认windows计算机已采取技术措施，阻断未经授权的外部网络连接（如公网访问、未知 IP/端口访问等）。使用win+R打开输入框，输入cmd，在命令行输入“ipconfig/all”。核查本机是否存在公有地址段（例如 1.x～223.x 但排除私网段）、网关是否指向内网地址、DNS 是否为企业自建DNS。



核查 Windows 防火墙是否启用 + 入/出站策略，打开控制面板 → 系统和安全 → Windows Defender 防火墙 → 高级设置。核查以下内容：防火墙“启用”状态（专用网络/公用网络均应开启）；出站规则是否限制：仅允许访问企业内部网段、禁止访问 80/443 公网、禁止访问未知 IP；是否存在“全部允许”的策略（应禁止）。



### 2.13.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo netstat -tunap”检查所有网络连接。



接着，依次输入如下命令检查文件输出通道。

echo"1.USB设备状态:'

1susb

1sblk

echo"2.可移动设备挂载:"

mount | grep -E "(usb|sd[b-z]|mmc)"

echo"3.网络文件共享:”

sudo ss -tup | grep -E "(nfs|samba|ftp)"

echo"4.云存储应用:"

ps aux | grep -E "(dropbox|onedrive|google|baidu)" | grep -v grep

echo"5.邮件客户端:"

ps aux| grep -E "(thunderbird|outlook|evolution)" | grep -v grep



示例如下。



## 2.14 应具备用户行为审计能力；审计日志应至少保留xxd

判定标准：已启用用户行为审计，能够记录登录、文件访问、外设使用、网络访问等行为，审计日志留存天数满足制度要求且不可被随意删除或篡改；审计未开启或日志留存期不足即不合规。

### 2.14.1 Windows7/WindowXP

使用win+R，输入gpmc.msc（域控环境）或 gpedit.msc（本机）。定位到计算机配置 → Windows 设置 → 安全设置 → 本地策略 → 审核策略。检查以下核查项。



核查审计日志记录是否正常，使用win+R打开输入框，输入“eventvwr.msc”。核查以下项目。



核查审计日志保留时长设置，点击事件查看器 → Windows 日志 → 安全 → 右键 → 属性。核查点如下。



### 2.14.2中标麒麟/银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo systemctl status auditd --no-pager”检查审计服务是否开启。接着，配置审计日志的保存天数，输入如下命令。

RETENTION DAYS=90

sudo cp /etc/audit/auditd.conf /etc/audit/auditd.conf.backup.$(date +%Y%m%d)

sudo sh -c "cat > /etc/audit/auditd.conf << EOF

log_file = /var/log/audit/audit.log

log_format = RAW

log_group = root

priority_boost = 4

flush = INCREMENTAL_ASYNC

freq = 50

max_log_file = 100

num_logs = ${RETENTION_DAYS}

disp_qos = lossy

dispatcher = /sbin/audispd

name_format = NONE

max_log_file_action = ROTATE

space_left = 512

space_left_action = SYSLOG

action_mail_acct = root

admin_space_left = 256

admin_space_left_action = SUSPEND

disk_full_action = SUSPEND

disk_error_action = SUSPEND

use_libwrap = yes

tcp_listen_queue = 5

tcp_max_per_addr = 1

tcp_client_max_idle = 0

enable_krb5 = no

krb5_principal = auditd

EOF"





## 2.15 检查安全策略配置情况和设置功能

### 2.15.1Windows7/WindowXP

判定标准：密码策略、账户锁定策略、审核策略等安全策略均已按要求配置并启用，密码复杂度、最小长度、更换周期、登录失败锁定等符合规定。

也可通过组策略核查，运行gpedit.msc，定位到“计算机配置 → Windows 设置 → 安全设置 → 账户策略/本地策略”核查上述各项。

核查审核策略。在secpol.msc中定位到“本地策略 → 审核策略”，或在命令行输入“auditpol /get /category:*”，确认审核登录事件、账户管理等策略已启用。

核查账户锁定策略。在secpol.msc中定位到“账户策略 → 账户锁定策略”，查看账户锁定阈值、锁定时间、重置计数时间，或在命令行输入“net accounts”查看锁定阈值，确认已设置登录失败锁定。

核查密码策略。在命令行输入“net accounts”查看密码最短使用期限、密码最长使用期限、最小密码长度、密码历史等，确认已按要求配置（如最小密码长度不小于10、启用密码复杂度、密码最长使用期限不超过30天）。

核查本地安全策略。使用win+R组合键打开运行框，输入secpol.msc打开本地安全策略，依次核查账户策略（密码策略、账户锁定策略）、本地策略（审核策略、安全选项、用户权限分配）。

### 2.15.2中标麒麟/银河麒麟

判定标准：密码复杂度、最小长度与更换周期已在PAM和login.defs中配置；审计服务已运行且审计规则已加载；防火墙已启用并按最小化原则开放端口；登录失败锁定等安全策略均已按要求配置。

核查账户锁定与登录失败处理，输入“grep -E "pam_tally2|pam_faillock|deny|unlock_time" /etc/pam.d/system-auth /etc/pam.d/common-auth /etc/security/faillock.conf 2>/dev/null”确认已配置登录失败锁定。

核查防火墙，输入“systemctl status firewalld”或“ufw status verbose”查看防火墙运行状态与规则，输入“firewall-cmd --list-all”或“ufw status”确认已按最小化原则配置开放端口。

核查审计规则，输入“sudo systemctl status auditd”确认审计服务运行，输入“auditctl -l”查看已加载的审计规则，确认对关键操作、登录事件等已配置审计。

核查PAM密码策略，输入“grep -E "pam_pwquality|pam_cracklib|minlen|minclass|difok|retry" /etc/pam.d/system-auth /etc/pam.d/common-password”，确认已启用密码复杂度校验（最小长度、字符类别等）。

在桌面鼠标右键-打开终端，命令行输入“cat /etc/login.defs”核查密码有效期相关参数，重点查看PASS_MAX_DAYS（密码最长使用天数）、PASS_MIN_DAYS（密码最短使用天数）、PASS_MIN_LEN（最小密码长度）、PASS_WARN_AGE（过期提醒天数）。

## 2.16 检查被试装备中操作系统、数据库以及应用软件等是否完成补丁修复和升级到最新版本

判定标准：被试装备中操作系统、数据库及应用软件补丁均已完成修复并升级到官方最新版本，或经评估确认无对应高危漏洞；存在已知高危漏洞未修复即不合规。

### 2.16.1核查操作系统安装补丁情况

#### 2.16.1.1Windows操作系统 (Win7, WinXP)

补丁修复和升级到最新版本

方法1：针对本地登录，使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“wmic qfe list brief”，查看结果。



方法2：开始-控制面板-系统和安全-Windows Update-查看更新历史记录。此界面会显示补丁名称、编号、安装信息等。



点击检查更新，查看可更新的补丁。



#### 2.16.1.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“cat /etc/os-release”查看操作系统的具体版本和型号，输入“uname -r”查看当前运行的内核版本。



接着，在命令行中输入命令“apt list --installed”查看所有已安装的软件包列表及其版本，示例如下。



依次输入以下命令，刷新软件列表并升级所有包。

sudo apt update

sudo apt upgrade



示例如下。





### 2.16.2核查数据库补丁修复和升级到最新版本

#### 2.16.2.1 MySQL

Windows操作系统下的核查方法

首先配置mysql的环境变量，创建系统变量MYSQL_HOME,变量值为mysql安装目录。在Path系统变量中添加%MYSQL_HOME%\bin。



使用win+R组合键打开运行框，输入cmd，在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行“ SELECT VERSION();” 查看当前完整版本号。执行“SHOW STATUS LIKE 'patch%'; ”，





最后访问MySQL官方公告，在下载页面查找MySQL版本对应的最新补丁和更新。

中标麒麟、银河麒麟操作系统下的核查方法

首先配置mysql的环境变量，在桌面鼠标右键-打开终端，在终端输入“vi ~/.bashrc”或“vi ~/.bash_profile”编辑当前用户的bash配置文件，在文件末尾加入以下内容：

export MYSQL_HOME=/usr/local/mysql

export PATH=$MYSQL_HOME/bin:$PATH

export LD_LIBRARY_PATH=$MYSQL_HOME/lib:$LD_LIBRARY_PATH



添加成功后，在终端输入“source ~/.bashrc”或“. ~/.bash_profile”重新加载配置文件，再输入“env | grep MYSQL”验证配置。



在命令行界面输入命令“mysql -uroot -p”，输入密码登录MySQL数据库，执行“ SELECT VERSION();” 查看当前完整版本号。执行“SHOW STATUS LIKE 'patch%'; ”，





最后访问MySQL官方公告，在下载页面查找MySQL版本对应的最新补丁和更新。

#### 2.16.2.2 SQL Server

Windows操作系统下的核查方法

在搜索框中输入“SSMS”后回车，打开SQL Server Management Studio (SSMS)，在连接服务器对话框中输入服务器名称和认证信息连接到SQLServer实例，连接成功后，右键点击数据库实例，选择新建查询，打开一个新的查询窗口，用以下查询语句查看补丁信息：

SELECT

@@VERSION AS '版本信息',

SERVERPROPERTY('ProductVersion') AS '产品版本',

SERVERPROPERTY('ProductLevel') AS '产品级别',

SERVERPROPERTY('Edition') AS '版本',

SERVERPROPERTY('ResourceVersion')AS '资源版本',

SERVERPROPERTY('ResourceLastUpdateDateTime') AS '资源最后更新'

GO



可查看到以下结果。



最后访问SQLServer官方公告，在下载页面查找SQLServer版本对应的最新补丁和更新。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“ sqlcmd -S localhost -U SA -P 'SA账户密码' ”连接到SQLServer实例，连接成功后，在命令行输入以下命令查看补丁信息：

SELECT

@@VERSION AS '版本信息',

SERVERPROPERTY('ProductVersion') AS '产品版本',

SERVERPROPERTY('ProductLevel') AS '产品级别',

SERVERPROPERTY('Edition') AS '版本',

SERVERPROPERTY('ResourceVersion')AS '资源版本',

SERVERPROPERTY('ResourceLastUpdateDateTime') AS '资源最后更新'

GO



可查看到以下结果。



最后访问SQLServer官方公告，在下载页面查找SQLServer版本对应的最新补丁和更新。

#### 2.16.2.3 达梦

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，输入“su  -用户名”按回车键，输入密码，切换为达梦数据库用户，接着输入“cd  /达梦数据库安装目录下的 bin 目录”进入到 DM 数据库安装目录下的 bin 目录中，然后输入“./disql SYSDBA/SYSDBA@localhost:5236”，连接到数据库（假设使用默认端口5236）。



连接成功后，在disql提示符下输入“select banner from v$version;”查看数据库版本信息。



接着在disql提示符下依次输入以下命令，查询补丁历史记录。

set linesize 200 pagesize 20

col patch_name for a20

col patch_desc for a50

col apply_time for a25

select patch_name, patch_desc, apply_time from v$patch_history order by apply_time desc;



结果示例如下：



最后根据需求更新补丁。

#### 2.16.2.4 Redis

Windows操作系统下的核查方法

打开Redis安装所在目录，在redis-cli.exe程序的当前目录下鼠标右键打开终端，命令行输入 “.\redis-cli -version ”查看版本号。本地执行命令截图如下：



最后访问 Redis官方发布页面，确认当前版本是否为最新稳定版。

中标麒麟、银河麒麟操作系统下的核查方法

在桌面鼠标右键-打开终端，在命令行界面输入命令“redis-cli”全局连接到Redis，在命令行输入 “ redis-cli --version”查看版本号。本地执行命令截图如下：





最后访问 Redis官方发布页面，确认当前版本是否为最新稳定版。

#### 2.16.3应用软件补丁修复和升级到最新版本

浏览器通常在 “关于” 菜单中会触发自动检查和更新。



办公软件，Microsoft Office/Office 365： 在任意 Office 应用中（如 Word）进入 “文件” > “账户” > “更新选项” > “立即更新”。



杀毒软件，手动运行安全软件的更新功能，确保病毒库和程序本身为最新。

第三方应用，检查其他关键的第三方应用程序（如 Adobe Reader, Java, 虚拟化软件等），使用软件内置的“检查更新”功能或访问官方网站下载最新版本安装。

## 3 数据安全

## 3.1 集中存储的涉密数据应采取加密保护措施

### 3.1.1Win7\WinXP

核查是否启用 BitLocker，开始栏的搜索框中输入cmd，以管理员身份打开。命令行输入“manage-bde -status”核查点:Conversion Status : Fully EncryptedPercentage、 Encrypted : 100%、加密方式：AES。



核查文件存储是否加密（windows文件加密），命令行输入“cipher /u /n”。核查点：Encrypted = Yes。



### 3.1.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt-get install cryptsetup”安装 LUKS 工具（若未安装）。



依次执行以下命令，访问加密分区， 提示 "ls: 无法访问 '/mnt/secret_data': 没有那个文件或目录"。



执行“sudo cryptsetup luksOpen /dev/sdb1 crypt_data”（/dev/sdb1为例子），必须输入正确密码才能解锁，否则提示失败。

执行如下命令，查看加密分区信息及挂载状态。

sudo cryptsetup luksDump /dev/sdb1

mount | grep crypt_data









### 3.1.3 通用核查方法（数据库层与应用层）

（1）数据库层核查数据库透明加密（TDE）：SQL Server 执行 SELECT name, is_encrypted FROM sys.databases；Oracle 11g+ 执行 SELECT * FROM V$ENCRYPTED_TABLESPACES 或 DBA_ENCRYPTED_COLUMNS；MySQL 8.0 执行 SHOW VARIABLES LIKE 'default_table_encryption'，或核查 keyring_file 插件是否加载（SHOW PLUGINS）。判据：TDE 已开启且加密密钥独立存储于密钥库，数据不落明文盘。

（2）数据库层核查列级加密：MySQL 查看敏感列是否以密文存储（SELECT 涉密字段，确认无明文）；PostgreSQL 核查是否启用 pgcrypto（CREATE EXTENSION pgcrypto，\d 查看列为 bytea 密文类型）。判据：涉密字段以密文存储，解密密钥独立管理。

（3）应用层核查应用数据加密存储：查看应用配置中是否启用字段级/文件级加密参数。Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "encrypt|crypto|aes|sm4"；麒麟：grep -r "encrypt\|crypto\|aes\|sm4" /opt/*/conf/*.conf。判据：敏感字段在落库或落盘前已完成加密，密钥与应用数据分离管理。

【判定】数据库层（TDE 或列级加密）与应用层加密任一层确认有效且密钥独立受控即满足；仅磁盘加密（BitLocker/LUKS）不能替代数据库/应用层加密，需结合数据库与应用配置人工确认。

## 3.2 用户计算机之间的数据交互、文件传输凳应统一管控和审计

### 3.2.1Win7\WinXP

核查是否启用 Windows 高级审计策略，开始栏的搜索框中输入cmd，以管理员身份打开。命令行输入“auditpol /get /category:*”。





核查文件系统访问审计是否启用，命令行输入“auditpol /get /subcategory:"文件系统"”。如果出现以下情况说明未开启。



命令行输入“auditpol /set /subcategory:"文件系统" /success:enable /failure:enable”。启用文件系统审计（成功 + 失败）。

核查用户计算机间的数据交互（网络），查 Windows 防火墙是否开启（统一管控基础），命令行输入“netsh advfirewall show allprofiles”，核查点为Domain / Private / Public = ON。



### 3.2.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo bash -c 'echo "blacklist usb-storage" >> /etc/modprobe.d/usb-blacklist.conf'”阻断 U 盘拷贝；执行以下命令停止蓝牙服务并禁止开机自启。

sudo systemctl stop bluetooth

sudo systemctl disable bluetooth





执行命令“sudo firewall-cmd --permanent --remove-port=21/tcp”关闭非法网络传输端口，如 FTP 的 21 端口；执行命令“sudo firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="192.168.1.0/24" port port=445 protocol=tcp accept'”允许某网段（192.168.1.0/24）访问某端口（445），接着执行“sudo firewall-cmd --reload”更新。

按照需求输入如下命令查看审计日志：

sudo tail -f /var/log/samba/log.smbd

sudo grep "user1" /var/log/samba/log.smbd





若部署了麒麟天御终端安全管理平台，可集中收集所有终端的传输日志：登录天御平台，进入「日志中心」→「文件传输日志」，查看所有终端的 Samba/SFTP 传输记录。可按时间范围、终端 IP、操作用户、文件名称等检索，导出审计报告。当出现超大文件传输、非授权 IP 访问等异常行为时，触发平台告警。

### 3.2.3 通用核查方法（应用层与网络层）

（1）应用层核查文件传输/交互通道统一管控：核查是否部署统一的文件传输/交换平台（FTP/SFTP/网盘/摆渡系统），禁止点对点直传。查看应用传输配置与平台日志：grep -r "transfer\|upload\|sftp\|ftp" /opt/*/conf/*.conf，并登录平台查看「文件传输日志」。判据：数据交互均经统一平台、有日志、有审批或白名单控制。

（2）应用层核查交互通道权限与授权：核查传输通道是否按用户、文件类型、传输方向授权，是否存在匿名上传或越权下载。判据：非授权交互被阻断，交互范围与业务需求一致。

（3）网络层核查传输审计：核查边界/安全设备是否对文件传输行为留痕（源/目的 IP、用户、文件名、时间、方向）。中标麒麟/银河麒麟：ss -tlnp | grep -E ':21|:22|:445' 查看传输服务，tail -f /var/log/samba/log.smbd 查看传输审计。判据：传输行为在网络层可追溯、可审计。

【判定】文件传输/交互经统一平台管控，且传输行为在应用层与网络层均留有审计日志即满足；存在脱离管控的直传通道或传输无审计即不合规。

## 3.3 涉密存储载体在降密级使用前或重大JS演训活动结束后，应采取数据写覆盖方法及时清除数据

### 3.3.1Win7\WinXP

核查是否具备“写覆盖清除”能力，开始栏搜索框输入cmd，以管理员身份打开，命令行输入“where cipher”。有 cipher = 具备写覆盖清除能力（Windows 官方工具）。



核查是否执行过“写覆盖清除”，命令行输入“cipher /w:D:”。



若是U盘/移动介质，接入U盘在E:命令行输入“cipher /w:E:”。

核查是否有执行痕迹，核查是否有执行痕迹，命令行输入“doskey /history”，若出现 cipher /w 相关记录 = 有执行痕迹。



核查存储载体是否“可重复使用”，核查磁盘当前状态（未锁定 / 未只读）命令行依次输入“diskpart、list disk、exit”。



### 3.3.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt-get install coreutils”，安装shred工具。



然后输入命令“sudo shred -n 3 -z -v /dev/sdb”对存储载体（如/dev/sdb）执行 3 轮随机数据覆盖（可增加至 7 轮以提升安全性）。



### 3.3.3 通用核查方法（专业擦除工具层）

（1）专业擦除工具核查多次覆写能力：核查是否部署 DBAN、Blancco、Eraser 或国产专业擦除工具。Win7：where dban、where eraser；WinXP：dir /s /b dban*.exe（XP 无 where 命令）；麒麟：which dban nwipe wipe。判据：具备按标准算法多轮覆写能力（如 DoD 5220.22-M 7 轮、Gutmann 35 轮）。

（2）专业擦除工具核查 SSD 安全擦除：核查是否支持 ATA Secure Erase/NVMe Format 命令。麒麟：先执行 hdparm --user-master u --security-set-pass NULL /dev/sdX 设置口令，再执行 hdparm --user-master u --security-erase NULL /dev/sdX 完成安全擦除。判据：SSD/U 盘采用 ATA 安全擦除或物理粉碎，不依赖普通覆写（闪存磨损均衡会残留数据，blkdiscard 仅触发 TRIM 不保证安全擦除）。

（3）执行痕迹核查：核查是否留存擦除报告/日志（擦除时间、载体序列号、擦除算法与轮数）。判据：有擦除凭证可证明数据已按标准清除。

【判定】使用专业工具按标准算法多次覆写，SSD/U 盘采用安全擦除或物理粉碎，且留存擦除报告即满足；仅依赖普通删除或格式化、无擦除凭证即不合规。

## 3.4 对确定销毁的涉密载体，应采取消磁、粉碎、溶解、化浆和熔化等方法进行销毁

为确保对确定销毁的涉密载体的彻底性与不可恢复性，必须依据国家保密行政管理部门的相关技术标准和操作规程，采用物理形态彻底改变或信息信号完全消除的销毁方法。

具体而言，应根据载体的物理特性与信息存储原理，分类实施消磁、粉碎、溶解、化浆或熔化等专业化处理流程。对于磁性存储介质（如硬盘、磁带），应采用强磁场消磁设备使其剩磁归零，并通过验证程序确保存储数据无法通过磁力显微镜等高级手段重建；对于半导体存储介质（如固态硬盘、U盘），除消磁外，仍需辅以物理粉碎以确保存储芯片的完全碎裂。对于光介质与纸张载体，应优先选用粉碎机进行精细化破碎，其粉碎颗粒尺寸须严格控制在国家安全保密标准规定的极限值以下。对于纸质文档的批量销毁，可采用化浆工艺，通过水力碎浆与化学药剂的共同作用，使纤维结构彻底瓦解并失去字迹还原能力；对于部分特殊材质的涉密载体，则需通过溶解或熔化等化学与热力学方法，破坏其分子结构或物理形态，确保信息存储层发生不可逆变性。所有销毁过程必须实行双人操作、全程监控、实时记录，形成包含销毁时间、地点、载体类别、数量、操作人及监督人签字的销毁档案，并留存影像资料以备追溯审计，确保涉密载体从产生到销毁的全生命周期闭环管理，从根本上阻断信息泄露的技术途径。

### 3.4.1 通用核查方法（物理销毁）

（1）消磁销毁核查（磁性介质）：核查硬盘、磁带等磁性介质是否使用强磁场消磁设备消磁使剩磁归零。核查消磁设备型号及磁场强度是否满足保密技术标准（如不低于对应国标/保密标准规定值），并抽查消磁后载体经检测无法恢复数据。判据：有消磁设备使用记录，消磁后数据不可通过磁力显微镜等高级手段重建。

（2）粉碎销毁核查（半导体介质与光介质/纸张）：核查 SSD、U 盘等半导体介质是否采用物理粉碎使存储芯片完全碎裂；光盘与纸质载体是否使用粉碎机粉碎至标准颗粒尺寸以下。判据：粉碎颗粒尺寸满足保密标准极限值，存储芯片碎裂不可恢复。

（3）熔毁/溶解/化浆核查（特殊材质）：核查特殊材质载体是否采用熔毁（热力学破坏分子结构）、溶解（化学破坏存储层）或化浆（纸质批量销毁）工艺。判据：存储层发生不可逆变性，字迹与数据不可还原。

（4）销毁记录凭证核查：核查销毁档案是否包含销毁时间、地点、载体类别、数量、操作人与监督人签字，是否实行双人操作、全程监控、实时记录并留存影像资料。判据：档案要素完整、双人签认、影像可追溯，形成载体从产生到销毁的全生命周期闭环。

【判定】按载体类型采用对应物理销毁方法（磁性介质消磁、半导体/光介质/纸张粉碎、特殊材质熔毁/溶解/化浆），且留存双人签认的销毁档案与影像即满足；未按类型销毁、无销毁记录凭证、无监督签认即不合规。

## 3.5 网络、系统、应用和用户行为等日志应采取读写控制、加密、变换、完整性校验等保护措施，TM级数据存储1应采取加密保护措施

### 3.5.1Win7\WinXP

日志读写控制核查，核查日志目录权限（读写控制），开始栏搜索框输入cmd以管理员身份打开。命令行输入“icacls C:\Windows\System32\winevt\Logs”，合规判断点：仅 SYSTEM / Administrators 有完全控制、普通用户 无写权限。



日志完整性 / 防篡改核查（命令行兜底）、核查是否启用 Windows 日志服务保护。命令行输入“sc qc eventlog”。合规点：服务状态正常、运行账户为 LocalSystem。



日志加密核查（系统层），核查日志所在磁盘是否加密（关键），命令行输入“manage-bde -status”，日志所在盘（通常 C:）为：转换状态: 完全加密。





TM 级数据存储加密核查（核心），核查 TM 级数据所在磁盘是否加密命令行输入“manage-bde -status”。合规条件：TM 级数据所在盘符、BitLocker 状态为 完全加密。



若是文件级加密，命令行输入“cipher /c <TM数据目录路径>”，如果显示已加密即合规。

日志完整性校验，核查是否具备日志转储 / 变换能力，命令行输入“wevtutil gl Security”。关注：最大日志大小、保留策略。表明日志 不可随意覆盖。



### 3.5.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo visudo”编辑 /etc/sudoers，仅允许审计用户通过 sudo 查看日志，添加一行“audit_admin ALL=(ALL) NOPASSWD: /bin/cat, /bin/grep, /usr/bin/journalctl”。

安装日志脱敏工具，在命令行输入“sudo apt-get install logrotate” 系统默认预装，用于日志轮转。



输入下列命令创建日志脱敏脚本，对日志中的 IP 地址、用户名等敏感字段进行脱敏变换。

sed -i 's/ \b(192\.168\.)[0-9]+\.[0-9]+\b/ \1xxx.xxx/g'  $1

sed -i 's/ \badmin\b/user_xxx/g'  $1



配置 logrotate 自动脱敏轮转日志，编辑 /etc/logrotate.d/syslog，添加脱敏脚本如下：

/var/log/syslog {

daily

rotate 7

compress

delaycompress

missingok

notifempty

create 0600 root syslog

postrotate 

/usr/local/bin/log_desensitize.sh /var/log/syslog.1 

Endscript

}



### 3.5.3 补充核查方法（应用层与数据库层日志保护）

【说明】本条标题明确列举“网络、系统、应用和用户行为等日志”，除已核查的系统事件日志/syslog外，还需核查应用日志、数据库审计日志、网络设备日志的保护措施。

（1）应用层应用日志读写控制与保护：

核查应用日志目录权限，仅应用服务账户可写、其他只读。Win7/WinXP：icacls "D:\tomcat\logs"；中标麒麟/银河麒麟：ls -la /var/log/tomcat/ /var/log/nginx/。

（2）数据库层数据库审计日志保护：

MySQL：SHOW VARIABLES LIKE 'audit%'；Oracle：SELECT * FROM dba_audit_trail WHERE ROWNUM<=5；SQL Server：SELECT * FROM sys.server_audits WHERE is_enabled=1。

（3）网络层网络设备日志传输保护：

登录网络设备核查syslog是否配置加密传输(TLS)、日志服务器访问控制。中标麒麟：grep -r "tls\|ssl\|encrypt" /etc/rsyslog.conf 2>/dev/null。

（4）完整性校验日志防篡改：核查是否部署日志完整性监控(AIDE/FIM)及日志远程转存防本地篡改。

【判定】系统日志、应用日志、数据库审计日志、网络设备日志的保护措施均需核查，任一类日志未采取读写控制/加密/完整性校验即存在风险。

## 3.6 网络边界应通过边界设备具备信息过滤、敏感内容识别等数据防泄漏能力

### 3.6.1 边界设备核查方法

登录边界防火墙、上网行为管理、代理网关、数据防泄漏（DLP）设备、邮件网关或安全接入网关等边界设备管理界面或命令行，核查互联网出口、跨安全域访问和对外数据传输是否经过统一边界设备进行控制，而非仅依赖主机本地防火墙。

查看边界访问控制策略，重点核查是否仅放行业务必需的源地址、目的地址、端口和协议，是否默认拒绝未授权访问，是否对外联、文件外发、邮件发送、Web 上传、FTP/SMB 传输等场景设置了访问控制或阻断策略。

查看内容过滤和敏感内容识别策略，重点核查是否启用了 URL/域名过滤、文件类型过滤、关键字或正则表达式匹配、文件指纹、OCR/内容识别等能力，是否覆盖 HTTP、HTTPS、邮件、FTP 等主要数据外发通道。

抽查设备告警、拦截和审计日志，确认日志中能够记录时间、源/目的 IP、用户或账号、应用/协议、命中策略、处置动作等关键信息，并能够追溯敏感内容外发尝试或违规访问行为。

如现场部署了 IPS、WAF、沙箱或防病毒网关等联动设备，还应核查与边界策略的联动情况，确认能够对高风险访问、恶意文件或异常流量进行告警、阻断或隔离处理。

### 3.6.2 合规判定

合规判断时，重点确认已部署统一边界防护设备，且重要数据对外传输、跨区域访问或互联网访问均经过边界设备控制；仅依赖服务器本机防火墙、主机杀毒软件或终端自身策略，而未通过边界设备统一防护的，不宜判定为符合本项要求。

边界设备应已启用访问控制、内容过滤和敏感内容识别策略，能够对未授权连接、违规文件外发、敏感关键字命中、异常协议传输等行为进行阻断或告警，并留有完整日志记录。

若抽查发现边界仅放行必要业务，存在明确的过滤/识别/拦截策略，且日志中可查询到命中记录、告警记录或处置记录，则可判定网络边界具备通过边界设备实施数据防泄漏和内容过滤的能力；反之则不符合。

### 3.6.3 通用核查方法（DLP 系统层）

（1）DLP 系统核查内容识别策略配置：登录 DLP 设备/平台管理界面，核查是否配置关键字/正则、文件指纹、OCR 等内容识别规则及文件类型、传输通道策略。判据：DLP 覆盖 HTTP/HTTPS、邮件、FTP、外设等主要数据外发通道。

（2）DLP 系统核查告警与拦截处置：抽查 DLP 告警、拦截日志，确认记录用户、时间、源/目的、通道、命中规则、处置动作。判据：敏感内容外发被识别并告警或阻断，日志可追溯。

（3）DLP 系统核查终端 DLP 联动：核查终端是否部署 DLP 客户端（外设管控、剪切板、打印、屏幕水印）。Win7/WinXP：Get-Process | Where-Object {$_.Name -match "dlp|防泄漏"}；中标麒麟/银河麒麟：ps aux | grep -i dlp。判据：终端侧数据外发受控。

【判定】已部署 DLP 并覆盖主要外发通道、内容识别与告警拦截生效且日志可追溯即满足；仅边界防火墙无 DLP 内容识别能力即不合规。

## 3.7 数据存储系统管理登录至少采取验证码等增强措施，修改默认用户名和口令等默认设置，管理与访问应具有行为审计功能，审计日志应至少保留180天

### 3.7.1Win7\WinXP

使用管理员账户登录 Windows 系统，点击“开始菜单”，进入“设置–账户–登录选项”，确认已启用验证码、短信、动态口令或多因素认证等增强登录措施，并通过实际登录验证管理账户除用户名和口令外需进行额外校验。

在桌面鼠标右键选择“以管理员身份运行 Windows Terminal（或命令提示符）”，在命令行中执行“net user”，查看本地账户列表，并通过执行net user <用户名>核查管理账户信息，确认已修改或禁用系统默认管理员账户，未使用默认用户名和默认口令。





在管理员命令行窗口中检查审计策略配置情况，执行“auditpol /get /category:*”确认已启用登录/注销、对象访问及账户管理等相关审计策略，确保管理与访问行为具备审计功能。



然后，通过命令行查看安全审计日志记录情况，在管理员命令行中执行“eventvwr.msc”进入“事件查看器–Windows 日志–安全”，抽查登录、访问和管理操作相关事件，确认审计日志已正常生成。



最后，通过命令行或界面方式核查日志保留时间，在管理员命令行中执行“wevtutil gl Security”查看日志最大大小及保留策略，并结合最早事件时间，确认安全审计日志保存时间不少于180天，保留相关界面截图或命令输出作为核查依据。



### 3.7.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo storage-cli login”登录存储管理，确认是否要求输入动态验证码（如通过邮件 / 短信获取），验证无验证码时无法登录。



在命令行输入“sudo storage-cli user list”确认无默认账户或默认账户重命名或禁用。



输入命令“sudo storage-cli audit log list --type=management”可查询审计日志。



输入命令“sudo storage-cli audit log config show”查看retention_days参数值是否≥180。



### 3.7.3 通用核查方法（数据库层）

（1）数据库层核查数据库管理登录增强认证：核查数据库管理登录是否启用验证码、双因素或证书认证（MySQL 认证插件、Oracle Database Vault/MFA、SQL Server Windows 集成认证或证书）。判据：除用户名口令外需额外校验方可登录。

（2）数据库层核查默认口令与默认账户修改：MySQL 5.7+ 执行 SELECT user,host,plugin FROM mysql.user WHERE user IN ('root','admin','test')，核查空口令执行 SELECT user,host FROM mysql.user WHERE authentication_string=''；MySQL 5.6 及以前空口令列名为 password：SELECT user,host FROM mysql.user WHERE password=''。SQL Server 执行 SELECT name, is_disabled FROM sys.sql_logins。判据：默认账户已改名或禁用、默认口令已修改、无空口令。

（3）数据库层核查行为审计与日志保留：核查数据库管理行为审计是否启用且日志保留不少于 180 天。MySQL 社区版核查 general_log 与 binlog 开启情况（SHOW VARIABLES LIKE 'general_log'，SHOW VARIABLES LIKE 'log_bin'），企业版核查 audit_log 插件；Oracle：SHOW PARAMETER audit_trail 并核查审计日志保留策略。判据：管理行为可审计且日志保留≥180 天。

【判定】数据库管理登录具备增强认证、默认账户口令已修改、行为审计启用且日志保留≥180 天即满足；使用默认账户/默认口令、无增强认证或无审计即不合规。

## 3.8 数据存储系统应根据重要程度划分不同存储区快，并设置用户访问权限

### 3.8.1Win7、WinXP

使用管理员账户登录 Windows 服务器，在桌面鼠标右键选择“以管理员身份运行 Windows Terminal（或命令提示符）”，通过命令行查看磁盘与分区情况，执行“diskpart”并依次输入“list disk”、“list volume”。确认系统已根据业务和数据重要程度划分不同的存储分区或逻辑卷。







在命令行中查看各存储分区对应的目录结构，执行
dir D:\、dir E:\等命令，确认不同重要级别的数据分别存放在不同分区或指定目录中。

在管理员命令行中核查存储区块的访问权限配置情况，通过执行icacls D:\、icacls E:\或icacls <数据目录路径>查看各存储区块或数据目录的访问控制列表，确认已按用户或用户组分配不同访问权限。



通过命令行查看本地用户和用户组配置，执行“net user”和
“net localgroup”确认访问存储区块的账号与权限分配相匹配，不同重要级别数据仅授权给相应用户或用户组访问。





结合实际访问测试，使用具备不同权限的用户账户登录系统，对不同存储区块进行访问验证，确认权限控制生效，并保留命令输出或测试结果作为核查依据。

### 3.8.2中标麒麟、银河麒麟

首先点击“开始菜单”，进入“安全中心”，



进入“防火墙与网络保护”，根据需求调整相关设置。



在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt-get update”和“sudo apt-get install suricata -y”安装Suricata。







根据实际需求在命令行执行如下命令配置suricata规则文件“alert http any any -> any any (msg:"DLP: 检测到'商业秘密'关键字"; http.uri; content:"商业秘密"; nocase; sid:1000001; rev:1;)”。

### 3.8.3 通用核查方法（数据库层）

（1）数据库层核查数据库表空间/库分区：核查是否按数据重要程度划分不同表空间、数据库或 Schema。MySQL：SHOW DATABASES、SELECT tablespace_name FROM information_schema.INNODB_TABLESPACES；Oracle：SELECT tablespace_name, status FROM dba_tablespaces；SQL Server：SELECT name FROM sys.filegroups。判据：不同密级或重要度数据存于不同表空间或库。

（2）数据库层核查库级用户权限：核查各库/表空间仅授权对应用户角色。MySQL：SHOW GRANTS FOR 'user'@'host'；Oracle：SELECT * FROM dba_tab_privs WHERE grantee='用户'；SQL Server：SELECT * FROM sys.database_permissions。判据：不同重要度数据仅授权相应用户/角色，无越权访问。

【判定】数据库按重要程度划分表空间/库并配置库级用户权限即满足；所有数据同库同表空间、权限未区分即不合规。

## 3.9 检查数据传输过程是否按照要求进行加密，传输路径是否合理，是否统一管控、留有日志记录，是否具有防泄漏措施，是否存在安全风险

### 3.9.1Win7、WinXP

使用管理员账户登录 Windows 服务器，在桌面鼠标右键选择“以管理员身份运行 Windows Terminal（或命令提示符）”，通过查看网络与服务配置核查数据传输是否启用加密，对涉及数据传输的服务执行netstat -ano或Get-NetTCPConnection确认业务端口及通信方式，并结合服务配置文件核实是否使用 HTTPS、TLS、SSL、SFTP 等加密协议。





在管理员命令行中查看证书及加密配置情况，执行certutil -store My或Get-ChildItem Cert:\LocalMachine\My核实数据传输服务已正确部署有效证书，未使用明文传输方式。





在命令行中检查数据传输路径配置，通过查看应用配置文件、共享路径或映射关系，并执行net use、Get-SmbShare确认数据传输路径清晰、固定，未通过非授权中间节点或不受控路径传输数据。





在管理员命令行中核查统一管控与日志记录情况，通过执行
auditpol /get /category:"Object Access"和auditpol /get /category:"Logon/Logoff"。确认已启用数据访问与传输相关审计策略，并通过eventvwr.msc查看“Windows 日志–安全”中数据访问、共享访问及网络连接相关日志记录。







在命令行中检查防泄漏相关配置，通过查看共享权限、传输策略或安全组件状态，执行Get-SmbShareAccess、icacls <数据目录路径>
或查看已部署的 DLP、防火墙、终端防护软件运行状态，确认具备数据外发控制和防泄漏措施。

结合命令行检查结果和实际传输测试，评估是否存在明文传输、路径不清晰、权限过宽、日志缺失或防护措施缺失等情况，判断数据传输过程是否存在安全风险，并留存核查记录作为依据。

### 3.9.2中标麒麟、银河麒麟

配置PAM实现登录验证码

在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt-get install libpam-google-authenticator -y”安装依赖。



输入“sudo -u 用户名 google-authenticator”为每个需要增强认证的用户配置；接下来配置PAM，输入“sudo vi /etc/pam.d/sshd”，在第一行添加“auth required pam_google_authenticator.so nullok”。





输入“sudo vi /etc/ssh/sshd_config”检查以下配置是否有误。



最后重启SSH服务，输入“sudo systemctl restart sshd”。

修改默认用户名和口令

输入以下命令，检查是否存在默认账户“cat /etc/passwd | grep -E "^(admin|root|test|user|kylin)" ”如果存在默认账户，输入以下命令“sudo passwd 用户名  #”修改默认用户口令。

查看审计状态，输入命令“sudo auditctl -s”。



### 3.9.3 通用核查方法（应用层与数据库层）

（1）应用层核查 HTTPS/TLS 加密：核查应用/Web 服务是否启用 HTTPS/TLS。Win7/WinXP：netstat -ano | findstr :443（Win7 的 PowerShell 2.0 无 Get-NetTCPConnection）；麒麟：openssl s_client -connect localhost:443 2>/dev/null | grep -i "protocol\|cipher"、grep -r "ssl\|tls" /etc/nginx/nginx.conf。判据：传输使用 HTTPS/TLS 且证书有效，未使用明文 HTTP。

（2）应用层核查应用间接口加密：核查应用间调用/接口是否使用 HTTPS/TLS 或国密算法。查看接口配置：grep -r "https\|tls\|ssl\|sm2\|sm4" /opt/*/conf/*.conf。判据：应用间接口传输加密。

（3）数据库层核查数据库连接 SSL 加密：MySQL：SHOW STATUS LIKE 'Ssl_cipher'、SHOW VARIABLES LIKE 'require_secure_transport'；PostgreSQL：SHOW ssl；SQL Server：SELECT encrypt_option FROM sys.dm_exec_connections。判据：应用到数据库连接已启用 SSL/TLS 加密，require_secure_transport=ON。

【判定】应用层 HTTPS/TLS 与数据库连接 SSL 加密均启用且证书有效即满足；存在明文传输（HTTP 或非 SSL 数据库连接）即不合规。

## 3.10 检查数据共享是否合理，是否存在安全隐患

### 3.10.1Win7、WinXP

在管理员命令行中查看证书及加密配置情况，执行certutil -store My、Get-ChildItem Cert:\LocalMachine\My，核实数据传输服务已正确部署有效证书，确认未使用明文传输方式。





在命令行中检查数据传输路径配置，通过查看应用配置文件、共享路径或映射关系，并执行 net use、Get-SmbShare，确认数据传输路径清晰、固定，未经过非授权中间节点或不受控路径传输数据。





在管理员命令行中核查统一管控与日志记录情况，执行auditpol /get /category:"Object Access"、auditpol /get /category:"Logon/Logoff"，确认已启用数据访问与传输相关审计策略，并通过 eventvwr.msc 查看“Windows 日志–安全”中数据访问、共享访问及网络连接相关日志记录。







### 3.10.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令，检查存储挂载点是否存在分级目录结构。

sudo find / -type d -name "*storage*" -o -name "*data*" 2>/dev/null | head-20

sudo find / -maxdepth 2 -type d \( -name "*l1*" -o -name "*l2*" -o -name

"*13*" -o -name "*14*"\) 2>/dev/nul1





接着输入下面命令，检查存储目录的权限情况。

for dir in /storage /data /opt/data; do

if [ -d "$dir" ]; then

echo"检查目录:$dir"

sudo 1s -1d "$dir" 

sudo find "$dir" -maxdepth 1 -type d 2>/dev/null | while read subdir; do

if [ "$subdir" I= "$dir" ]; then

echo " 子目录: $(basename $subdir)"

sudo 1s -1d "$subdir"

fi

done

echo" "

fi

done



### 3.10.3 通用核查方法（应用层与网络层）

（1）应用层核查共享平台/接口管控：核查数据共享是否经统一共享平台或数据交换接口，是否按最小必要原则授权（字段级、脱敏、审批）。查看共享接口配置与授权：grep -r "share\|共享\|api" /opt/*/conf/*.conf。判据：共享经统一平台、有授权与审批，无越权共享。

（2）应用层核查共享内容控制：核查共享数据是否脱敏、是否限制共享字段与范围、是否具备水印/追溯。判据：敏感数据共享经过脱敏与范围控制，可追溯。

（3）网络层核查共享服务审计：核查共享服务（SMB/NFS/FTP/SFTP）访问是否留痕。中标麒麟/银河麒麟：ss -tlnp | grep -E ':139|:445|:2049|:21|:22'、smbstatus 查看共享会话、查看 /var/log/samba/log.smbd。判据：共享访问行为可审计、可追溯。

【判定】数据共享经统一平台管控、共享内容受控且共享行为在网络层可审计即满足；存在未授权共享、越权共享或共享无审计即不合规。

## 3.11 检查对数据的访问是否按照权限分级访问，是否对访问行为进行审计

### 3.11.1Win7、WinXP

在管理员命令行中查看用户及用户组配置情况，执行net user、net localgroup，核实不同用户和用户组的权限划分情况，确认数据访问按照权限分级进行控制。





在命令行中检查数据目录或数据存储位置的访问权限配置，执行 icacls <数据目录路径>，核实仅授权相应级别的用户或用户组访问对应数据，未出现越权或权限过宽情况。

在管理员命令行中核查访问审计策略配置情况，执行auditpol /get /category:"Object Access"、auditpol /get /category:"Logon/Logoff"，确认已启用数据访问和登录相关审计策略。





在命令行或通过事件查看器查看审计日志记录情况，执行 eventvwr.msc，检查“Windows 日志–安全”中数据访问、文件访问及登录行为相关事件，确认对数据访问行为已进行审计并可追溯。



### 3.11.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“grep -r "CryptoPolicy" /etc/ssh/sshd_config /etc/nginx/nginx.conf”检查系统加密策略。



输入“ip route show”查看路由表，检查传输路径合理性。



接着输入“ sudo find /var/log -name "*network*" -o -name "*firewall*" -o -name "*iptables*" ”检查网络传输日志。



### 3.11.3 补充核查方法（数据库层与应用层权限分级）

【说明】数据访问权限分级除操作系统文件权限外，数据常存储于数据库，需核查数据库表/字段级权限及应用角色权限映射。

（1）数据库层数据库权限分级核查：

MySQL：SHOW GRANTS FOR 'user'@'host';；SQL Server：SELECT * FROM sys.database_permissions；PostgreSQL：\dp classified_table。确认不同密级数据表/字段仅授权对应用户角色。

（2）应用层应用角色与数据权限映射：

核查应用配置中角色-数据表/字段权限映射（grep -r "role\|权限\|classified" 应用conf）；通过不同角色用户登录应用测试能否访问不同密级数据。

（3）访问审计：核查数据库与应用层访问行为是否启用审计（数据库audit_trail/应用操作日志）。

【判定】数据访问权限分级需覆盖OS文件、数据库表/字段、应用角色三层，确认按密级分级且访问行为可审计。

## 3.12 检查数据采集是否超出业务需求范围

### 3.12.1Win7、WinXP

在管理员命令行中查看数据采集相关服务运行情况，执行sc query，核实当前运行的采集、同步或接口服务是否与业务系统清单一致，确认不存在无关或异常的数据采集服务。



在命令行或数据库管理终端中查看实际存储的数据内容结构，结合业务需求说明，通过查看数据库表、字段或文件目录名称，核实当前采集的数据类型、字段范围与业务需求相匹配，未发现明显超出业务需求范围的数据内容。

在管理员命令行中查看数据采集或接口访问日志，通过查看应用日志路径并执行 type <日志文件路径>、Get-Content <日志文件路径>，抽查数据采集来源、采集频率和采集内容摘要，确认采集行为与业务调用场景一致。

通过对比业务功能说明、系统实际运行服务和采集日志记录，综合判断是否存在采集与业务无关数据、采集范围过大或采集频率异常等情况，确认数据采集未超出业务需求范围，并保留核查记录作为依据。

### 3.12.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo smbstatus”检查当前共享状态。



接着输入“sudo netstat -tulpn | grep -E ":139|:445|:2049" | grep LISTEN”查看网络连接状态。



### 3.12.3 通用核查方法（应用层）

（1）应用层核查采集配置与最小化原则：核查采集功能配置是否仅采集业务必需字段/类型，比对采集清单与业务需求。查看采集配置：grep -r "collect\|采集\|field\|字段" /opt/*/conf/*.conf；Select-String -Path D:\app\conf\* -Pattern "collect|采集|field"。判据：采集字段/范围与业务需求一致，无定位、通讯录、传感器等与业务无关数据的过度采集。

（2）应用层核查采集授权与告知：核查采集是否有用户授权或告知，是否可配置关闭。判据：采集行为有授权依据，用户可拒绝非必要采集。

（3）应用层核查采集日志：核查采集日志记录采集来源、频率、内容摘要。Win7/WinXP：type <日志文件路径>、Get-Content <日志文件路径>；中标麒麟/银河麒麟：tail -f <日志路径>。判据：采集行为与业务场景一致，无异常采集。

【判定】采集范围与业务需求匹配、遵循最小化原则且采集行为可审计即满足；采集与业务无关数据、范围过大或频率异常即不合规。

## 3.13 检查数据各环节处理是否满足密级相应的保密要求

### 3.13.1Win7、WinXP

搜索框中输入cmd，用管理员命令打开，在管理员命令行中查看数据存储位置和目录划分情况，执行dir、diskpart、list volume，核实不同密级数据是否存放在独立的磁盘分区或指定目录中，确认物理或逻辑层面实现密级区分。







在管理员命令行中检查数据目录访问权限配置，执行 icacls <数据目录路径>，核实不同密级数据仅授权对应级别的用户或用户组访问，确认未出现跨密级访问或权限过宽情况。

在管理员命令行中查看数据传输和处理相关服务运行情况，执行 sc query，核实涉及数据处理、传输、同步的服务是否受控运行，确认未存在绕过安全控制的数据处理环节。



在管理员命令行中核查数据处理和访问审计配置情况，执行 auditpol /get /category:"Object Access"、auditpol /get /category:"Logon/Logoff"，确认已对不同密级数据的访问、处理行为启用审计。





在事件查看器中查看审计日志记录情况，执行 eventvwr.msc，检查“Windows 日志–安全”中数据访问、处理及管理操作相关事件，确认各环节操作可追溯，满足相应密级的保密要求。



结合数据存储位置、访问权限、处理服务和审计日志记录情况，综合判断数据在存储、处理、传输等环节是否符合对应密级的保密控制要求，并保留核查记录作为依据。

### 3.13.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查数据各环节处理的保密措施，按照实际需求或规定判断是否合规。

sudo bash -c '

echo "存储加密:$(cryptsetup status 2>/dev/null | grep -q LUKS && echo  是|| echo 否 )"

echo "传输加密:$(netstat -tulpn | grep -q ":443.*LISTEN" && echo 是 || echo 否)"

echo "访问控制: $(grep -q "PasswordAuthentication no" /etc/ssh/sshd_config 2>/dev/null && echo 是||echo否)"

echo "审计监控:$(systemctl is-active auditd && echo 是 || echo 否)"

echo "数据分类:$(find / -type d -name "*密*" 2>/dev/null | head -1 && echo 是 || echo 否)"





### 3.13.3 通用核查方法（应用层与数据库层）

（1）应用层核查密级标识功能：核查应用是否对数据/文档在生成与流转时标注密级标识（公开、内部、秘密、机密），密级标识是否随数据流转且不可篡改。查看应用密级配置：grep -r "密级\|classified\|secret" /opt/*/conf/*.conf。判据：数据有明确密级标识且流转中保留。

（2）应用层核查密级访问控制：核查应用是否按密级实施差异化访问控制（低密级用户不能访问高密级数据）。判据：跨密级访问被阻止。

（3）数据库层核查密级标识与处理合规：核查数据库是否存储密级字段并据此授权、是否按密级分区/分表存储。MySQL：SELECT column_name FROM information_schema.columns WHERE table_name='目标表'、SHOW GRANTS FOR 'user'@'host'。判据：数据库按密级字段实施权限控制，密级数据分区/分表存储。

【判定】数据在存储、处理、传输各环节均有密级标识并按密级实施访问控制即满足；无密级标识、跨密级访问或处理即不合规。

## 3.14 检查对数据的访问是否按照权限分级访问，是否对访问行为进行审计，对大数据的访问是否提供同一管控和访问控制

### 3.14.1Win7、WinXP

搜索框中输入cmd，用管理员命令打开，在管理员命令行中查看系统用户和用户组配置情况，执行net user、net localgroup，核实已根据岗位或职责划分不同用户和用户组，为数据分级访问提供基础支撑。





在管理员命令行中检查数据存储目录或数据存储位置的访问权限配置，执行 icacls <数据目录路径>，核实不同级别数据仅授权对应用户或用户组访问，确认数据访问按照权限分级进行控制。

在管理员命令行中查看大数据存储或集中数据平台相关服务运行情况，执行 sc query，核实涉及大数据存储、分析和访问的服务是否统一部署并处于受控状态，确认不存在绕过统一平台的独立访问通道。

在管理员命令行中核查数据访问审计策略配置情况，执行 auditpol /get /category:"Object Access"、auditpol /get /category:"Logon/Logoff"，确认已启用数据访问、文件访问及登录相关审计策略。





在事件查看器中查看数据访问审计日志记录情况，执行 eventvwr.msc，检查“Windows 日志–安全”中与文件访问、共享访问及平台访问相关的事件，确认对数据及大数据访问行为已统一记录和审计。



结合用户权限配置、数据访问控制、大数据平台服务运行状态及审计日志记录情况，综合判断数据访问是否实现分级授权、大数据访问是否纳入统一管控和访问控制范围，并保留核查记录作为依据。

### 3.14.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo find / -type d -name "*l[1-4]*" -exec ls -ld {} \;”检查目录权限分级。



接着输入以下命令检查审计状态。

sudo bash -c '

echo "审计服务: $(systemctl is-active auditd)"

echo "规则数量:$(auditctl -1|wc -1)"

echo"关键规则:"

auditctl -1 | grep -E "passwd|shadow|sudo|su|sshd" | head -5

'





输入“[ -f "/etc/krb5.conf" ] && echo 是 || echo 否”检查对大数据的访问是否提供同一管控和访问控制。



### 3.14.3 补充核查方法（大数据平台层统一管控）

【说明】本条标题专门提及“大数据访问统一管控”，除操作系统层权限外，需核查大数据平台(Hadoop/Hive/Ranger)的访问控制。

（1）大数据平台层HDFS/Hive权限与Ranger策略：

核查HDFS ACL：hdfs dfs -getfacl /data/classified；核查Hive权限：beeline -e "SHOW GRANT"；核查Apache Ranger策略：通过Ranger Admin界面查看policy列表；核查Kerberos认证：klist。

（2）统一管控核查：确认大数据访问是否必须经统一平台(无直连数据库旁路)，ss -tlnp | grep -E ':50070|:10000|:8020'。

（3）数据库审计：核查数据库审计策略是否覆盖大数据查询行为。

【判定】大数据访问需通过Ranger/HDFS ACL/Hive权限/Kerberos统一管控，存在绕过统一平台的直连通道即不合规。

## 4 应用安全

## 4.1 应用系统软件应采取备份措施

### 4.1.1Windows操作系统 (Win7, WinXP)

找到备份目录，使用win+R打开控制框，输入cmd，在命令行输入“dir C:\ /ad /s | findstr /i "backup bak”。



查看是否由“近期备份文件”，命令行输入“dir D:\backup /o:-d”看时间。没输出就是没有近期备份文件。



检查是否有定时备份任务，命令行输入“schtasks”，列出所有计划任务。输入“schtasks | findstr /i "backup bak copy"”只看可能的备份任务。





### 4.1.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查系统备份状态。

sudo bash -c '

echo"备份状态检查:"

echo "备份目录:$(find / -type d -name "*backup*" 2>/dev/null | head -3 | wc -1)个"

echo "备文件: $(find / -type f -name "*.bak" -o -name "*.tar.gz" -o -name "*.sql.gz" 2>/dev/null |wc-1)个"



### 4.1.3 补充核查方法（应用层与数据库层）

【说明】应用系统软件备份可能涉及三个层面，不可只核查操作系统层。以下补充应用层与数据库层的核查方法，与4.1.1/4.1.2的操作系统层方法配合使用。

（1）应用层核查应用自身备份功能与备份配置：

查看应用管理界面是否具备“备份/恢复”功能模块；核查应用配置文件中备份相关参数（如 backup.path、backup.schedule、dump.target）。

Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "backup|备份|dump" -ErrorAction SilentlyContinue

中标麒麟/银河麒麟：grep -r "backup\|备份\|dump" /opt/*/conf/*.conf 2>/dev/null

（2）数据库层核查数据库备份策略与备份文件：

核查数据库是否配置定时备份任务、备份文件是否存在且可恢复。

MySQL：核查mysqldump定时任务 crontab -l | grep mysqldump；查binlog是否开启 mysql -e "SHOW VARIABLES LIKE 'log_bin'"（用于增量恢复）。

SQL Server：SELECT database_name, backup_finish_date, type FROM msdb.dbo.backupset ORDER BY backup_finish_date DESC

Oracle：RMAN> list backup summary（核查RMAN备份集）

（3）恢复能力验证：选取最近一次备份文件，在测试环境执行恢复操作，确认备份可恢复；核查是否存在恢复脚本（find -name "*restore*"）。

【判定】应用层、数据库层、操作系统层任一层确认有效备份措施即视为满足；仅操作系统层未发现不能直接判不合规，需结合另外两层人工确认。

## 4.2 应用系统软件应及时安装补丁程序，且更新所用升级包应经过安全性测试

### 4.2.1Windows操作系统 (Win7, WinXP)

Windows 系统补丁，使用win+R打开控制框，输入cmd，在命令行输入“wmic qfe list brief /format:table”。



查看windows安装日志，开始栏输入框输入powershell打开命令行，命令行输入“type C:\Windows\WindowsUpdate.log”。



查看应用程序日志，命令行输入“Get-ChildItem "C:\App\Logs" -Filter "*update*.log" | Select Name, LastWriteTime”。核查安全性测试记录。

### 4.2.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo apt update”检查更新状态。



接着输入“sudo apt list --upgradable”查看安全更新列表。



### 4.2.3 通用核查方法（应用层与数据库层）

（1）应用层核查应用补丁：查看应用版本号与官方最新安全版本对比。Win7/WinXP：wmic product get name,version（或人工查看应用"关于"界面）；麒麟：dpkg -l | grep <应用名> 或 rpm -qa | grep <应用名>。判据：应用已升级到最新安全版本，无已知高危漏洞版本。

（2）数据库层核查数据库补丁/版本：MySQL 执行 SELECT VERSION()；SQL Server 执行 SELECT @@VERSION；Oracle 执行 SELECT * FROM v$version。判据：数据库版本在官方安全维护周期内，无已知高危 CVE 未修复。

【判定】操作系统、应用、数据库任一层补丁缺失即判不合规，需逐层确认。

## 4.3 基于可信根对应用系统软件进行可信验证。可信性受到破坏后报警

### 4.3.1Windows操作系统 (Win7, WinXP)

使用 sigcheck 验证可执行文件签名,sicheck 是 Sysinternals 提供的工具，可验证文件是否由可信根签名。

下载Sigcheck地址：

https://learn.microsoft.com/en-us/sysinternals/downloads/sigcheck。

打开搜索框搜索“ 命令提示符（管理员）”。命令行执行命令验证文件签名：“sigcheck.exe -q -m C:\路径\到\程序.exe”，-q 静默输出，-m 显示签名的证书链。示例：“sigcheck.exe -q -m C:\Windows\System32\notepad.exe”。如果显示 Verified: Signed 并且证书链可信，说明软件可信。如果显示 Unable to verify signature 或证书链有问题，说明可信性被破坏。

搜索框输入PowerShell 检查签名，“Get-AuthenticodeSignature "C:\路径\到\程序.exe"”

输出示例：



启用和查看系统安全日志，使用win+R打开输入框，打开事件查看器：输入“eventvwr.msc”。路径：Windows 日志 → 安全筛选 可信性/签名相关事件：事件 ID 传感器：4656、4688 等，可结合 PowerShell 读取：“Get-WinEvent -LogName Security | Where-Object {$_.Message -like "*签名*"}”。可设置报警脚本，当状态非 Valid 时触发通知。

### 4.3.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令查看是否基于可信根（硬件）。

ls /sys/class/tpm/tpm* 2>/dev/null | while read tpm; do

echo " $(basename $tpm):存在"

done



接着输入“systemctl list-units | grep -E "aide|tripwire"”检查监控报警配置。



### 4.3.3 通用核查方法（应用层可信验证）

（1）应用层应用签名验证：核查应用可执行文件/安装包是否由可信根签名。Win7/WinXP：Get-AuthenticodeSignature "C:\路径\程序.exe"（状态应为 Valid）；麒麟：rpm -K <软件包.rpm>（验证 RPM 包签名），deb 包人工核对签名或用 dpkg -V 校验完整性。判据：应用签名有效且证书链可信。

（2）应用层可信启动链核查：核查应用启动时是否校验自身及依赖组件完整性（哈希校验/可信启动）。人工查看应用启动配置与完整性校验记录；麒麟：systemctl list-units | grep -iE "aide|tripwire"。判据：应用具备启动完整性校验能力。

（3）报警能力核查：核查可信性受破坏后的报警配置（告警脚本/日志/通知）。判据：签名校验失败或完整性破坏时能够触发报警。

【判定】应用基于可信根签名且启动时进行完整性校验、破坏后能报警方为满足；仅操作系统层存在 TPM 而应用未签名不可判合规。

## 4.4 提供公共信息服务的服务器应应与涉密信息服务器分设，专用服务器应只提供专用服务

### 4.4.1Windows操作系统 (Win7, WinXP)

在 Windows 下核查“公共信息服务服务器是否与涉密信息服务器分设，专用服务器是否只提供专用服务”，本质上是 检查服务、端口和网络绑定情况。使用win+R打开输入框，输入cmd，打开命令行，查看本机开放端口和对应服务“netstat -ano | findstr LISTENING”，-a 显示所有连接和监听端口，-n 以数字显示地址和端口，-o 显示对应 PID。



然后用 PID 查对应进程：“tasklist /FI "PID eq <PID号>"”这样可以确认哪些端口和服务在运行，判断是否有公共服务与涉密服务混在一台服务器上。

列出所有 Windows 服务，“sc query state= all”。核查专用服务器上是否只运行专用服务（比如数据库服务、内部应用服务），核查公共服务（如 Web 服务、邮件服务）是否运行在单独服务器上。



查看特定端口对应的程序“netsh interface ipv4 show excludedportrange protocol=tcp”。可确认端口分配，确保公共服务和专用服务不使用相同端口。



### 4.4.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo netstat -tulpn | grep LISTEN | awk '{print $4 " " $7}' | sort”，进行服务绑定分析。



接着输入“sudo iptables -L INPUT -n --line-numbers”，查看防火墙策略是否合规。



### 4.4.3 通用核查方法（网络层隔离与分区）

（1）网络层网络隔离/分区核查：核查公共信息服务与涉密信息服务是否处于不同网段/VLAN/安全域，核查跨域访问是否受控。登录交换机/防火墙查看 VLAN 划分与访问控制策略；麒麟：sudo ip route show、sudo iptables -L -n。判据：公共信息服务器与涉密信息服务器网络隔离，不存在未受控的跨区直连。

（2）网络层边界访问控制核查：核查防火墙是否仅放行必要的跨区访问并默认拒绝。判据：仅放行业务必需的跨区通信，其他访问默认拒绝。

【判定】公共服务与涉密服务需同时满足主机层（不同服务器、专用服务）与网络层（不同网段/分区）隔离，任一层混用即判不合规。

## 4.5 提供公共信息服务的服务器应具备防DDoS攻击能力

### 4.5.1Windows操作系统 (Win7, WinXP)

在 Windows 上，核查服务器是否具备防 DDoS 能力，本质上是检查 是否启用了防火墙、端口限制和入站/出站流量策略。命令行可用以下方式：

查看 Windows 防火墙状态“netsh advfirewall show allprofiles”，查看 域、专用、公用 三个防火墙配置是否启用，输出中 State ON → 防火墙已启用。



查看防火墙规则，“netsh advfirewall firewall show rule name=all”，查看所有规则，确认是否有针对 Web 服务或特定端口的限制规则。



查看入站/出站端口限制“Get-NetFirewallRule | Where-Object {$_.Direction -eq "Inbound" -and $_.Enabled -eq "True"} | Select-Object DisplayName, Action, Enabled”。可以核查哪些入站流量被允许或阻断。

查看网络保护状态（Windows Defender 防护网络攻击）“powershell -Command "Get-NetFirewallRule | Where-Object {$_.Direction -eq 'Inbound' -and $_.Enabled -eq 'True'} | Select-Object DisplayName, Action, Enabled"”，用于查看 攻击面减小（ASR）规则 是否启用。



### 4.5.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo sysctl -a 2>/dev/null|grep-E"tcp_syncookies|tcp_max_syn_backlog|nf_conntrack”"，查看内核防护参数。



接着输入“sudo iptables -L -n | grep -E "syn\|flood\|limit"”查看防火墙防护规则（完整配置）。



### 4.5.3 补充核查方法（网络边界设备与应用层限流）

【说明】防DDoS需多层协同：边界设备(网络层)+应用服务器(应用层)+主机(操作系统层)。4.5.1/4.5.2已核查主机防火墙与内核参数，补充边界设备与应用层。

（1）网络边界层边界DDoS防护设备核查：

登录上游防火墙/流量清洗设备，核查是否配置DDoS检测和清洗策略（SYN Flood/UDP Flood/ICMP Flood检测），查看边界设备DDoS告警和处置日志。

（2）应用层应用服务器限流核查：

Nginx：grep -r "limit_req\|limit_conn\|limit_rate" /etc/nginx/；Tomcat：grep -r "maxThreads\|acceptCount\|maxConnections" server.xml；核查WAF限速规则。

【判定】防DDoS能力需主机+边界设备+应用层多层协同，仅主机防火墙不足以防御DDoS。

## 4.6 Web应用系统应采取Web安全防护措施

### 4.6.1 通用核查方法

输入校验核查。通过浏览器、接口测试工具或查阅应用配置/代码，梳理登录、查询、搜索、评论、上传、接口调用等用户输入点，检查应用是否对参数长度、字符类型、数据格式、取值范围进行校验，是否采用正则表达式、白名单、长度限制或过滤函数处理异常输入；应注意黑名单验证方式存在局限性，仅查找或替换引号、script 等少量字符或关键字，容易被大小写变形、编码变换、事件属性、标签拆分等跨站脚本变种绕过，不宜作为唯一控制措施。

输出校验与编码核查。对页面回显、提示信息、富文本展示、URL 参数回显等输出点进行检查，确认用户提交的数据在输出前已进行 HTML 编码、JavaScript 编码、URL 编码或模板自动转义，避免脚本代码、特殊标签或恶意内容被直接执行；同时应明确指定统一的输出编码方式，通常固定为 UTF-8，不允许由客户端或用户自行选择、切换编码方式（如 ISO 8859-1、UTF-8 等），避免利用编码差异绕过校验或输出控制。

SQL 注入防护核查。抽查应用访问数据库的关键代码、ORM/DAO 配置或接口实现，确认未采用字符串拼接方式动态构造 SQL，而是使用参数化查询、预编译语句、绑定变量或存储过程；必要时可结合代码搜索、配置文件、数据库慢日志或查询日志进行佐证。

数据库连接最小权限核查。查看应用数据库连接配置和数据库授权情况，确认每个应用使用独立的数据库账号，账号仅具备业务所需最小权限，不使用 DBA、sa、root、SYSDBA 等高权限账户作为应用连接账户。

异常处理与错误信息控制核查。通过构造非法参数、越权访问、空值、超长值等异常请求，观察前台返回信息，确认系统未直接暴露数据库报错、程序堆栈、物理路径、SQL 语句等敏感信息；同时检查后台是否通过独立日志、异常表或安全日志记录原始异常。

请求方法区分核查。查看应用接口设计、路由配置、前后端交互和服务器处理逻辑，确认系统能够区分 GET 与 POST 等不同请求方法，对查询类只读操作与新增、修改、删除、登录提交等状态变更操作进行分离；对于不符合预期的方法调用应拒绝处理或返回错误，从而增加攻击利用难度。

Cookie 安全属性与有效期核查。检查认证 Cookie、会话 Cookie 或令牌相关配置，确认已根据业务需要设置合理的 Expires 或 Max-Age，并结合服务端会话超时进行控制，不应长期有效或永久不过期；同时宜检查是否启用 HttpOnly、Secure、SameSite 等安全属性。

附加防护措施核查。查看是否部署 WAF、反向代理、请求过滤、限速、防暴力破解、安全响应头等措施，重点关注 URL/参数检查、恶意特征规则、上传限制、CSP、X-Frame-Options、X-Content-Type-Options、Cookie 的 HttpOnly/Secure/SameSite 等配置。

### 4.6.2 主要判定标准

判定是否符合时，至少应确认应用对主要输入点和输出点进行了有效校验或编码处理，能够对超长、非法格式、特殊字符和脚本内容进行拦截、过滤、转义或规范化处理；若仅依赖简单黑名单替换少量字符或关键字，不能有效防范变形攻击的，不应判定为符合。

应用应固定并明确使用统一的安全输出编码方式，不允许用户或客户端自行切换编码；若存在可控编码方式导致输出校验、转义或过滤可被绕过的情形，应判定为不符合。

数据库访问层应未发现动态拼接 SQL 的高风险实现，应用数据库连接应使用独立且权限受限的专用账号；若使用管理员或系统级高权限账户作为应用连接账户，应判定为不符合。

系统应能够区分 GET 与 POST 等请求方法，对敏感操作采用更严格的提交和校验方式，并对认证 Cookie 或会话 Cookie 设置受控的到期时间；若请求方法混用严重、敏感操作可直接通过 GET 触发，或认证 Cookie 长期不过期、缺乏有效超时控制，应判定为不符合。

前台错误提示应尽可能少，仅返回经过包装的通用错误信息；后台应保留原始异常或审计日志，便于追溯。若测试时可直接看到数据库类型、表名、字段名、程序堆栈或服务器路径等信息，应判定为不符合。

若结合实际测试发现典型 SQL 注入、跨站脚本、异常输入无法成功执行，页面或接口未暴露敏感错误信息，且存在必要的附加防护措施、请求方法控制和会话有效期控制，则可判定 Web 应用系统已采取较完善的 Web 安全防护措施。

### 4.6.3 通用核查方法（数据库连接最小权限补充）

（1）数据库层数据库连接最小权限具体命令：MySQL 执行 SELECT user,host FROM mysql.user; 及 SHOW GRANTS FOR 'appuser'@'%';；SQL Server 执行 SELECT name, type_desc FROM sys.database_principals WHERE type IN ('S','U'); 及 EXEC sp_helprolemember;。判据：应用使用独立受限账号连接数据库，未使用 root/sa/DBA 等高权限账号。

【判定】应用数据库连接账号必须为独立且权限最小化的专用账号，使用高权限账号即判不合规。

## 4.7 网站服务宜以静态页面形式发布

### 4.7.1Win7、WinXP

核查是否存在 PHP 文件，Windows搜索框中输入PowerShell回车，在 PowerShell 中执行：“Get-ChildItem -Path C:\inetpub\wwwroot -Recurse -Include *.php -File -ErrorAction SilentlyContinue | Measure-Object | Select-Object -ExpandProperty Count”。判定标准： 返回值应为 0。



核查是否存在 JSP / ASP 动态脚本，在 PowerShell 中执行：“Get-ChildItem -Path C:\inetpub\wwwroot -Recurse -Include *.jsp,*.asp,*.aspx -File -ErrorAction SilentlyContinue | Measure-Object | Select-Object -ExpandProperty Count”判定标准：返回值应为 0。



核查服务器配置中是否存在动态脚本解析配置，在 PowerShell 中执行：“Select-String -Path C:\Windows\System32\inetsrv\config\applicationHost.config -Pattern "php|asp|jsp" -SimpleMatch”。判定标准： 无任何输出。



核查 HTTP 响应头中是否包含动态技术标识，在 PowerShell 中执行：“Invoke-WebRequest -Uri http://localhost -Method Head -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Headers | Select-Object "X-Powered-By"”。判定标准： 无返回值。



综合判定，上述各项核查结果均为 0 或无输出时，判定网站服务以静态页面形式发布。

### 4.7.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“find /var/www/html -name "*.php" -type f 2>/dev/null | wc -l”查看PHP文件请个数，静态网站应为0个；然后输入“find /var/www/html -name "*.jsp" -o -name "*.asp" -type f 2>/dev/null | wc -l”查看动态脚本，静态网站应为0个；接着输入“grep -c "php" /etc/nginx/sites-enabled/* 2>/dev/null”查看服务器配置，静态网站应无配置，无返回值；最后输入“ curl -I http://localhost 2>/dev/null | grep -c "X-Powered-By" ”检查HTTP标识，静态网站应为0。



### 4.7.3 通用核查方法（Web服务器层静态页配置）

（1）Web服务器层Nginx 静态页配置核查：核查 Nginx 是否仅配置静态资源 location，未启用 PHP-FPM/动态解析。麒麟：grep -rE "fastcgi_pass|proxy_pass|\.php|\.jsp" /etc/nginx/nginx.conf /etc/nginx/conf.d/*.conf 2>/dev/null。判据：无动态脚本解析配置，站点仅返回静态文件。

（2）Web服务器层Tomcat 静态页配置核查：核查 Tomcat 未部署 JSP/servlet 动态应用。Win7/WinXP：dir C:\tomcat\webapps；麒麟：ls -la /opt/tomcat/webapps/。判据：webapps 目录无动态 Web 应用，仅发布静态内容。

【判定】网站服务应仅发布静态页面，Nginx/Tomcat 等 Web 服务器未启用动态脚本解析且无动态应用部署方为满足。

## 4.8 网站应采取网页防篡改措施，防止对信息内容的非法修改

### 4.8.1Win7、WinXP

通过浏览器访问网站首页及主要业务页面，查看页面内容展示形式，确认页面主要以 HTML、CSS、JavaScript 等静态资源方式呈现，未直接暴露后台处理逻辑或数据库操作接口。

在网站服务器上以管理员身份打开命令行，查看网站部署目录结构，执行 dir <网站根目录路径>，核实网站目录中以 .html、.htm、.css、.js、图片等静态文件为主，未在对外访问目录中部署不必要的动态脚本文件。

在命令行中检查 Web 服务运行情况和站点配置，执行 sc query、netstat -ano，确认网站服务仅监听必要端口，未对外开放调试接口、后台管理接口或不必要的动态服务端口。

在服务器上检查网站访问日志，通过查看 Web 服务日志文件，核实用户访问记录主要集中在静态资源请求，未发现频繁调用后台接口或异常动态请求行为。

结合网站页面展示形式、部署目录结构、服务端口开放情况和访问日志记录，判断网站服务是否以静态页面形式发布，未因动态页面或接口暴露引入额外安全风险，并保留核查记录作为依据。

### 4.8.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo grep -E "/var/www|html" /etc/aide/aide.conf”，查看Web目录监控配置。



接着输入“sudo ls -la /var/www/html/index.*”查看关键文件状态。



所有者为www-data，所有者可读写，其他人只读。

### 4.8.3 补充核查方法（专业防篡改系统与文件完整性）

【说明】网页防篡改需核查专业防篡改系统、文件完整性监控、Web目录权限，而非仅查静态页面发布(属4.7)。

（1）专业网页防篡改系统：

核查防篡改软件进程/服务。Win7/WinXP：Get-Process | Where-Object {$_.Name -match "guard\|protect\|tamper\|防篡改"}；中标麒麟：systemctl list-units | grep -iE "guard\|tamper\|protect\|防篡改"。

（2）文件完整性监控(FIM)：核查AIDE/FIM是否覆盖Web目录。grep -E "/var/www\|html" /etc/aide/aide.conf。

（3）Web目录权限与只读保护：icacls C:\inetpub\wwwroot（仅管理员可写、Web服务账户只读）；mount | grep "/var/www"（核查只读挂载）。

【判定】部署专业防篡改系统或文件完整性监控覆盖Web目录、且Web目录权限收紧，方为满足。

## 4.9 Web应用系统应具备防范SQL注入、跨站脚本等攻击能力

### 4.9.1Win7、WinXP

核查 Web 应用系统是否具备防范 SQL 注入攻击能力。通过浏览器或测试工具（如 PowerShell、Postman）向应用输入参数中构造典型 SQL 注入语句（如 ' or '1'='1、union select 等），观察系统响应情况。判定标准：系统未返回数据库错误信息，未出现异常数据泄露，未绕过正常身份认证。

核查 Web 应用系统是否具备防范跨站脚本（XSS）攻击能力。通过浏览器在输入框、URL 参数等位置注入脚本代码（如 <script>alert(1)</script>），观察页面回显情况。判定标准： 注入脚本未被执行，页面对特殊字符进行了转义或过滤处理。

核查 Web 应用系统是否对输入数据进行合法性校验。对表单、URL 参数、请求头等输入项输入超长字符、特殊字符及非法格式数据，观察系统处理结果。判定标准： 系统能够进行有效校验并给出合理提示，不发生异常。

核查 Web 应用系统是否对错误信息进行安全控制。通过构造异常请求，观察系统返回的错误提示内容。判定标准： 系统未暴露数据库结构、后台路径、程序堆栈等敏感信息。

综合判定。上述各项核查结果均符合判定标准时，判定 Web 应用系统具备防范 SQL 注入、跨站脚本等常见 Web 攻击的安全防护能力。

### 4.9.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“ curl -I http://目标URL | grep -iE "(X-Content-Type|X-Frame|CSP)" ”，检查HTTP安全头，期望看到如下结果。



接着输入“ curl -s "$URL' OR '1'='1" | grep -i "error\|sql\|mysql" ”检测SQL注入漏洞，无返回值表示已防护，处于安全状态。

### 4.9.3 通用核查方法（应用层SQL注入与XSS防护）

（1）应用层参数化查询/预编译核查：核查应用数据库访问是否使用参数化查询、预编译语句而非字符串拼接 SQL。Win7/WinXP：Select-String -Path D:\app\src\* -Pattern "Statement|拼接"（人工查看代码）；麒麟：grep -rn "Statement\|concat\|拼接" /opt/app/src/ 2>/dev/null。判据：未发现动态拼接 SQL，使用参数化/预编译。

（2）应用层WAF规则核查：核查 WAF 是否配置 SQL 注入/XSS 防护规则并启用。人工查看 WAF 策略界面。判据：WAF 已启用 SQL 注入、XSS 攻击检测拦截规则。

【判定】应用需在代码层使用参数化查询/输出编码，并在 WAF 层配置 SQL 注入/XSS 防护规则，任一层缺失即存在风险。

## 4.10 Web应用系统应具备执行代码有效验证能力

### 4.10.1Win7、WinXP

通过查看 Web 应用系统的功能页面和业务流程，检查是否存在可上传、执行或调用脚本、程序的功能模块，重点关注文件上传、接口调用、插件加载等场景，确认系统对可执行代码具备校验控制。

在 Web 服务器上以管理员权限检查应用部署目录和配置文件，核实对上传文件或可执行内容是否设置了类型校验、后缀校验或白名单控制，确认未允许未经验证的脚本或程序直接执行。

通过实际功能测试，在文件上传、参数提交等位置尝试提交包含脚本或代码的内容，观察系统响应情况，确认系统能够拒绝、过滤或拦截非法代码，未被直接执行或解析。

查看 Web 应用或安全防护组件的日志记录，核实对非法代码提交、执行失败或拦截行为是否进行了记录，确认执行代码校验过程可审计、可追溯。

结合系统配置、功能测试结果和日志记录情况，综合判断 Web 应用系统是否具备对执行代码进行有效验证和控制的能力，并保留核查记录作为依据。

### 4.10.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“nikto -h http://192.168.1.100 -Tuning 1,3,5 -timeout 5”扫描漏洞（假设$URL="http://192.168.1.100）。



接着，输入“curl -I http://192.168.1.100:8080/webapp/”检查安全头（假设目标URL为http://192.168.1.100:8080/webapp/）。





### 4.10.3 通用核查方法（应用层执行代码验证）

（1）应用层输入白名单/代码执行校验核查：核查应用对可执行代码（上传文件、脚本、命令、表达式）是否采用白名单校验，禁止执行未授权代码。人工查看代码与配置（上传白名单、命令白名单、表达式引擎限制）。判据：应用对可执行内容采用白名单校验，未授权代码无法执行。

（2）应用层危险函数/命令执行核查：核查应用是否存在命令执行、代码执行高危函数未做限制。麒麟：grep -rnE "eval|exec|system|Runtime.exec|shell_exec" /opt/app/ 2>/dev/null。判据：高危执行函数已禁用或严格受限。

【判定】应用需对可执行代码采用白名单校验并限制高危执行函数，存在可直接执行未验证代码的入口即判不合规。

## 4.11 应具备用户授权访问控制能力

### 4.11.1Win7、WinXP

核查系统是否具备基于用户授权的访问控制能力。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看本地用户账户及所属用户组：

    Get-LocalUser | Select-Object Name, Enabled

    Get-LocalGroupMember -Group "Administrators"

判定标准：系统中的用户账户均已明确授权，管理员组成员数量受控，不存在未授权或权限异常的账户，用户权限与其业务角色相匹配。

### 4.11.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令查看系统用户身份、所属用户组及授权范围，核查用户与权限的绑定关系。

id 用户名

groups 用户名

sudo -l

判定标准：系统用户身份与所属用户组、sudo 授权范围均已明确记录，不存在权限越界或未授权账户，用户权限分配与其业务角色相匹配。

### 4.11.3 补充核查方法（应用层RBAC与数据库授权）

【说明】本条位于应用安全章，应核查应用自身角色权限管理，而非仅查操作系统本地用户/组。

（1）应用层应用RBAC核查：

登录应用管理界面查看角色列表和权限分配；核查应用配置中角色-权限映射：Select-String -Path D:\app\conf\* -Pattern "role\|permission\|auth\|权限"；grep -r "role\|permission\|auth\|权限" /opt/*/conf/*.conf。通过不同角色用户登录应用测试功能权限区分。

（2）数据库层数据库用户授权：

MySQL：SELECT user,host FROM mysql.user; SHOW GRANTS FOR 'appuser'@'%';；SQL Server：SELECT * FROM sys.database_principals WHERE type IN ('S','U')；确认应用使用独立受限数据库账号而非DBA高权限账号。

【判定】应用自身实现RBAC(角色-功能/数据权限绑定)、数据库账号权限最小化，方为满足。

## 4.12 应具备访问应用最大并发会话连接数限制能力

### 4.12.1Win7、WinXP

核查 Web 应用系统是否具备访问应用最大并发会话连接数限制能力。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，模拟并发访问应用服务：“1..200 | ForEach-Object {

    Start-Job { Invoke-WebRequest -Uri http://localhost -UseBasicParsing }

}”判定标准：当并发会话数超过系统设定阈值时，部分请求被拒绝、延迟响应或返回限制提示信息，系统未出现服务崩溃或异常中断。



核查 Web 应用系统在并发访问场景下的会话控制情况。在 PowerShell 中执行以下命令，查看并发请求执行状态：“Get-Job”判定标准：系统能够对并发会话进行有效控制，请求数量未无限制增长。



核查 Web 应用系统在高并发访问情况下的服务稳定性。在并发请求执行过程中，通过浏览器持续访问应用页面，观察系统运行状态。判定标准：应用服务响应正常，未出现拒绝服务、进程异常退出或系统资源耗尽情况。

综合判定。上述核查结果符合判定标准时，判定 Web 应用系统具备访问应用最大并发会话连接数限制能力。

### 4.12.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“cat /proc/sys/fs/file-max”查看系统最大文件数；接着输入如下命令查看网络连接限制。

cat /proc/sys/net/core/somaxconn

sudo sysctl net.core.somaxconn



然后输入“ss -s | grep 'TCP:' | awk '{print $2}' ”查看当前TCP总连接数。



### 4.12.3 通用核查方法（应用层与Web服务器层）

（1）应用层会话配置核查：核查应用最大并发会话连接数限制与超时配置。Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "maxSession|session|连接数|并发"；麒麟：grep -r "maxSession\|session\|maxConnection\|并发" /opt/*/conf/*.conf 2>/dev/null。判据：应用配置了最大并发会话数限制。

（2）Web服务器层连接数限制核查：Nginx 核查 limit_conn/limit_req 配置 grep -r "limit_conn\|limit_req" /etc/nginx/；Tomcat 核查 maxThreads/acceptCount/maxConnections grep -r "maxThreads\|acceptCount\|maxConnections" server.xml。判据：Web服务器设置了连接数/并发限制参数。

【判定】应用会话与Web服务器连接数均需设置并发限制，超出阈值的连接被拒绝或延迟方为满足。

## 4.13 业务管理终端专设专用

### 4.13.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo grep -E "(Accepted|Failed)" /var/log/auth.log”查看SSH登录记录，以此判断终端设备是否专用。



### 4.13.3 通用核查方法（业务管理终端专设专用）

（1）终端用途隔离核查：核查业务管理终端是否专用于业务管理，未用于日常办公、上网等无关用途。人工查看终端已安装软件、外设使用记录。Win7/WinXP：Get-WmiObject Win32_Product | Select-Object Name；麒麟：rpm -qa 或 dpkg -l。判据：业务管理终端仅安装业务管理相关软件，无无关软件与无关用途。

（2）登录限制核查：核查业务管理终端是否仅允许授权管理人员登录，限制无关账户登录。Win7/WinXP：net localgroup Administrators、net user；麒麟：grep -E "AllowUsers|DenyUsers" /etc/ssh/sshd_config 及 cat /etc/passwd。判据：终端仅允许授权管理账户登录，无关账户已禁用或删除。

（3）专用终端清单核查：核查是否建立业务管理专用终端清单（终端编号、用途、责任人、IP/MAC）。人工查看终端管理台账。判据：存在专用终端清单，实际终端与清单一致，无未登记终端接入。

【判定】业务管理终端需用途专用、登录受限并纳入专用终端清单管理，存在与办公/上网混用或未登记终端即判不合规。

## 4.14 应具有基于用户角色的授权访问控制能力，访问控制主体的细粒度达到用户级或进程级，客体的细粒度应达到文件级、表和记录级、字段级

### 4.14.1Win7、WinXP

核查系统是否具备基于用户角色的授权访问控制能力。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统本地用户及用户组配置情况：net user、net localgroup判定标准：系统存在不同用户或用户组（角色）配置，支持基于角色进行权限区分。



核查访问控制主体细粒度是否达到用户级或进程级。在 PowerShell 中执行以下命令，查看当前运行进程及其所属用户：Get-Process | Select-Object Name,Id,StartInfo或查看进程对应用户：Get-WmiObject Win32_Process | Select-Object Name,ProcessId,@{Name="User";Expression={$_.GetOwner().User}}判定标准：系统能够识别并区分不同用户或进程的执行身份，实现用户级或进程级访问控制。



核查访问控制客体细粒度是否达到文件级。在 PowerShell 中执行以下命令，查看文件访问控制列表（ACL）：Get-Acl C:\inetpub\wwwroot | Format-List判定标准：文件或目录具备明确的访问权限配置，不同用户或角色权限区分明确。



核查访问控制客体细粒度是否达到表、记录及字段级。通过数据库管理终端登录数据库，执行以下示例命令进行核查（以常见关系型数据库为例）：SELECT user, host FROM mysql.user;SHOW GRANTS FOR CURRENT_USER;或（SQL Server 示例）：SELECT name FROM sys.database_principals;SELECT * FROM sys.database_permissions;判定标准：数据库支持对表、记录或字段进行权限控制，不同用户角色权限配置不同。

核查权限控制策略的有效性。使用低权限用户或受限角色，尝试访问受保护文件、数据表或字段。判定标准：系统能够有效阻止越权访问行为，访问控制策略执行正确。

### 4.14.2中标麒麟、银河麒麟

（1）主体细粒度（用户/进程级）

在桌面鼠标右键-打开终端，在命令行输入以下命令检查主体细粒度。

echo "- Linux用户数:$(wc -1 < /etc/passwd)"

echo"-用户组数:$(wc -1</etc/group)"

echo "- SELinux状态: $(getenforce 2>/dev/null || echo 'Disabled')"

echo "- 进程能力限制:$(sudo getcap /usr/bin/ping 2>/dev/null| wc -1)个程序有特殊能力"





（2）客体细粒度（文件级）

输入如下命令检查系统客体细粒度。

echo "- ACL支持: $(which getfacl >/dev/null 2>&1 && echo 'Yes' ||  echo 'No')"

echo "- SELinux文件标签:$(sudo 1s -Z /etc/passwd 2>/dev/nul1 | wc -1)个文件有标签"

echo "-Web目录权限:$(sudo 1s -1d /var/www 2>/dev/null | awk '{print $1}')"





### 4.14.3 通用核查方法（应用层与数据库层细粒度授权）

（1）应用层角色-权限映射核查：核查应用角色与功能/菜单/数据权限的细粒度映射。Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "role|permission|权限|映射"；麒麟：grep -r "role\|permission\|权限\|映射" /opt/*/conf/*.conf 2>/dev/null。判据：应用权限细粒度到功能/数据项级别。

（2）数据库层表/字段级授权核查：MySQL 执行 SHOW GRANTS FOR 'user'@'host';；SQL Server 执行 SELECT * FROM sys.database_permissions WHERE class=1;；PostgreSQL 执行 \dp <表名>。判据：数据库授权细粒度到表/字段级，不同角色权限区分明确。

【判定】访问控制细粒度需覆盖应用功能/数据级与数据库表/字段级，仅操作系统文件级不足以满足。

## 4.15 文电等文档专用业务处理系统应具有签名验证、密级标识等功能

### 4.15.1Win7、WinXP

核查文电等文档专用业务处理系统是否具备签名验证功能。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，对系统生成或接收的文档签名信息进行查看：Get-AuthenticodeSignature C:\文档路径\测试文件.docx。判定标准：
文档存在有效的数字签名，签名状态显示为 Valid，系统能够对签名有效性进行验证。



核查文档在业务处理过程中是否强制执行签名校验。通过业务系统操作界面，上传或处理未签名或签名无效的文档，观察系统处理结果。判定标准：系统能够识别未签名或签名无效的文档，并进行阻止或告警提示。

核查文电等文档是否具备密级标识功能。在 PowerShell 中执行以下命令，查看文档文件属性信息：Get-ItemProperty C:\文档路径\测试文件.docx或查看文件的标签/扩展属性：Get-Content C:\文档路径\测试文件.docx -Stream * 2>$null判定标准：文档文件或系统页面中存在明确的密级标识（如公开、内部、秘密、机密等）。

核查系统是否对不同密级文档实施差异化访问控制。使用不同权限用户登录系统，对不同密级文档进行访问、下载或处理操作。判定标准：系统能够依据文档密级限制访问权限，低权限用户无法访问高密级文档。

### 4.15.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令进行数字签名支持检查。

gpg  --version 2>/dev/null | head -2

which gpg 2>/dev/null && echo "GnuPG已安装" ||echo "GnuPG未安装"





接着，输入命令“ sudo sestatus -v 2>/dev/null | grep -i "mls\|policy" ”，检查SELinux MLS支持。



然后输入“sudo ls -Z /etc/passwd 2>/dev/null ”检查SELinux安全上下文中的密级，S0-S15密级由低到高。





### 4.15.3 通用核查方法（应用层签名验证与密级标识）

（1）应用层数字签名验证功能核查：核查文档业务系统是否具备对文档的数字签名验证功能。人工查看系统功能界面，上传已签名/未签名文档测试。判据：系统能验证文档签名有效性，未签名或签名无效文档被阻止或告警。

（2）应用层密级标识功能核查：核查系统是否支持对文档设置/显示密级标识（公开/内部/秘密/机密），并按密级控制访问。人工查看系统功能界面。判据：文档可设置密级标识，系统按密级实施访问控制。

【判定】文档专用业务系统需具备签名验证与密级标识功能，缺一即判不合规。

## 4.16 远程管理应采取加密保护措施

### 4.16.1Win7、WinXP

核查远程管理服务是否采用加密传输协议。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统当前开放的远程管理端口：netstat -ano | findstr ":3389"判定标准：
远程管理服务使用受控端口，且为加密远程管理服务（如 RDP）。



核查远程管理连接是否启用加密。通过远程连接方式登录系统，在 PowerShell 中执行以下命令，查看远程桌面安全配置：reg query "HKLM\SYSTEM\CurrentControlSet\Control\TerminalServer\WinStations\RDP-Tcp" /v SecurityLayer判定标准：返回值为 1 或 2，表明远程管理连接启用加密保护。



核查远程管理是否采用安全协议进行访问。针对基于 Web 的远程管理接口，在 PowerShell 中执行以下命令：Invoke-WebRequest -Uri https://localhost -Method Head -ErrorAction SilentlyContinue判定标准：远程管理接口使用 HTTPS 协议，通信过程采用加密传输。



核查系统是否禁止明文远程管理协议。通过 PowerShell 执行以下命令，检查是否启用不安全远程管理服务：netstat -ano | findstr ":23"判定标准：未启用 Telnet 等明文传输的远程管理协议。



综合判定。上述核查结果均符合判定标准时，判定远程管理已采取加密保护措施。

### 4.16.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查SSH加密配置。

sudo grep -i "Protocol" /etc/ssh/sshd_config

sudo grep -i "Ciphers\|MACs\|KexAlgorithms" /etc/ssh/sshd_config

sudo grep -i "UsePAM\|PasswordAuthentication" /etc/ssh/sshd_config





接着输入以下命令进行加密连接测试。

ssh -vvv localhost 2>&1 | grep -i "cipher\|encryption\|kex" | head -10

openssl s_client -connect localhost:443 2>/dev/null | grep -i "protocol\|cipher'





### 4.16.3 通用核查方法（应用层与数据库层远程管理加密）

（1）应用层管理界面HTTPS/TLS核查：核查应用管理界面是否启用 HTTPS/TLS。Win7/WinXP：Invoke-WebRequest -Uri https://<管理地址> -Method Head；麒麟：openssl s_client -connect <管理地址>:443。判据：管理界面使用 HTTPS/TLS 加密，无明文 HTTP 管理入口。

（2）数据库层远程连接加密核查：MySQL 执行 SHOW VARIABLES LIKE 'have_ssl'（应为 YES）；SQL Server 人工查看是否强制加密连接（ForceEncryption=1）；Oracle 人工查看 sqlnet.ora 中 SQLNET.ENCRYPTION_SERVER 配置。判据：数据库远程连接已启用加密。

【判定】应用管理界面与数据库远程连接均需启用加密，存在明文管理入口即判不合规。

## 4.17 应支持管理员、安全员、审计员三权分立的职责划分，禁止设立超级管理员，并限制管理员、安全员、审计员的数量

### 4.17.1Win7、WinXP

核查系统是否支持管理员、安全员、审计员三权分立的职责划分。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统本地用户及用户组配置情况：net localgroup判定标准：系统中存在职责区分明确的管理角色，管理员、安全员、审计员分别独立配置。



核查是否禁止设立超级管理员账号。在 PowerShell 中执行以下命令，查看是否存在权限过高或合并多重职责的账号：net user、net localgroup administrators判定标准：不存在同时具备系统管理、安全管理和审计权限的超级管理员账号。



核查管理员、安全员、审计员账号权限是否相互制约。使用不同角色账号登录系统，分别尝试执行其他角色的管理操作。判定标准：
各角色仅能执行其职责范围内的操作，无法越权执行其他角色功能。

核查管理员、安全员、审计员账号数量是否受限。在 PowerShell 中执行以下命令，统计各角色账号数量：net localgroup administrators、net localgroup "Event Log Readers"判定标准：管理员、安全员、审计员账号数量设置合理，数量受控，无异常冗余账号。





核查审计员是否独立于管理员和安全员权限。通过查看事件日志访问权限进行核查，在 PowerShell 中执行：net localgroup "Event Log Readers"判定标准：审计员具备日志审计权限，但不具备系统配置和安全策略修改权限。



综合判定。上述核查结果均符合判定标准时，判定系统支持管理员、安全员、审计员三权分立，未设立超级管理员账号，且对各类管理角色数量进行了有效限制。





### 4.17.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令进行三员账户检查。

sudo grep -E "^(admin|security|audit)" /etc/passwd

sudo grep -E ":(1001|1002|1003):" /etc/passwd

sudo grep -E "^(admin|security|audit)" /etc/group





接着输入以下命令检查是否有其他其他超级权限账户。

sudo grep -E ":0:" /etc/passwd

sudo grep -E "ALL.*ALL" /etc/sudoers | grep -v "^#"





然后输入以下命令进行权限分离检查。

sudo grep -E "^(admin|security|audit)" /etc/sudoers.d/* 2>/dev/null

sudo grep -r "pam_wheel\pam_succeed_if" /etc/pam.d/ 2>/dev/null | head -5







### 4.17.3 通用核查方法（应用层与数据库层三权分立）

（1）应用层应用内三员角色划分核查：核查应用是否内置管理员、安全员、审计员三类角色且职责分离。人工登录应用管理界面查看角色定义。判据：应用存在三员角色划分，无超级管理员角色，各角色职责独立。

（2）数据库层数据库三权账号核查：Oracle 执行 SELECT * FROM dba_users 核查 DBA/SYSDBA 与审计管理员账号分离；MySQL 核查 root 是否仅运维持有且应用不使用；SQL Server 核查 sysadmin 与审计角色分离。判据：数据库无超级管理员滥用，管理、审计账号分离。

【判定】三权分立需覆盖应用内角色与数据库账号两层，存在超级管理员或职责合并即判不合规。

## 4.18 业务管理终端登录应采取网络地址限制措施

### 4.18.1Win7、WinXP

核查业务管理终端登录是否采取网络地址限制措施。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统防火墙规则配置情况：Get-NetFirewallRule | Where-Object { $_.Enabled -eq "True" } | Select-Object DisplayName, Direction, Action判定标准：系统已配置网络访问控制规则，对业务管理终端的访问来源进行限制。



核查是否基于 IP 地址对管理登录进行访问控制。在 PowerShell 中执行以下命令，查看远程管理端口的允许访问地址范围：Get-NetFirewallPortFilter | Where-Object { $_.LocalPort -eq 3389 }、Get-NetFirewallAddressFilter判定标准：远程管理服务仅允许来自指定 IP 地址或网段的访问请求。





核查业务管理系统是否对管理功能登录来源进行限制。通过登录业务系统管理界面，查看登录策略或安全配置。判定标准管理功能仅允许来自指定网络地址的终端登录，非授权地址访问被拒绝。

核查网络地址限制措施的有效性。使用非授权网络地址尝试登录业务管理终端或管理系统。判定标准：系统能够阻止来自非授权网络地址的登录请求。

### 4.18.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查SSH访问控制。

sudo grep -E "^(AllowUsers|DenyUsers|AllowGroups|DenyGroups |AllowFrom|DenyFrom)" /etc/ssh/sshd_config

sudo grep -B2 -A2 "Match Address" /etc/ssh/sshd_config





接着输入以下命令检查防火墙规则。

sudo iptables -L -n | grep -E "(22|23|3389|5900)"





### 4.18.3 通用核查方法（应用层与网络层地址限制）

（1）应用层应用访问IP白名单核查：核查业务管理系统是否配置登录来源IP白名单。人工查看应用安全配置/登录策略。判据：应用仅允许授权网段/IP登录，非授权地址被拒绝。

（2）网络层ACL核查：核查网络设备ACL是否限制业务管理终端的访问来源。登录防火墙/交换机查看ACL规则；麒麟：sudo iptables -L -n | grep -E "22|23|3389"。判据：网络层ACL已限制管理访问来源，仅放行授权地址。

【判定】业务管理终端登录需在应用层IP白名单与网络层ACL双重限制来源地址，任一层缺失即存在风险。

## 4.19 应具有结束会话、限定登录错误次数和自动退出等登录失败处理功能

### 4.19.1Win7、WinXP

核查系统是否具备结束会话功能。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看当前登录会话情况：query user、quser判定标准：系统支持对已登录会话进行查看和管理，管理员能够对异常或空闲会话进行结束处理。

核查系统是否对登录错误次数进行限制。在 PowerShell 中执行以下命令，查看账户锁定策略配置情况：net accounts判定标准：
系统已设置登录失败次数阈值，连续登录失败超过限定次数后，账户被锁定或限制登录。

核查系统是否具备自动退出（会话超时）功能。在 PowerShell 中执行以下命令，查看会话空闲超时配置情况：reg query "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" /v InactivityTimeoutSecs或查看远程桌面会话超时策略：reg query "HKLM\SYSTEM\CurrentControlSet\Control\TerminalServer\WinStations\RDP-Tcp" /v MaxIdleTime判定标准：系统已设置会话空闲超时时间，超过规定时间未操作时自动退出登录。

核查登录失败处理功能的有效性。通过连续输入错误口令方式进行验证，并在空闲状态下观察会话自动退出情况。判定标准：系统能够按策略执行会话结束、登录错误次数限制及自动退出等登录失败处理措施。

综合判定。上述核查结果均符合判定标准时，判定系统具备结束会话、限定登录错误次数和自动退出等登录失败处理功能。

### 4.19.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查登录失败锁定。

sudo grep -E "^(auth|account). *pam_ (tally2|faillock)" /etc/pam.d/system-auth /etc/pam.d/password-auth 2>/dev/null

sudo grep-r "deny\|unlock_time\|lock_time" /etc/security/ 2>/dev/null | head -5







接着输入以下命令检查会话超时设置。

sudo grep -E "^(TMOUT|readonly TMOUT)" /etc/profile /etc/bashrc /etc/bash.bashrc 2>/dev/null

sudo grep -i "ClientAlive" /etc/ssh/sshd_config







### 4.19.3 补充核查方法（应用层登录失败处理）

【说明】登录失败处理除操作系统的账户锁定/会话超时外，还需核查应用自身的登录失败处理配置。

（1）应用层应用登录失败处理配置：

核查应用配置中登录失败次数限制/账户锁定时间/CAPTCHA after N failures（grep -r "loginFail\|lockout\|maxAttempts\|captcha" 应用conf）；核查应用会话超时配置(session-timeout/timeout参数)。

（2）功能测试：通过应用界面连续错误登录，验证是否触发账户锁定/CAPTCHA；验证空闲超时自动退出。

【判定】应用层与操作系统层均需具备登录失败处理(锁定/超时/自动退出)，任一层缺失即存在风险。

## 4.20 宜使用自主设计开发的网络服务、协议、接口等，增强应用安全

### 4.20.1Win7、WinXP

核查系统是否直接使用通用网络服务和标准协议。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统对外监听的网络服务端口：netstat -ano | findstr LISTENING判定标准：系统未直接对外暴露数据库端口、通用管理端口或高风险标准服务端口，网络服务部署具备定制化特征。



核查系统对外提供的网络接口是否为自主设计接口。通过访问业务接口地址，在 PowerShell 中执行：Invoke-WebRequest -Uri http://localhost/api -Method Options -ErrorAction SilentlyContinue判定标准：接口路径、参数格式及返回内容具有业务定制特征，未使用默认示例接口或通用测试接口。



核查系统接口通信过程中是否暴露标准协议或实现细节。在 PowerShell 中执行：Invoke-WebRequest -Uri http://localhost -Method Head -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Headers判定标准：HTTP 响应头中未暴露服务器类型、框架名称、协议实现版本等敏感技术信息。



核查系统是否对接口访问实施身份鉴别与访问控制。通过构造未携带认证信息的接口请求进行验证，在 PowerShell 中执行：Invoke-WebRequest -Uri http://localhost/api/test -ErrorAction SilentlyContinue判定标准：未授权请求被拒绝，接口无法匿名访问，接口调用需通过系统定义的鉴权机制。



核查系统是否限制通用调试接口和默认接口。在 PowerShell 中执行以下命令，检查常见默认接口路径：Invoke-WebRequest -Uri http://localhost/swagger -ErrorAction SilentlyContinue、Invoke-WebRequest -Uri http://localhost/actuator -ErrorAction SilentlyContinue。判定标准：通用调试接口、默认管理接口未对外开放或已关闭访问。

综合判定。上述核查结果符合判定标准时，判定系统通过采用自主设计开发的网络服务、协议或接口，并结合必要的访问控制措施，有效增强了应用系统安全性。

### 4.20.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo systemctl list-unit-files | grep -i "kylin\|neokylin\|yongyou\|wps\|kingsoft" ”检查麒麟特有服务。



接着输入“sudo find /etc -name "*.conf" -type f | xargs grep -l "sm2\|sm3\|sm4\|gmssl" 2>/dev/null”检查协议配置文件，紧接着输入“sudo systemctl list-units | grep -i "gmssl\|sm\|国密" 2>/dev/null”检查国密相关服务。



然后输入以下命令检查国产接口。

sudo find /usr -name "*api*.so" -o -name "*sdk*.so" 2>/dev/null | grep -i "kylin\|china" | head -5

sudo find /usr -name "*midware*" -o -name "*middleware*" 2>/dev/null | head -5



### 4.20.3 通用核查方法（应用层自研协议与接口）

（1）应用层自研协议/接口核查：核查应用对外接口/协议是否自主设计（接口路径、参数格式、通信协议定制化），未直接暴露通用框架默认接口。Win7/WinXP：Invoke-WebRequest -Uri http://localhost/swagger -Method Head、Invoke-WebRequest -Uri http://localhost/actuator -Method Head（默认接口应 404）；麒麟：curl -I http://localhost/swagger。判据：无通用框架默认接口暴露，接口具有业务定制特征。

（2）应用层自定义协议核查：核查应用是否使用自研协议/国密算法（sm2/sm3/sm4）。麒麟：find /etc -name "*.conf" | xargs grep -l "sm2\|sm3\|sm4\|gmssl" 2>/dev/null。判据：应用采用自研或国产密码协议，未完全依赖通用标准服务。

【判定】应用应尽量采用自主设计开发的网络服务/协议/接口，暴露通用框架默认接口或调试接口即判不合规。

## 4.21 应具有基于专用物理部件或生物特征多因素的、与JD密码算法相结合的数字证书用户身份认证功能

### 4.21.1Win7、WinXP

核查系统是否具备基于数字证书的用户身份认证功能。Windows 搜索框中输入PowerShell右键管理员身份打开并回车，在 PowerShell 中执行以下命令，查看本机用户证书存储情况：certutil -store My判定标准：系统存在用户数字证书，认证过程中使用数字证书进行身份鉴别。



核查数字证书是否结合专用物理部件进行身份认证。通过查看证书密钥存储方式，在 PowerShell 中执行：certutil -store -user My并检查证书“私钥”属性是否标识为硬件保护（如智能卡、USB Key、密码模块）。判定标准：用户证书私钥存储于专用物理介质或密码硬件中，非纯软件方式保存。



核查系统是否支持基于生物特征的身份认证方式。通过查看系统生物特征设备及配置情况进行核查，在 PowerShell 中执行：Get-WmiObject Win32_BiometricDevice判定标准：系统支持指纹、人脸等生物特征识别设备，并可与用户身份认证机制结合使用。

核查数字证书认证过程中是否采用符合要求的密码算法。通过查看证书算法信息，在 PowerShell 中执行：certutil -dump或针对指定证书文件执行：certutil -dump C:\证书路径\user.cer判定标准：证书所采用的密码算法符合系统安全要求，支持与 JD 密码算法或相关密码算法体系结合使用。



核查多因素身份认证机制的有效性。通过模拟仅使用单一因素（如口令或证书）的登录方式，观察系统处理结果。判定标准：系统要求同时满足数字证书与专用物理部件或生物特征等多因素条件，单一因素无法完成身份认证。

综合判定。上述核查结果均符合判定标准时，判定系统具备基于专用物理部件或生物特征的多因素数字证书用户身份认证能力，且与相关密码算法体系相结合，实现了增强型身份鉴别安全控制。

### 4.21.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo grep -r "pam_fprintd\|pam_biometric" /etc/pam.d/ 2>/dev/null ”检查PAM生物识别模块。



随后输入“openssl ecparam -list_curves 2>/dev/null | grep -i "sm2" ”检查SM2证书支持。



### 4.21.3 通用核查方法（应用层数字证书认证）

（1）应用层应用证书认证配置核查：核查应用是否配置基于数字证书的身份认证（客户端证书/双向TLS/证书登录）。人工查看应用认证配置；麒麟：grep -r "clientAuth\|sslVerify\|证书\|client_cert" /opt/*/conf/*.conf 2>/dev/null。判据：应用启用数字证书认证，用户需持有效证书方可登录。

（2）应用层多因素结合核查：核查应用证书认证是否结合生物特征或专用物理部件（UKey）。人工查看认证模块配置。判据：应用认证结合证书与物理部件/生物特征等多因素。

【判定】应用需启用基于数字证书的用户身份认证并与专用物理部件/生物特征结合，仅口令认证即判不合规。

## 4.22 应更改Web应用系统默认服务发布端口

### 4.22.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo netstat +-tunlp”查找正在监听的Web端口。



### 4.22.3 通用核查方法（更改Web应用系统默认服务发布端口）

（1）Web服务器层Nginx/Tomcat 默认端口更改核查：核查 Web 服务器是否更改默认发布端口。麒麟：grep -E "listen" /etc/nginx/nginx.conf /etc/nginx/conf.d/*.conf（应非80）；Tomcat 核查 server.xml 中 Connector port（应非8080）grep -E "port=" /opt/tomcat/conf/server.xml；Win7/WinXP：Select-String -Path C:	omcat\conf\server.xml -Pattern "port="。判据：Web 服务发布端口已从默认 80/8080 更改为自定义端口。

（2）端口开放核查：核查当前监听端口与默认端口对比。Win7/WinXP：netstat -ano | findstr LISTENING；麒麟：sudo ss -tlnp。判据：Web 服务监听端口已偏离默认端口，无默认端口对外发布。

【判定】Web应用与数据库服务均需更改默认服务发布端口，仍使用默认端口对外发布即判不合规。

## 4.23 应分开设置管理端口与应用端口

### 4.23.1Win7、WinXP

核查系统是否分开设置管理端口与应用端口。Windows 搜索框中输入PowerShell右键管理员身份打开并回车，在 PowerShell 中执行以下命令，查看系统当前监听的网络端口情况：netstat -ano | findstr LISTENING判定标准：系统管理服务端口与业务应用服务端口配置独立，端口号不同，未混用同一端口提供管理和业务功能。



核查管理服务端口配置情况。通过查看远程管理、运维接口或管理后台监听端口，在 PowerShell 中执行：netstat -ano | findstr 3389或针对 Web 管理端口执行：netstat -ano | findstr :80netstat -ano | findstr :443判定标准：管理端口采用专用端口号，未与业务访问端口共用。

核查应用服务端口配置情况。通过查看业务系统对外提供服务的端口，在 PowerShell 中执行：netstat -ano | findstr :80、netstat -ano | findstr :443判定标准：业务应用端口仅用于提供业务访问服务，不承载系统管理或配置功能。

核查管理端口与应用端口的访问控制情况。在 PowerShell 中执行以下命令，查看防火墙策略配置：Get-NetFirewallRule | Where-Object { $_.Enabled -eq "True" }判定标准：管理端口访问范围受限，仅允许授权网络地址访问；应用端口面向业务访问，策略区分明确。

综合判定。上述核查结果均符合判定标准时，判定系统已分开设置管理端口与应用端口，管理接口与业务接口相互独立。

### 4.23.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令查看端口情况。

echo "=== 管理端口常见 ==="

sudo ss -tlnp | grep -E ':(22|2222|3000|8443|9090|8081)'

echo -e "\n===应用端口常见 ==="

sudo ss -tlnp | grep -E ':(80|443|3306|5432|6379|8080|9000)'

echo -e "\n=== 防火墙规则 ==="

sudo firewall-cmd --list-ports 2>/dev/null ||echo "firewalld未运行"





### 4.23.3 通用核查方法（Web服务器层与数据库层管理端口分离）

（1）Web服务器层Nginx/Tomcat 管理端口与应用端口分离核查：核查管理端口（如8443/9090/管理后台）与应用端口（80/443/8080）是否分离。麒麟：sudo ss -tlnp | grep -E ':(80|443|8080|8443|9090)'；Tomcat 核查 server.xml 中 Shutdown 端口/管理端口与 Connector 端口不同。判据：管理端口与应用端口分离，未共用同一端口。

（2）数据库层数据库管理端口核查：MySQL 执行 SHOW VARIABLES LIKE 'port'，核查管理连接与应用连接是否区分（专用管理端口/管理网段）；SQL Server 人工查看管理端口配置。判据：数据库管理入口与业务访问分离。

【判定】Web服务器与数据库的管理端口需与应用/业务端口分开设置，混用同一端口即判不合规。

## 4.24 应用服务和数据存储应部署在不同的服务器上

### 4.24.1Win7、WinXP

核查应用服务与数据存储是否部署在不同的服务器上。Windows 搜索框中输入 PowerShell 并回车，在应用服务器上执行以下命令，查看本机运行的服务及监听端口情况：netstat -ano | findstr LISTENING判定标准：应用服务器仅监听业务应用相关端口，未监听数据库等数据存储服务端口。



核查应用服务器是否部署数据库服务。在 PowerShell 中执行以下命令，查看是否存在常见数据库进程：Get-Process | Where-Object { $_.Name -match "sql|mysql|oracle|mongo" }判定标准：应用服务器上未运行数据库相关进程。



核查数据库服务器是否独立部署。登录数据库服务器，在 PowerShell 中执行以下命令，查看本机监听端口：netstat -ano | findstr LISTENING判定标准：数据库服务器仅监听数据库服务端口，未运行或监听业务应用服务端口。



核查应用服务与数据库的访问关系。在应用服务器上执行以下命令，查看数据库连接配置：netstat -ano | findstr ESTABLISHED判定标准：应用服务通过网络方式访问远程数据库服务器，数据库未与应用部署在同一主机。



核查应用服务器与数据库服务器的网络地址。在两台服务器分别执行以下命令：hostname、ipconfig判定标准：应用服务器与数据库服务器主机名、IP 地址不同，物理或虚拟服务器相互独立。



综合判定。上述核查结果均符合判定标准时，判定应用服务和数据存储已部署在不同的服务器上，实现了应用与数据的物理或逻辑隔离。

### 4.24.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查应用服务和数据存储的部署情况。

echo "===数据库连接检查==="

sudo netstat -tnp 2>/dev/null | grep -E ':3306|:5432|:6379' | grep ESTAB

echo -e"\n===配置文件中的远程IP==="

grep -r "host\s*=" /etc/my.cnf /etc/mysql/*.cnf /opt/*/conf/*.conf 2>/dev/null | grep -v "127.0.0.1\|localhost"

echo -e "\n=== 存储挂载点 ==="

mount | grep -E ":/|nfs | ceph | gluster"

echo -e "\n===关键进程监听 ==="

sudo ss -tlnp | grep -E ':3306|:5432|:6379|:27017'





### 4.24.3 通用核查方法（数据库连接地址核查）

（1）数据库层数据库连接地址核查：核查应用配置中的数据库连接地址是否指向独立数据库服务器（非本机/非应用服务器）。Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "jdbc|DataSource|Server=|Data Source="；麒麟：grep -r "jdbc\|host\s*=\|Server=" /opt/*/conf/*.conf 2>/dev/null | grep -v "127.0.0.1\|localhost"。判据：应用连接地址指向独立数据库服务器，未与应用同机部署。

【判定】应用服务与数据存储需部署在不同服务器，数据库连接地址指向独立主机方为满足。

## 4.25 应具有对所有访问行为和管理行为的日志审计功能，支持多组合查询检索，可读性强，具有解释和展示功能，审计日志应只是保留180d

### 4.25.1Win7、WinXP

核查系统是否具备对所有访问行为和管理行为的日志审计功能。Windows 搜索框中输入 PowerShell右键管理员身份打开并回车，在 PowerShell 中执行以下命令，查看系统审计策略配置情况：auditpol /get /category:*判定标准：系统已启用登录、对象访问、权限使用、系统管理等相关审计策略，能够记录访问行为和管理行为。



核查系统是否实际记录访问行为和管理操作日志。通过查看安全日志内容进行核查，在 PowerShell 中执行：Get-WinEvent -LogName Security -MaxEvents 20判定标准：日志中包含用户登录、登录失败、权限变更、管理操作等事件记录，日志内容完整。



核查审计日志是否支持多条件组合查询检索。在 PowerShell 中执行以下命令，对日志按事件类型和时间进行组合筛选：Get-WinEvent -LogName Security | Where-Object { $_.Id -eq 4624 -or $_.Id -eq 4625 } | Select-Object TimeCreated,Id,Message判定标准：系统支持按时间、事件类型、用户等多条件方式对审计日志进行组合查询检索。



核查审计日志内容的可读性、解释性和展示能力。通过查看日志事件详细信息进行核查，在 PowerShell 中执行：Get-WinEvent -LogName Security -MaxEvents 1 | Format-List *判定标准：日志字段含义明确，事件内容可解释，能够清晰展示访问和管理行为过程。



核查审计日志保存期限配置情况。在 PowerShell 中执行以下命令，查看日志保留及覆盖策略：wevtutil gl Security重点查看以下字段：
maximumSizeInBytes、retention、autoBackupLogFiles判定标准：日志保留策略配置合理，能够保证审计日志保存时间不少于 180 天，日志未被提前覆盖或删除。



综合判定。上述核查结果均符合判定标准时，判定系统具备对所有访问行为和管理行为的日志审计能力，支持多条件组合查询，日志内容清晰可读、可解释，并满足审计日志至少保留 180 天的要求。

### 4.25.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“sudo systemctl status auditd 2>/dev/null || sudo systemctl status rsyslog 2>/dev/null”检查审计服务状态。



接着输入“ls -lh /var/log/auth.log 2>/dev/null ”检查日志文件。



随后，输入“grep -r "rotate\|maxage\|180" /etc/logrotate.d/ /etc/logrotate.conf 2>/dev/null | grep -i audit”检查日志轮转策略。



然后输入以下命令检查查询工具是否可用。

which ausearch 2>/dev/null && echo "ausearch:可用"

which aureport 2>/dev/null && echo "aureport:可用"





### 4.25.3 补充核查方法（应用层与数据库层日志审计）

【说明】本条标题写“所有访问行为和管理行为”，除已核查的系统审计(auditd/事件日志)外，还需核查应用访问日志和数据库审计日志及其180天留存。

（1）应用层应用访问日志核查：

核查应用访问日志是否存在且记录访问行为：ls -lh /var/log/nginx/access.log；Get-ChildItem D:\app\logs -Filter *access*.log。核查应用日志保留≥180天：grep -r "rotate\|maxage\|180" /etc/logrotate.d/* | grep -i "nginx\|tomcat\|app"。

（2）数据库层数据库审计日志核查：

Oracle：SELECT * FROM dba_audit_trail；MySQL：SHOW VARIABLES LIKE 'general_log%'；SQL Server：SELECT * FROM sys.server_audits WHERE is_enabled=1。核查数据库审计日志保留时间。

（3）多组合查询能力：核查日志是否支持按时间/事件类型/用户多条件组合查询检索(ausearch/aureport/Get-WinEvent筛选)。

【判定】系统审计、应用访问日志、数据库审计日志均需启用且保留≥180天，支持多组合查询，方为满足。

## 4.26 应经过代码级安全漏洞挖掘

### 4.26.1Win7、WinXP

核查应用系统是否经过代码级安全漏洞挖掘通过查看代码安全检测工具执行记录、检测报告及整改材料进行核查。

核查是否具备代码级安全漏洞挖掘工具使用记录，在 Windows 系统中，通过 PowerShell右键管理员身份打开查看是否留存代码安全扫描结果文件：Get-ChildItem -Path D:\ -Recurse -Include *scan*,*audit*,*sonar*,*code* -ErrorAction SilentlyContinue判定标准：
存在代码安全检测报告或扫描结果文件。

核查代码级安全漏洞挖掘报告内容，通过人工查看检测报告，核查其内容是否包含以下要素：检测工具名称及版本、检测时间、检测范围（源代码目录或模块）、漏洞类型及风险等级。判定标准：报告内容完整，具备代码级安全漏洞挖掘结论。

核查漏洞整改与复测情况，通过查看漏洞整改说明、代码提交记录或复测报告进行核查。判定标准：发现的高危、中危漏洞已完成修复或采取有效缓解措施，并通过复测验证。

核查代码级安全漏洞挖掘过程的规范性，通过查看制度文件或开发流程说明进行核查。判定标准：系统在上线前或版本发布前，已纳入代码级安全漏洞挖掘流程。

### 4.26.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“find /opt /usr/local /home -name "*.py" -o -name "*.sh" 2>/dev/null | xargs grep -l "scan\|test\|audit\|fuzz\|exploit" 2>/dev/null ”检查安全测试痕迹。



接着输入命令“find /var/log /opt /root /home -name "*审计*" -o -name "*报告*" -o -name "*scan*" 2>/dev/null ”检查代码审计报告。



然后输入“grep -i "CVE\|漏洞\|修复\|patch" /var/log/yum.log /var/log/dpkg.log 2>/dev/null | tail -5”查看最近的安全漏洞/修复相关的记录。



### 4.26.3 通用核查方法（应用层SAST/DAST与代码审计流程）

（1）应用层SAST/DAST工具扫描核查：核查是否使用静态/动态应用安全测试工具（SAST 如 SonarQube/Fortify，DAST 如 AWVS/Burp）进行代码扫描。核查扫描报告留存。Win7/WinXP：Get-ChildItem -Path D:\ -Recurse -Include *scan*,*sonar*,*fortify* -ErrorAction SilentlyContinue；麒麟：find /opt /home -name "*scan*" -o -name "*报告*" 2>/dev/null。判据：存在SAST/DAST扫描报告，覆盖业务代码。

（2）应用层代码审计流程核查：核查是否建立代码审计制度，上线/发版前经过人工代码审计与漏洞整改复测。人工查看审计记录与整改报告。判据：代码审计纳入开发流程，高危漏洞已整改并复测通过。

【判定】应用需经过SAST/DAST工具扫描与人工代码审计流程，无扫描报告或未整改高危漏洞即判不合规。

## 4.27 检查是否具备对人机接口输入、网络通信输入、文件输入的数据进行格式和长度检查的功能

### 4.27.1Win7、WinXP

核查系统是否具备对人机接口输入数据进行格式和长度检查功能。通过在业务系统人机交互界面输入异常数据进行验证。在 Windows 系统中，使用 PowerShell 右键管理员身份打开构造超长及非法格式输入请求：Invoke-WebRequest -Uri "http://localhost/login?username=$( 'a' * 5000 )&password=test" -ErrorAction SilentlyContinue判定标准：系统能够识别异常长度或非法格式输入，拒绝请求或给出合理提示，未发生异常。



核查系统是否具备对网络通信输入数据进行格式和长度检查功能。在 PowerShell 中执行以下命令，构造异常网络请求数据：Invoke-WebRequest -Uri "http://localhost/api/test?id=$( '1' * 10000 )" -ErrorAction SilentlyContinue判定标准：系统对异常长度或格式的网络通信输入进行校验处理，未导致服务异常或信息泄露。



核查系统是否具备对文件输入数据进行格式和长度检查功能。通过上传异常文件进行验证。在 PowerShell 中构造大文件或非法格式文件上传请求（示例）：Invoke-WebRequest -Uri http://localhost/upload -Method Post -InFile C:\Windows\System32\drivers\etc\hosts -ContentType "application/octet-stream" -ErrorAction SilentlyContinue判定标准：系统能够对文件类型、大小等进行校验，拒绝非法文件或超出限制的文件上传。



核查输入校验机制的统一性和有效性。通过多次输入异常数据进行交叉验证。判定标准：系统在各类输入场景下均实施统一的格式和长度校验机制，不因输入来源不同而失效。

综合判定。上述核查结果均符合判定标准时，判定系统具备对人机接口输入、网络通信输入及文件输入数据进行格式和长度检查的安全控制功能。

### 4.27.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查文件上传限制。

find /etc -name "*.conf" -exec grep -1 "upload_max_filesize\|post_max_size" {} \; 2>/dev/null

grep -r "file. *size\|upload. *limit" /etc/php* /etc/httpd* 2>/dev/null







然后输入命令“find /opt /usr/local -name "*.conf" -o -name "*.xml" -o -name "*.properties" 2>/dev/null | xargs grep -l "valid\|check\|sanitize" 2>/dev/null”检查应用程序输入检查。



### 4.27.3 通用核查方法（应用层前端与后端双重校验）

（1）应用层前端校验核查：核查前端（页面/客户端）是否对输入格式、长度、字符类型进行校验。人工查看页面输入限制。判据：前端已做格式与长度校验。

（2）应用层后端校验核查：核查后端是否对输入独立进行格式与长度校验（不依赖前端）。Win7/WinXP：Select-String -Path D:\app\src\* -Pattern "validate|校验|length|正则"；麒麟：grep -rnE "validate|校验|length|regex" /opt/app/src/ 2>/dev/null。判据：后端独立校验输入格式与长度，前端校验可被绕过时后端仍能拦截。

【判定】输入格式与长度检查需前端+后端双重校验，仅前端校验可被绕过即判不合规。

## 4.28 检查是否能够有效检测并防御SQL注入、网页篡改、跨站脚本、拒绝服务等应用层攻击

### 4.28.1Win7、WinXP

核查系统是否能够有效检测并防御 SQL 注入攻击。通过构造典型 SQL 注入请求进行验证。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell右键管理员身份回车中执行：Invoke-WebRequest -Uri "http://localhost/login?username=admin'or'1'='1&password=test"-ErrorAction SilentlyContinue判定标准：系统未返回数据库错误信息，未绕过正常认证流程，请求被拒绝或记录为攻击行为。



核查系统是否能够有效检测并防御跨站脚本（XSS）攻击。在 PowerShell 中执行以下命令，向应用注入脚本代码：Invoke-WebRequest-Uri"http://localhost/search?q=<script>alert(1)</script>" -ErrorAction SilentlyContinue判定标准：返回页面未执行脚本，脚本内容被过滤或转义，攻击行为被拦截或记录。



核查系统是否能够防御网页篡改攻击。通过查看网站目录权限及文件完整性进行核查，在 PowerShell 中执行：Get-Acl C:\inetpub\wwwroot | Format-List并尝试使用非授权账户修改页面文件。判定标准：非授权用户无法修改网页文件，系统具备网页篡改防护或完整性校验机制。



核查系统是否能够检测并防御拒绝服务（DoS）攻击。通过模拟短时间内大量请求进行验证，在 PowerShell 中执行：

1..200 | ForEach-Object {

    Start-Job { Invoke-WebRequest -Uri http://localhost -UseBasicParsing }

}判定标准：系统在高频访问情况下仍保持稳定运行，对异常请求进行限制或丢弃，未出现服务不可用情况。



核查应用层攻击的检测与告警情况。通过查看安全日志或防护设备日志进行核查，在 PowerShell 中执行：Get-WinEvent -LogName Security -MaxEvents 50或查看应用/安全防护组件日志。判定标准：
攻击行为被记录，日志中可追溯攻击类型、时间及来源信息。



综合判定。上述核查结果均符合判定标准时，判定系统能够有效检测并防御 SQL 注入、网页篡改、跨站脚本、拒绝服务等常见应用层攻击。

### 4.28.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“ curl -I http://目标URL | grep -iE "(X-Content-Type|X-Frame|CSP)" ”，检查HTTP安全头，期望看到如下结果。



接着输入“ curl -s "$URL' OR '1'='1" | grep -i "error\|sql\|mysql" ”检测SQL注入漏洞，无返回值表示已防护，处于安全状态。

### 4.28.3 通用核查方法（应用层四类攻击防御）

（1）应用层SQL 注入防御核查：核查应用使用参数化查询/预编译语句而非字符串拼接 SQL。Win7/WinXP：Select-String -Path D:\app\src\* -Pattern "Statement|拼接"；麒麟：grep -rn "Statement\|concat\|拼接" /opt/app/src/ 2>/dev/null。判据：未发现动态拼接 SQL，使用参数化/预编译。

（2）应用层网页篡改防御核查：核查网页防篡改软件/WAF 是否覆盖网站目录并具备篡改告警与自动恢复（参见 4.8）。判据：具备网页防篡改检测与恢复能力。

（3）应用层跨站脚本（XSS）防御核查：核查应用对输出是否做 HTML/JS 编码转义，WAF 是否启用 XSS 检测规则（参见 4.9）。判据：输出已编码转义，XSS 攻击被拦截。

（4）应用层拒绝服务（DoS）防御核查：核查应用层限流、连接数限制与抗D设备配置（参见 4.5）。Nginx 核查 limit_req/limit_conn；Tomcat 核查 maxThreads/acceptCount。判据：具备应用层限流与连接数限制，能抵御常见 DoS/CC 攻击。

【判定】应用需在代码层参数化查询、数据库层最小权限，任一层缺失即存在SQL注入风险。

## 4.29 检查是否具有用户访问权限统一管理功能

### 4.29.1Win7、WinXP

核查系统是否具有用户访问权限统一管理功能。Windows 搜索框中输入 PowerShell右键管理员身份运行并回车，在 PowerShell 中执行以下命令，查看系统用户及角色配置情况：net user、net localgroup判定标准：系统通过统一的用户或角色管理机制对用户访问权限进行集中管理。





核查用户访问权限是否通过统一策略进行配置。通过查看用户所属角色或权限分配情况进行核查，在 PowerShell 中执行：net user 用户名，判定标准：用户访问权限基于统一角色或策略进行授权，未分散配置。

核查用户权限变更是否集中受控。通过新增、修改或删除用户权限进行验证。判定标准：用户访问权限变更需通过统一管理接口或统一策略配置，变更过程可控、可追溯。

综合判定。上述核查结果符合判定标准时，判定系统具备用户访问权限统一管理功能。

### 4.29.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查集中授权管理。

ls -la /etc/pam.d/sssd /etc/sssd/sssd.conf 2>/dev/null

grep -r "sudoers" /etc/sssd /etc/pam.d 2>/dev/null







接着输入如下命令，检查权限管理工具。

which authconfig 2>/dev/null && echo "authconfig 存在"

which sssctl 2>/dev/null && echo "sssctl 存在"



### 4.29.3 补充核查方法（应用层统一权限管理平台）

【说明】用户访问权限统一管理除操作系统的SSSD/本地用户外，还需核查应用层是否部署统一认证/权限平台(IAM/CAS/OAuth2.0)。

（1）应用层统一权限管理平台核查：

核查是否部署统一认证平台(IAM/CAS/OAuth2.0/SSO)：Get-Service | Where-Object {$_.Name -match "iam\|cas\|oauth\|sso\|auth"}；systemctl list-units | grep -iE "iam\|cas\|oauth\|sso\|auth"。核查应用是否对接统一认证：grep -r "sso\|cas\|oauth\|auth.server\|iam\|统一认证" /opt/*/conf/*.conf。

（2）权限变更集中受控：核查应用权限是否通过统一策略配置(而非分散在各应用)；核查权限变更审批流程和操作日志。

【判定】应用接入统一认证/权限平台、权限变更集中受控可追溯，方为满足统一管理要求。

## 4.30 检查是否具备统一管理措施，是否对远程管理进行限制

### 4.30.1Win7、WinXP

核查系统是否具备统一管理措施。Windows 搜索框中输入 PowerShell 并回车，在 PowerShell 中执行以下命令，查看系统用户及管理角色配置情况：net user、net localgroup判定标准：系统通过统一的用户或角色管理机制对管理权限进行集中配置和控制。





核查远程管理是否纳入统一管理范围。通过查看远程管理服务配置情况进行核查，在 PowerShell 中执行：netstat -ano | findstr LISTENING判定标准：远程管理服务端口明确，纳入统一管理和安全控制策略。



核查是否对远程管理进行访问限制。在 PowerShell 中执行以下命令，查看远程管理相关防火墙规则：Get-NetFirewallRule | Where-Object { $_.Enabled -eq "True" }、Get-NetFirewallPortFilter | Where-Object { $_.LocalPort -eq 3389 }判定标准：远程管理服务仅允许来自授权网络地址的访问，未对外开放。

核查远程管理限制措施的有效性。使用非授权网络地址尝试进行远程管理访问。判定标准：系统能够阻止来自非授权地址的远程管理连接请求。

综合判定。上述核查结果均符合判定标准时，判定系统具备统一管理措施，并对远程管理行为进行了有效限制。

### 4.30.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查集中管理平台。

ps aux | grep -E "(cockpit|webmin|ansible|salt|puppet)"|grep -v grep

sudo netstat -tlnp | grep -E ':9090|:10000|:4433|:8140'





接着输入以下命令检查远程管理限制。

grep -E "AllowUsers |DenyUsers|AllowGroups|DenyGroups" /etc/ssh/sshd_config

sudo iptables -L -n 2>/dev/null | grep -E "DROP.*:22|REJECT.*:22"

sudo firewall-cmd --list-all 2>/dev/null | grep -E "ssh|22"





### 4.30.3 通用核查方法（应用层管理面限制）

（1）应用层管理面限制核查：核查应用管理功能是否纳入统一管理平台，管理入口是否限制来源地址并禁止公网开放。人工查看应用管理配置与管理平台策略。判据：应用管理面纳入统一管理，管理入口受限。

【判定】应用需具备统一管理措施并对远程管理进行限制，管理入口对外无限制即判不合规。

## 4.31 检查是否能够设置最大并发会话连接数、会话建立速率、单用户并发会话数

### 4.31.1Win7、WinXP

核查系统是否能够设置最大并发会话连接数。Windows 搜索框中输入 PowerShell右键管理员身份运行并回车，在服务器上执行以下命令，查看当前系统并发连接情况：Get-NetTCPConnection -State Established | Measure-Object | Select-Object -ExpandProperty Count并查看应用或服务器并发连接配置参数。判定标准：系统支持对最大并发会话连接数进行配置，超出阈值的连接被限制或拒绝。

核查系统是否能够限制会话建立速率。通过短时间内构造大量连接请求进行验证，在 PowerShell 中执行：

1..100 | ForEach-Object {

    Start-Job { Invoke-WebRequest -Uri http://localhost -UseBasicParsing }

}判定标准：系统对会话建立速率进行控制，异常高频连接请求被延迟、限制或丢弃。



核查系统是否能够限制单用户并发会话数。通过同一用户在多个终端或会话中同时登录系统进行验证。并在 PowerShell 中查看会话情况：Get-CimInstance Win32_LogonSession判定标准：同一用户并发会话数量受到限制，超过阈值的会话无法建立或被强制终止。



核查并发会话控制策略的有效性。结合系统配置及实际测试结果进行综合验证。判定标准：最大并发会话数、会话建立速率及单用户并发会话数均可配置，且控制策略能够生效。

综合判定。上述核查结果均符合判定标准时，判定系统能够设置最大并发会话连接数、会话建立速率及单用户并发会话数。

### 4.31.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入以下命令检查SSH会话限制配置。

grep -E "MaxSessions|MaxStartups|MaxAuthTries" /etc/ssh/sshd_config

sshd -T 2>/dev/null | grep -E "maxsessions|maxstartups|maxauthtries"







接着输入以下命令检查系统级限制。



然后输入“grep -r "maxlogins\|maxsyslogins" /etc/security/limits.conf /etc/security/limits.d/ 2/dev/null ”检查PAM会话限制。



### 4.31.3 通用核查方法（应用层与Web服务器层并发会话）

（1）应用层并发会话配置核查：核查应用最大并发会话数、会话建立速率、单用户并发会话数配置。Win7/WinXP：Select-String -Path D:\app\conf\* -Pattern "maxSession|maxConnection|rate|并发"；麒麟：grep -r "maxSession\|maxConnection\|rate\|并发" /opt/*/conf/*.conf 2>/dev/null。判据：应用可配置并限制并发会话数/速率/单用户会话数。

（2）Web服务器层Nginx limit_conn 核查：核查 Nginx 并发连接限制配置 grep -r "limit_conn\|limit_req\|limit_rate" /etc/nginx/。判据：Web服务器已配置连接数/速率限制。

【判定】最大并发会话数、会话建立速率、单用户并发会话数均需可配置且生效，任一项不可控即判不合规。

## 4.32 检查所有应用是否具备备份与恢复功能

### 4.32.1Win7、WinXP

### 4.32.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“find /opt /etc /root -name "*备份*" -o -name "*backup*" -type f 2>/dev/null”检查备份策略文档。



### 4.32.3 补充核查方法（应用层与数据库层备份恢复）

【说明】本条核查所有应用是否具备备份与恢复功能，除4.32.1/4.32.2的方法外，需核查应用自身备份配置、数据库备份恢复能力及实际恢复验证。

（1）应用层应用备份配置：

核查应用配置文件中backup.path/backup.schedule参数：grep -r "backup\|备份\|dump" /opt/*/conf/*.conf；查看应用管理界面是否有备份/恢复功能模块。

（2）数据库层数据库备份与恢复：

MySQL：核查mysqldump定时任务 crontab -l | grep mysqldump；查binlog mysql -e "SHOW VARIABLES LIKE 'log_bin'"。SQL Server：SELECT * FROM msdb.dbo.backupset ORDER BY backup_finish_date DESC。Oracle：RMAN> list backup summary。

（3）恢复能力验证：核查恢复脚本存在：find /opt /root -name "*restore*" -o -name "*recover*"；选取最近备份在测试环境执行恢复操作确认可恢复。

【判定】应用自身备份功能、数据库备份策略、OS定时备份任务任一层有效且经恢复验证可恢复，方为满足。仅存在备份文件未经恢复验证不能直接判合规。

## 4.33 检查应用软件是否基于国产自主可控软硬件自主开发

### 4.33.2中标麒麟、银河麒麟

在桌面鼠标右键-打开终端，在命令行输入命令“cat /etc/os-release | grep -E "NAME|VERSION|ID" ” “uname -a”检查操作系统信息。



接着输入“dpkg -l 2>/dev/null | grep -v "ubuntu\|debian" ”检查软件包来源。



然后输入“find /usr/share/doc -name "copyright" -o -name "LICENSE" 2>/dev/null | xargs grep -l "GPL\|Apache\|MIT" ”检查开源软件标识。



### 4.33.3 通用核查方法（国产自主可控软硬件）

（1）软件清单与名录核查：核查操作系统、数据库、中间件、应用是否来自《JY关键软硬件自主可控产品名录》。麒麟：cat /etc/os-release 查看系统信息、cat /proc/version、dpkg -l 或 rpm -qa 列出软件包；Win7/WinXP：人工核对软件清单。判据：所用软件均列入国产自主可控产品名录，无名录外依赖。

（2）国产化率核查：核查软硬件国产化率（国产 CPU/操作系统/数据库/中间件/应用的占比）。人工统计软件清单与硬件清单，计算国产化率。判据：国产化率达到规定比例，关键软硬件均国产。

（3）供应商资质核查：核查软硬件供应商是否具备自主可控相关资质/证书（自主知识产权、资质认证）。人工查看供应商资质文件与授权证明。判据：供应商具备自主可控资质，产品具有自主知识产权。

【判定】应用软件需基于国产自主可控软硬件自主开发，所用操作系统/数据库/中间件/应用均来自国产自主可控名录且供应商具备资质，存在名录外非自主可控依赖即判不合规。

## 5 网络安全

## 目前无网络设备

## 6 物理安全

## 6.1 使用的网络设备、服务器、终端等，应选用进入《全J计算机及网络设备集中采购目录》的产品

设备范围确认，对信息系统中使用的设备进行全面梳理，明确核查范围，重点包括网络设备（如交换机、防火墙、路由器等）、服务器以及终端设备（台式机、笔记本及专用终端）。通过形成完整的设备清单，确保系统内所有计算、网络及终端设备均纳入核查范围，不存在遗漏设备。

采购来源核查，对设备清单中所列设备逐一核查其采购来源，重点确认设备是否通过军队集中采购渠道取得，包括通过军队集中采购平台采购、由上级单位统一集中采购后配发，或由具备军队采购资格的供应商供货。凡通过上述渠道取得的设备，视为符合全军集中采购相关要求。

采购文件合规性核查，查阅设备对应的采购合同、招标文件或采购需求说明，核实采购文件中是否明确要求所供产品应符合《全军用计算机及网络设备集中采购目录》相关规定。如采购文件中已明确相关要求，则可确认设备采购过程符合集中采购目录管理要求。

供应商资质核查，对设备供货单位资质进行核查，确认其是否为军队集中采购平台合格供应商或军队集中采购项目中标单位。具备军队采购资质的供应商，其所供应产品已通过军队集中采购管理程序审核，可视为属于《全军用计算机及网络设备集中采购目录》范围内产品。

核查结论形成，在完成设备清单、采购来源、采购文件及供应商资质核查后，形成统一核查结论。确认系统中使用的网络设备、服务器及终端设备均通过军队集中采购或统一配发渠道取得，所选产品符合《全军用计算机及网络设备集中采购目录》相关要求，并满足现行军队信息化管理和保密管理规定。

## 6.2 使用的安全网关、防火墙等信息安全产品，应通过JD信息安全测评认证机构的认证

信息安全产品范围确认，对系统中部署和使用的信息安全产品进行全面梳理，明确核查对象范围，重点包括安全网关、防火墙、入侵检测与防御系统、堡垒机、日志审计系统等信息安全专用设备。通过形成信息安全产品清单，确保所有承担安全防护功能的设备均纳入本次认证核查范围。

认证要求适用性确认结合系统建设性质和管理要求，确认所使用的信息安全产品需通过军队信息安全测评认证机构认证。明确安全网关、防火墙等产品属于军队信息系统中关键安全防护设备，按照相关管理规定，应选用经军队信息安全测评认证的产品方可部署使用。

认证证书核查，对信息安全产品逐一核查其认证情况，重点查验产品是否取得军队信息安全测评认证机构出具的有效认证证书或测评通过证明材料。核查认证证书所列产品名称、型号及认证有效期，确认与实际部署设备一致，且认证状态处于有效期内。

采购与来源佐证核查，结合采购合同、招标文件或供货清单，核实信息安全产品的采购来源及合规性，确认所采购产品为已通过军队信息安全测评认证的型号或系列产品。通过采购文件与认证证书的对应关系，进一步验证产品认证情况的真实性和有效性。

部署一致性核查，核查现场实际部署的信息安全产品型号、规格与认证证书及采购文件的一致性，确认实际运行设备未发生型号替换或降级使用等情况，确保投入使用的信息安全产品与通过军队信息安全测评认证的产品保持一致。

核查结论形成，在完成信息安全产品清单梳理、认证证书核查、采购文件比对及现场部署一致性核查后，形成统一核查结论。确认系统中使用的安全网关、防火墙等信息安全产品均已通过军队信息安全测评认证机构认证，符合军队信息系统安全建设和管理相关要求。

## 6.3 机房应有序、规范、合理走线布线，明确互联网区域和内部区域，粘贴标识，区分不同线路

机房布线现状梳理，对机房内现有线路布放情况进行全面检查，重点包括网络线、电源线、光纤及其他弱电线路，梳理线路走向、敷设方式及集中情况。通过现场核查，确认机房内线路整体布局清晰，无随意拉设、交叉混乱或悬挂现象，为后续规范化管理提供基础。

线路规范与整理核查，对机房内线路是否采用线槽、线架或扎带进行规范固定进行核查，确认线路敷设整齐有序，符合“横平竖直、分层布放”的基本要求。对存在凌乱、无固定或影响设备维护的线路进行整理，确保整体布线规范、便于识别和维护。

区域划分情况核查，对机房网络区域划分情况进行核查，重点确认是否明确区分互联网区域与内部网络区域。通过机柜分区、物理隔离或明显位置标识等方式，实现不同网络区域的清晰区分，避免不同安全级别网络线路混用或交叉。

线路类型区分核查，核查机房内是否对不同用途、不同安全级别的线路进行区分管理，包括互联网线路、内部业务线路、管理线路等。通过颜色区分、标签标识或走线分层等方式，确保不同类型线路一目了然，便于运维和安全管理。

标识粘贴与信息完整性核查，对机房内线路及机柜标识情况进行核查，确认在线路两端、机柜、交换设备等关键位置粘贴清晰、规范的标识标签。标识内容应包含线路用途、所属区域或设备名称，确保标识信息完整、准确、易于识别。

核查结论形成，在完成布线整理、区域划分、线路区分及标识核查后，形成统一核查结论。确认机房布线整体有序、规范、合理，互联网区域与内部区域划分明确，不同线路区分清晰并粘贴规范标识，符合机房安全管理和运维管理相关要求。

## 6.4 机房应符合GB 2887-2011中4.6.1所规定的温湿度等要求

确认适用标准，核查机房环境控制是否执行《GB 2887-2011 电子计算机场地通用规范》，明确机房温湿度管理需符合该标准第 4.6.1 条相关要求。

查看环境监测手段，核查机房是否配备温湿度监测设备，如温湿度传感器、监控系统或独立温湿度表，确认能够对机房环境参数进行实时或定期监测。

检查运行记录，查阅机房温湿度监测记录、巡检记录或监控系统历史数据，确认有持续、稳定的环境监测记录可供追溯。

现场抽查验证，通过现场查看温湿度显示数据或抽查监测终端，核实机房当前温湿度状态处于正常运行范围。

形成核查结论，综合标准适用性、监测手段、历史记录及现场抽查情况，确认机房温湿度控制符合 GB 2887-2011 第 4.6.1 条规定要求。

## 6.5 机房应安装安防监控设备，对机房的人员进出、设备操作等进行监管

安防监控配置确认，核查机房是否安装安防监控设备，重点确认在机房出入口、主要通道及关键设备区域部署视频监控设施，确保对机房整体环境具备基本的可视化监管能力。

人员进出监控核查，核查监控设备是否能够覆盖机房人员进出区域，确认能够对人员进入、离开机房的过程进行有效记录，并与门禁或出入管理措施相配合，实现人员进出行为的监管。

设备操作监管核查，核查监控范围是否覆盖机柜、服务器及网络设备操作区域，确认能够对设备维护、操作过程进行监控记录，具备事后追溯和管理支撑能力。

监控运行与记录核查，检查安防监控系统运行状态及视频存储情况，确认监控设备处于正常工作状态，相关监控记录可正常保存和调取，满足日常管理和检查需要。

核查结论形成，综合安防监控部署情况、人员进出覆盖、设备操作监管及系统运行状态，确认机房已安装安防监控设备，并对人员进出及设备操作行为实施有效监管，符合机房安全管理要求。

## 6.6 机房应将涉密区域和互联网区域设置于不同场所

区域划分情况确认，核查机房整体布局，确认是否根据业务和安全属性对机房区域进行划分，明确区分涉密区域与互联网区域，确保不同安全等级的系统和设备具备独立的物理空间。

物理隔离情况核查，核查涉密区域与互联网区域是否设置在不同场所或独立机房内，确认两类区域在空间位置上不存在混用、共用或无明显隔离的情况，满足物理隔离要求。

设备部署一致性核查，核查涉密设备与互联网设备的部署情况，确认涉密系统设备仅部署在涉密区域，互联网系统设备仅部署在互联网区域，避免跨区域部署或混放现象。

标识与管理措施核查，核查机房是否对涉密区域和互联网区域设置清晰标识，并采取相应的管理措施，确保运维和管理人员能够明确识别不同区域，防止误入或误操作。

核查结论形成，综合区域划分、物理隔离、设备部署及标识管理情况，确认机房已将涉密区域和互联网区域设置于不同场所，符合信息安全与保密管理相关要求。

## 6.7 在涉密网络中使用过的打印机、复印机、刻录机、扫描仪、存储载体等设备严禁在互联网中使用

涉密设备范围确认，对涉密网络中使用过的设备进行梳理，明确核查对象范围，重点包括打印机、复印机、刻录机、扫描仪以及各类存储载体等可能承载涉密信息的设备，形成涉密设备清单，确保全部纳入管理范围。

使用环境核查，核查上述涉密设备的当前使用环境，确认设备仅在涉密网络或涉密区域内使用，未接入互联网环境，也未部署在互联网区域机房或办公区域，避免跨网、跨区使用情况发生。

物理隔离核查，核查涉密设备与互联网环境之间的物理隔离情况，确认涉密设备未与互联网设备混放、共用接口或共用线路，相关设备在空间位置和连接关系上保持清晰隔离。

标识与管理措施核查，核查涉密设备是否粘贴明显的涉密标识，并通过台账登记、责任人明确等管理措施加以约束，确保涉密设备用途明确，防止被误带、误用或违规接入互联网环境。

核查结论形成，综合涉密设备清单、使用环境、物理隔离及标识管理情况，确认在涉密网络中使用过的打印机、复印机、刻录机、扫描仪及存储载体等设备，均未在互联网环境中使用，符合保密管理相关要求。

## 6.8 应选用《JY关键软硬件自主可控产品名录》中的芯片类、计算机及外设类、网络设备类、安全防护设备类、存储设备类等产品

设备和产品范围确认，对系统中使用的芯片、计算机及外设、网络设备、安全防护设备、存储设备等关键软硬件产品进行全面梳理，明确核查对象范围，形成关键软硬件产品清单，确保相关设备均纳入自主可控产品选用核查范围。

名录适用性确认，结合系统建设和使用要求，确认上述关键软硬件产品应选用《军用关键软硬件自主可控产品名录》中的相关产品类型，明确芯片类、计算机及外设类、网络设备类、安全防护设备类和存储设备类等均属于名录管理范围。

采购与选型依据核查，核查产品采购合同、招标文件或技术选型说明，确认在采购或选型过程中已明确要求选用《军用关键软硬件自主可控产品名录》内产品，确保产品选型依据符合自主可控相关管理要求。

产品来源与一致性核查，核查实际部署使用的产品型号、类别与采购文件及选型要求的一致性，确认系统中使用的关键软硬件产品与自主可控名录要求相符，未使用不符合名录要求的产品替代。

核查结论形成，在完成产品清单梳理、名录适用性确认、采购选型依据及实际使用情况核查后，形成统一核查结论。确认系统所使用的芯片、计算机及外设、网络设备、安全防护设备、存储设备等关键软硬件产品，均符合《军用关键软硬件自主可控产品名录》相关要求。

## 6.9 与安全防护等级低的网络使用不同色系线缆进行严格隔离区分

网络等级范围确认，对机房内不同安全防护等级的网络进行梳理，明确需进行区分管理的网络范围，重点识别安全防护等级较高网络与安全防护等级较低网络，作为线缆区分和隔离管理的对象。

线缆颜色区分核查，核查不同安全防护等级网络是否采用不同色系线缆进行布放，确认安全防护等级较高网络与安全防护等级较低网络在线缆颜色上存在明显区分，便于直观识别和管理。

物理隔离情况核查，核查不同色系线缆是否在布线路径、线槽或线架中进行有效隔离，确认不同安全等级网络线缆未混合捆扎或交叉布放，降低误接、误插风险。

标识与管理措施核查，核查不同色系线缆是否配套设置清晰标识，并结合线缆颜色区分落实相应管理要求，确保运维人员能够准确识别不同安全防护等级网络线路。

核查结论形成，综合网络等级识别、线缆颜色区分、物理隔离及标识管理情况，确认与安全防护等级较低的网络已采用不同色系线缆进行严格隔离区分，符合网络安全管理相关要求。

## 7 组织机构

## 7.1 应设置安全管理机构，保证系统安全措施的落实

安全管理机构设置核查，核查单位是否设立信息安全管理机构或明确承担系统安全管理职责的部门，确认系统安全管理工作有明确的组织保障。

安全职责明确核查，核查是否明确系统安全管理责任人及相关岗位职责，确保安全管理责任落实到部门和个人。

安全管理制度核查，核查是否建立与安全管理机构相配套的安全管理制度，支撑系统安全管理工作的开展。

安全措施落实核查，核查安全管理机构对系统安全措施落实情况的组织和监督情况，确认相关安全措施得到有效执行。

核查结论形成，综合安全管理机构设置、职责明确性及安全措施落实情况，确认已设置安全管理机构，能够保障系统安全措施落实到位，符合管理要求。

## 7.2 应配备专职系统安全保密管理人员，负责系统安全措施的落实

专职人员配备核查，核查单位是否配备专职系统安全保密管理人员，确认相关人员岗位设置明确，专门负责系统安全与保密管理工作。

职责明确性核查，核查专职系统安全保密管理人员的职责分工，确认其负责系统安全措施的组织、落实和监督，职责内容清晰、责任到人。

履职情况核查，核查专职人员在系统安全管理中的实际履职情况，如参与安全管理、检查和日常管理工作，确认安全措施得到有效落实。

核查结论形成，综合专职人员配备、职责明确性及履职情况，确认已配备专职系统安全保密管理人员，能够有效保障系统安全措施落实。

## 7.3 应具有安全管理领导机构，督导系统安全措施的落实

领导机构设置核查，核查单位是否设立安全管理领导机构，或明确由单位领导牵头负责系统安全管理工作，确认系统安全工作具备统一领导和组织保障。



领导职责明确核查，核查安全管理领导机构的职责分工，确认其对系统安全工作的决策、统筹和督导职责明确，能够对安全措施落实情况进行管理。

督导机制核查，核查安全管理领导机构是否建立安全工作督导机制，如定期检查、会议部署或工作通报，确保系统安全措施得到持续推动和落实。

核查结论形成，综合领导机构设置、职责明确性及督导机制情况，确认已具备安全管理领导机构，并能够有效督导系统安全措施落实，符合管理要求。

## 7.4 应具有系统安全保密技术管理人员，具体负责安全保密技术措施的落实，保证系统安全运行

技术管理人员设置核查，核查单位是否配备系统安全保密技术管理人员，明确其岗位设置及人员配置情况，确认系统安全保密技术管理工作具有专门的人员保障。

技术职责明确性核查，核查系统安全保密技术管理人员的职责界定情况，确认其具体承担安全保密技术措施的实施、维护与技术管理工作，职责内容清晰、分工明确。

技术措施落实情况核查，核查系统安全保密技术管理人员在系统运行中的实际履职情况，确认其能够组织落实访问控制、边界防护、审计监控等安全保密技术措施，保障技术措施有效运行。

系统运行保障核查，核查技术管理人员对系统安全运行的保障情况，确认其通过持续监控、技术维护和问题处置，确保系统处于安全、稳定运行状态。

核查结论形成，综合人员配置、职责明确性、技术措施落实及系统运行保障情况，确认已配备系统安全保密技术管理人员，能够有效落实安全保密技术措施，保障系统安全运行。

## 7.5 应具有完善的应急响应体系，应对突发事件

应急响应体系建立情况核查，核查单位是否建立覆盖系统安全运行的应急响应体系，明确突发事件应急处置的组织架构、职责分工和工作机制，确认应急响应工作具有制度和组织保障。

应急预案完备性核查，核查是否制定与系统安全相关的应急预案，明确突发事件的处置流程、响应级别及处置措施，确保应急处置工作具备可执行的操作依据。

应急资源与能力保障核查，核查应急响应体系中人员、技术和资源保障情况，确认具备开展应急处置所需的技术支持和协调能力，能够支撑突发事件的快速响应。

应急响应实施情况核查，核查应急响应体系在实际运行中的执行情况，如应急演练、事件处置或相关记录，确认应急响应机制能够在突发事件发生时有效启动并落实。

核查结论形成，综合应急体系建设、预案完备性、资源保障及实施情况，确认已建立较为完善的应急响应体系，能够有效应对系统运行中的突发事件。

## 8 规章制度

## 8.1 应具有日常安全管理制度、入网审批制度和系统安全保密检查制度等

安全管理制度建立情况核查，核查单位是否建立覆盖系统运行全过程的日常安全管理制度，明确安全管理要求和执行规范，确保系统安全管理工作有章可循。

入网审批制度核查，核查是否建立入网审批制度，对人员、设备和系统接入实行审批管理，明确审批流程和责任分工，防止未经授权的接入行为。

系统安全保密检查制度核查，核查是否建立系统安全保密检查制度，明确检查内容、频次及责任主体，确保能够通过定期或专项检查发现并整改安全隐患。

制度执行情况核查，核查上述安全管理制度在实际运行中的执行情况，通过制度文本、执行记录或检查台账，确认相关制度得到有效落实。

核查结论形成，综合安全管理制度建设、入网审批制度和安全保密检查制度的建立及执行情况，确认已具备较为完善的安全管理制度体系，能够有效支撑系统安全运行。

## 8.2 应具有安全管理操作规程、安全监控操作规程、安全审计操作规程、应急响应操作规程等

安全管理操作规程建立情况核查，核查单位是否制定安全管理操作规程，明确日常安全管理工作的操作要求和执行流程，确保安全管理活动规范开展。

安全监控操作规程核查，核查是否制定安全监控操作规程，明确安全监控系统的使用、维护和处置流程，确保监控措施能够持续、有效运行。

安全审计操作规程核查，核查是否制定安全审计操作规程，明确审计对象、审计方法及记录要求，确保系统运行行为具备可审计性和可追溯性。

应急响应操作规程核查，核查是否制定应急响应操作规程，明确突发事件处置的具体操作步骤和响应要求，确保应急处置过程规范、有序实施。

核查结论形成，综合各类安全操作规程的建立及执行情况，确认已形成较为完整的安全操作规程体系，能够有效支撑系统安全管理和运行保障。

## 8.3 应具有日常系统备份制度和存储载体使用管理制度

日常系统备份制度核查，核查单位是否建立日常系统备份制度，明确备份对象、备份周期、备份方式及管理要求，确保系统数据具备完整性和可恢复性保障。

存储载体使用管理制度核查，核查是否建立存储载体使用管理制度，对存储介质的使用、保管、流转及销毁等环节进行规范管理，防止存储载体违规使用或信息泄露风险。

制度执行情况核查，核查上述制度在实际运行中的执行情况，通过制度文本、台账记录或相关凭证，确认制度要求得到有效落实。

核查结论形成，综合日常系统备份制度和存储载体使用管理制度的建立及执行情况，确认已具备相应管理制度，能够有效保障系统数据安全和存储载体安全。

## 8.4 应具有脆弱性分析等规程

脆弱性分析规程建立情况核查，核查单位是否制定脆弱性分析相关规程，明确分析对象、分析方法和实施流程，确保脆弱性分析工作有章可循。

脆弱性分析实施情况核查，核查脆弱性分析规程在系统运行中的执行情况，通过分析记录、报告或整改台账，确认脆弱性分析工作得到有效开展。

脆弱性整改管理核查，核查是否对脆弱性分析发现的问题进行分类、处置和整改，确认整改措施落实到位，形成闭环管理机制。

核查结论形成，综合脆弱性分析规程建立及执行情况，确认已具备脆弱性分析等相关规程，能够支撑系统安全风险识别与控制。

## 9 管理实施

## 9.1 应具有应急响应预案，当发生危及系统安全的事件时应根据操作规程及时采取措施，符合应急预案启动条件时按预案开展应急措施

应急响应预案建立情况核查，核查单位是否制定覆盖系统安全运行的应急响应预案，明确应急处置的适用范围、启动条件及总体原则，确保在发生危及系统安全的事件时，具备明确的预案依据和组织指导。

操作规程衔接情况核查，核查应急响应预案是否与相关操作规程有效衔接，明确在系统安全事件发生后，各类技术和管理措施的具体操作步骤，确保应急处置过程中能够按照既定规程及时、规范地采取措施。

应急预案启动条件核查，核查应急响应预案中对应急启动条件的界定情况，确认对不同类型、不同影响程度的安全事件设定了清晰的启动标准，确保在符合条件时能够及时启动相应级别的应急响应。

应急措施实施情况核查，核查在应急预案启动后，应急措施的组织和实施情况，包括技术处置、风险控制及协同响应等内容，确认应急措施能够按照预案要求有序开展，并有效控制系统安全风险。

核查结论形成，综合应急响应预案建立、操作规程衔接、启动条件界定及应急措施实施情况，确认已具备较为完善的应急响应预案体系，能够在发生危及系统安全的事件时及时启动预案并有效落实应急措施。

## 9.2 计算机信息系统安全保密管理人员应能对网络中的网络安全防护设备进行统一配置、管理，并能实现安全管理中心与网络安全防护设备之间的响应

安全保密管理人员能力配置核查，核查是否配备具备相应技术能力的计算机信息系统安全保密管理人员，确认其能够承担网络安全防护设备的统一配置与管理工作，保障安全防护体系的整体一致性。

网络安全防护设备统一管理核查，核查安全保密管理人员对防火墙、入侵检测与防御系统、安全网关等网络安全防护设备的集中配置和管理情况，确认相关设备能够按照统一安全策略进行运行和维护。

安全管理中心联动能力核查，核查安全管理中心与网络安全防护设备之间的联动与响应能力，确认安全管理中心能够对安全防护设备进行集中监控、策略下发和状态管理，实现安全事件的协同响应。

响应机制有效性核查，核查在安全事件发生时，安全管理中心与网络安全防护设备之间的响应机制是否能够及时启动并有效执行，确保安全处置措施快速、准确落实。

核查结论形成，综合人员能力、设备统一管理及管理中心联动响应情况，确认计算机信息系统安全保密管理人员具备对网络安全防护设备进行统一配置、管理及联动响应的能力，能够有效支撑系统安全运行。

## 9.3 应具有与实际情况相符且完整的安全保密策略文档和安全保密技术及产品配置的详细记录

安全保密策略文档完备性核查，核查是否形成与系统实际运行环境相符的安全保密策略文档，内容涵盖安全目标、管理原则及主要控制措施，确认策略文件结构完整、表述清晰，并能够指导系统安全保密工作的实施。

安全保密策略适配性核查，核查安全保密策略是否与系统规模、业务特点及安全防护需求相匹配，确认策略内容与实际部署的安全技术措施和管理要求一致，避免出现策略与实际运行脱节的情况。

安全保密技术配置记录核查，核查安全保密相关技术措施及安全产品的配置记录情况，确认对安全设备、系统参数及关键防护策略等内容进行了详细、规范的记录，能够真实反映系统当前的安全配置状态。

配置记录完整性与可追溯性核查，核查安全保密技术及产品配置记录的完整性和可追溯性，确认配置变更过程有据可查，能够支撑安全审计、问题追溯和持续安全管理工作。

核查结论形成，综合安全保密策略文档的完备性、适配性及技术配置记录情况，确认已形成与实际情况相符且较为完整的安全保密策略文档和安全保密技术及产品配置的详细记录。

## 9.4 安全保密技术管理人员应对每日网络和系统运行情况实施安全审计，每月组织本级网络的安全性检测，编写安全审计与评估报告，并形成记录，系统配置发生变化的情况下应及时组织安全检测评估

日常安全审计实施情况核查，核查安全保密技术管理人员是否对网络和系统的日常运行情况开展持续性安全审计，重点关注日志审查、运行状态监控及异常行为分析，确认安全审计工作按日落实。

定期安全性检测组织情况核查，核查是否按要求每月组织开展本级网络的安全性检测，明确检测对象、检测内容和技术方法，确认定期检测工作具有计划性和规范性。

安全审计与评估报告核查，核查是否对安全审计和安全性检测结果进行汇总分析，并形成规范的安全审计与评估报告，确认报告内容完整、结论明确，能够反映网络和系统的安全状况。

审计与评估记录管理核查，核查安全审计与评估相关记录的保存和管理情况，确认审计过程、检测结果及整改情况均形成书面或电子记录，具备可追溯性。

配置变更后安全评估核查，核查在系统配置发生调整或变更的情况下，是否及时组织开展针对性的安全检测与评估，确认安全管理能够随系统变化同步更新，防止引入新的安全风险。

核查结论形成，综合日常安全审计、定期安全检测、报告编制及配置变更后评估情况，确认安全保密技术管理人员能够持续开展安全审计与评估工作，有效保障系统安全运行。

## 10 协议安全审计

## 10.1 审计协议的机密性、完整性、认可性、不可否认性

审计协议机密性核查，核查审计协议在数据传输和存储过程中是否采取有效的加密和访问控制措施，防止审计数据被未授权访问或泄露，确保审计信息仅对授权主体可见。

审计协议完整性核查，核查审计协议是否具备完整性保护机制，确保审计数据在生成、传输和存储过程中未被非法篡改或破坏，能够准确、真实地反映系统运行和安全事件情况。

审计协议认可性核查，核查审计协议是否对审计数据的来源和主体进行有效标识和认证，确保审计记录能够被相关管理和审计人员认可，具备作为安全管理和审计依据的有效性。

审计协议不可否认性核查，核查审计协议是否通过身份认证、日志签名等技术手段，确保审计行为和审计记录的产生主体事后无法否认其操作行为，为责任认定和事件追溯提供可靠依据。

核查结论形成，综合审计协议在机密性、完整性、认可性及不可否认性方面的保障情况，确认审计协议能够满足系统安全审计对数据可信性和安全性的要求。

